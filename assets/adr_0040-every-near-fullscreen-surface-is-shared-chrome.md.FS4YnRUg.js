import{_ as t,o as a,c as o,a0 as r}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0040: Every near-fullscreen surface is shared chrome","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0040-every-near-fullscreen-surface-is-shared-chrome.md","filePath":"adr/0040-every-near-fullscreen-surface-is-shared-chrome.md"}'),s={name:"adr/0040-every-near-fullscreen-surface-is-shared-chrome.md"};function n(i,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-0040-every-near-fullscreen-surface-is-shared-chrome" tabindex="-1">ADR 0040: Every near-fullscreen surface is shared chrome <a class="header-anchor" href="#adr-0040-every-near-fullscreen-surface-is-shared-chrome" aria-label="Permalink to &quot;ADR 0040: Every near-fullscreen surface is shared chrome&quot;">​</a></h1><p>Status: accepted Date: 2026-09-20</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Live view was the plane&#39;s last near-fullscreen surface outside the shared chrome. It drew its own bordered box, ran its own keys on a local handler, and had no Action bar and no Message line. Its settled sub-mode reused the decision&#39;s layout math by importing it from a sibling surface, and its keys: select, confirm, edit, scroll, cancel: had no catalogue controls at all, so the Control catalogue, the Action bar, and the Key guide could not name what the operator was pressing.</p><p>The Decision modal already moved its layout to the shared chrome, but its box accepted its body as raw children and its border color as a per-caller argument, and the plane spelled the same border ink five ways.</p><p>The Live view also switches sub-modes under the operator: <code>liveMode</code> re- derives stream, decision, missing, and closed from the ticket&#39;s facts on every render, so the turn can settle while the operator reads the stream, and a settled turn on an auto-close task type keeps streaming.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Live view is a shared-chrome surface.</strong> It renders on the modal surface, gains the Message line and the Action bar, and its keys dispatch from the Control catalogue. Its stream sub-mode answers to a <code>live-view</code> mode of its own, whose controls are the body&#39;s scroll, the Goto, and the cancel. Its settled sub-mode dispatches in the existing <code>decision-modal</code> mode, which <code>CONTEXT.md</code> already states: the Live view becomes the Decision modal when the turn settles.</p><p><strong>The sub-mode switch happens in place.</strong> One surface, one pop-in per opening. The border re-titles <code>Live: &lt;title&gt;</code> to <code>Decision: &lt;title&gt;</code> when the turn settles for the operator, and the chrome&#39;s body API owns the prefix so the two paths into the decision, Enter on an <code>awaiting</code> ticket and a turn settling under an open Live view, cannot drift. The bar&#39;s hints change with the mode.</p><p><strong>The in-box hint row dies.</strong> The Action bar is the plane&#39;s only place for keys, at every size and in every surface, and the row it frees goes to the body.</p><p><strong>The chrome owns the box.</strong> A modal&#39;s body is a typed body region, not raw children, so a surface cannot hand the chrome a box of its own. The border ink is the control ink&#39;s indicator, the checked essential-indicator pair, and no surface states a border color. The architecture check refuses a surface that paints action rows without the library&#39;s region state, so the drift the local Live view carried cannot come back.</p><p><strong>The Decision region&#39;s selection is a control behavior.</strong> It lives in the shared control library, one module beside the field, the selector row, and the form: the selection, its wrap, the auto-scroll that keeps the cursor&#39;s row visible, the visible window, and the range text. The chrome and the catalogue consume it.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><p><strong>Keep the Live view outside the chrome, and give it only the pane.</strong> Rejected: it is the drift the shared-control standard names, and it leaves the plane&#39;s largest key system outside the catalogue.</p><p><strong>Close and reopen on settle.</strong> Rejected: a second pop-in in the middle of an unread log moves the text under the operator&#39;s eyes, and it throws away the scroll position the operator held.</p><p><strong>Two catalogue modes for the two sub-modes.</strong> Rejected: the settled sub-mode is the Decision modal by the glossary, and duplicating a control set under a second name is a second truth for one key.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The guide images of the Live view and its settled turn are re-captured from the production binary, and the operation page&#39;s promise that the border reads <code>Live: &lt;title&gt;</code> in every state is replaced by the re-title rule. The verification record gains unverified targets for the Live view&#39;s new bar and Message line, and for the nested border at the declared minimum. ADR 0039&#39;s pane and region are the Live view&#39;s body, and this ADR is what lets the pane mean the same thing on both surfaces.</p>',18)])])}const m=t(s,[["render",n]]);export{p as __pageData,m as default};
