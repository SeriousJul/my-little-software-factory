import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0026: Auto-handoff holds a completed task type","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0026-auto-handoff-holds-a-completed-task-type.md","filePath":"adr/0026-auto-handoff-holds-a-completed-task-type.md"}'),n={name:"adr/0026-auto-handoff-holds-a-completed-task-type.md"};function l(i,e,h,r,d,c){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0026-auto-handoff-holds-a-completed-task-type" tabindex="-1">ADR 0026: Auto-handoff holds a completed task type <a class="header-anchor" href="#adr-0026-auto-handoff-holds-a-completed-task-type" aria-label="Permalink to &quot;ADR 0026: Auto-handoff holds a completed task type&quot;">​</a></h1><p>Status: accepted Date: 2026-09-15</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0005 ends a work cycle at close and returns the ticket to <code>open</code> at once, so the close-and-rehandoff loop is possible; the Handoff limit bounds it. The re-verify gate behind the open auto-handoff defends the loop against one case: the cycle&#39;s agent changed the source item (a merged pull request, a closed issue), and the stale fetch still lists it. The gate waits for the source to re-read the ticket; an item that left the list does not dispatch, and an item that is still open re-verifies and dispatches.</p><p>The gate&#39;s premise fails for work that hands off to another ticket. The <code>implement</code> task type opens a pull request and leaves its issue open: the pull request closes the issue at merge, and the follow-up work (review, merge) runs on the pull request&#39;s own ticket. For the whole review window the issue is open, listed, and actionable, and the task rules still suggest <code>implement</code>. Auto-handoff closes the cycle, the gate re-verifies the still-open issue, and the dispatch starts <code>implement</code> again on finished work. Each repeat re-runs an agent on a clean tree; the only bound is the Handoff limit, a dozen idle agent runs per ticket.</p><p>The label the source query filters on is the pipeline&#39;s &quot;work is needed&quot; signal, and the task template tells the implementing agent to remove it when it opens the pull request. An agent that omits the removal leaves the signal up, and the control plane cannot tell a left-behind signal from real new work: both read as the same suggestion.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Same-type hold.</strong> The open auto-handoff does not dispatch a ticket whose newest closed cycle settled a <code>completed</code> turn of exactly the task type the ticket now suggests. A completed work needs no repeat; progress needs a new signal. The hold ends when the ticket&#39;s suggested task type changes - the agent or the operator flips a label the rules read - or the ticket leaves the source list.</p><p>The hold reads the newest cycle-end decision - the same row the re-verify gate reads - and the task type and cause that row records. A cycle closed after an <code>aborted</code> or <code>failed</code> turn holds nothing: that work did not finish, and a retry is the next move. A cycle whose turn never settled, whose abandon wrote its own trace row, holds nothing: the row carries no cause.</p><p>The hold gates the open auto-handoff only. A manual handoff always passes it, exactly as the Parallel limit and the Handoff limit do: the operator who re-hands-off a just-completed type chooses the repeat, and the control plane records the choice in a new cycle. Workflow routes are untouched: they start from a routed decision inside an open cycle, never from a closed one.</p><p>The hold is derived from the completion traces on every cycle and never stored, so a restart cannot lose it and it cannot drift from the fact it describes, exactly as the Dispatch pause (ADR 0016).</p><p>The considered alternatives:</p><ul><li>Hold on raw label churn, not on the suggestion. Rejected: the suggestion is the work the dispatch would start; comparing facts the dispatch never uses still starts the same finished work when the churn keeps the suggestion constant.</li><li>Store a snapshot of the item&#39;s facts at cycle end and compare it to the newest fetch. Rejected: it stores state per cycle the control plane must keep, and it fails the same way when the agent&#39;s own edits keep the suggestion constant.</li><li>Let auto mode auto-close only the auto-close task types. Rejected: it stalls the label-driven pipeline. The review-to-merge progression on a pull request rides the close-and-rehandoff loop, and holding every non-auto-close type in <code>awaiting</code> parks the pipeline on an operator keystroke per ticket.</li><li>Have the control plane clear the work signal itself. Rejected: the control plane never writes to the external source; the label is the operator&#39;s and the agents&#39; fact.</li><li>Do nothing and let the Handoff limit bound the repeat. Rejected: a dozen idle agent runs per ticket to learn what the hold states in one cycle.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>An <code>implement</code> that finishes while its issue stays open rests as an open ticket the auto-handoff will not touch until the suggestion moves. The ticket is visible and stays manually handoff-able: the operator can force the repeat, and the control plane records it in a new cycle.</li><li>The label-driven pipeline keeps its rhythm: review settles, the label flips, the suggestion changes to <code>merge</code>, and the dispatch runs. The hold never fires on a changed suggestion.</li><li>The Handoff limit keeps its role as the last bound. The hold is the first line, and it states in one cycle what the limit only bounds.</li><li>A cycle the operator closes by hand after a completed turn holds the same way: the close is a cycle end, and auto-handoff will not repeat the completed type without a new signal. The operator who wants the repeat presses it themselves.</li><li>The re-verify gate, the Dispatch pause, and the Parallel limit are untouched. The hold is one more derived check on the same open auto-handoff path, beside them.</li></ul>',15)])])}const f=t(n,[["render",l]]);export{u as __pageData,f as default};
