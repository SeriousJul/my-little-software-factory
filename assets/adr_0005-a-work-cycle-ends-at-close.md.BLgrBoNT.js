import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const f=JSON.parse('{"title":"ADR 0005: A work cycle ends at close","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0005-a-work-cycle-ends-at-close.md","filePath":"adr/0005-a-work-cycle-ends-at-close.md"}'),d={name:"adr/0005-a-work-cycle-ends-at-close.md"};function c(s,e,i,r,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0005-a-work-cycle-ends-at-close" tabindex="-1">ADR 0005: A work cycle ends at close <a class="header-anchor" href="#adr-0005-a-work-cycle-ends-at-close" aria-label="Permalink to &quot;ADR 0005: A work cycle ends at close&quot;">​</a></h1><p>Status: accepted Date: 2026-08-31</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The work cycle model held a finished ticket in <code>done</code> until it left all sources and later returned. Auto-handoff and workflow routing change the needs: the operator accepts a settled agent turn, and the ticket must be usable again at once while its source item is still present and active. Holding the ticket as <code>done</code> would hide it from handoff, and re-arming it would need a special case for every just-closed ticket.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Closing a cycle, by the operator or by auto-close, ends the work cycle, stores the completion trace, and returns the ticket to <code>open</code> at once with an incremented cycle number. A cycle can hold several handoffs, so a workflow chain (implement, then review, then merge) is one cycle. <code>done</code> stops being a resting ticket state; it lives only in the work cycle record. The resting states are <code>open</code>, <code>handed-off</code>, <code>running</code>, and <code>awaiting</code>.</p><p>The considered alternatives:</p><ul><li>Hold the ticket as <code>done</code> until it leaves all sources and returns, the old model. Rejected: a closed ticket would stay invisible to handoff while its source item is still active, and auto-handoff would need a re-arm rule for every just-closed ticket.</li><li>Keep <code>done</code> as a resting state and add a separate re-open action. Rejected: two ways to end one cycle, and a state that means both &quot;finished&quot; and &quot;waiting to be re-opened&quot;.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The forward note in ADR 0002 (&quot;agent state for the <code>running</code> and <code>done</code> ticket states&quot;) lands as <code>running</code> and <code>awaiting</code> instead.</li><li>Immediate re-availability makes the close-and-rehandoff loop possible, so the per-ticket handoff limit bounds auto-handoff.</li><li>Source facts still never reset factory state. Close is a factory action and never touches the external source item.</li></ul><h2 id="amendment-a-close-of-a-decided-turn-ends-the-cycle-with-the-decision-standing" tabindex="-1">Amendment: a close of a decided turn ends the cycle with the decision standing <a class="header-anchor" href="#amendment-a-close-of-a-decided-turn-ends-the-cycle-with-the-decision-standing" aria-label="Permalink to &quot;Amendment: a close of a decided turn ends the cycle with the decision standing&quot;">​</a></h2><p>Date: 2026-09-21</p><p>A transition route records its decision on the settled turn when the routed handoff starts. The operator&#39;s instance routed an issue to its fixing pull request, and the issue then rested in <code>awaiting</code> wearing its <code>handed-off</code> decision. The close refused the turn: the decision write runs only on a pending trace, and a decided trace is not pending. The routed ticket&#39;s work cycle could never end, and its environment - the worktree, the workspace, the idle agent that still held the ticket&#39;s herdr name - could never be cleaned up.</p><p>The decision above is amended for that case: <strong>a close on a turn that already decided ends the cycle without rewriting the decision.</strong> The recorded decision stands - a fact is not rewritten - and the ticket returns to <code>open</code> with the cycle incremented, exactly as the closed decision leaves it. The move runs only from <code>awaiting</code>, so a repeated close changes nothing, and the cycle number still moves exactly once per end. The automatic close degrades the same way: a route that meets the handoff limit after its decision stands still ends the cycle. The automatic rule likewise decides a decided turn nothing more: the routed turn rests for the operator&#39;s close, and the poll routes no second time on the turn the route already decided.</p>',14)])])}const m=t(d,[["render",c]]);export{f as __pageData,m as default};
