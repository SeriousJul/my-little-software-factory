import{_ as t,o,c as n,a0 as s}from"./chunks/framework.D4fUSYzX.js";const a="/my-little-software-factory/assets/live-view.MGXJjrKa.png",i="/my-little-software-factory/assets/turn-log.BNw6qzxF.png",g=JSON.parse('{"title":"Live view","description":"Watch an agent that is running, and the decision the same screen carries when the turn settles.","frontmatter":{"title":"Live view","description":"Watch an agent that is running, and the decision the same screen carries when the turn settles."},"headers":[],"relativePath":"operation/live-view.md","filePath":"operation/live-view.md"}'),r={name:"operation/live-view.md"};function h(d,e,c,l,p,m){return o(),n("div",null,[...e[0]||(e[0]=[s('<h1 id="live-view" tabindex="-1">Live view <a class="header-anchor" href="#live-view" aria-label="Permalink to &quot;Live view&quot;">​</a></h1><p><img src="'+a+`" alt="The Live view on an in-flight ticket: the agent&#39;s terminal output in the
Agent view pane and its context above"></p><p>Enter on an in-flight ticket opens the Live view: a near-fullscreen screen that pops in over the app with the same fade and grow as the decision modal, one cell of margin on every side. Its border reads <code>Live: &lt;ticket title&gt;</code> while the agent works. The first row under the border names the context: repository, task type, agent, and <code>blocked</code> when the ticket wears the blocked marker.</p><p>The body is the agent&#39;s terminal output as plain text, in its own pane titled <code>Agent view</code>. The screen reads the ticket&#39;s agent pane on a one-second cadence and keeps the newest <code>completion-message-lines</code> of it. New output pins the stream to the bottom; a scroll releases the pin, and reaching the bottom by scrolling pins it again. A failed read keeps the last good lines under a dim stale note, the note as the body&#39;s last line. A ticket without a recorded pane shows a note in place of the stream and reads nothing.</p><p>The Action bar follows the mode. While the agent works it offers the stream&#39;s scroll, the leave, and the Goto confirm. <code>Enter</code> is the Goto: pure focus, the same navigation as <code>g</code> in the Ticket section. It focuses the agent&#39;s pane and closes the screen. It changes no state and records no decision. The confirmation stands on the Message line and names the workspace the pane lives in: the Goto moves herdr&#39;s view to the agent&#39;s pane, which is the one focus move the control plane makes and the one the operator asked for at the key (ADR 0061). Ending an environment is not such a move: a Close cleanup, a route close, or a Consultation close never changes what the operator is looking at.</p><p>When the turn settles and the factory leaves the decision to the operator, the same screen carries the decision: the pane re-titles from <code>Agent view</code> to <code>Turn log</code>, the choice rows come in at the box&#39;s floor, the border re-titles from <code>Live:</code> to <code>Decision:</code> in the same place, and the bar&#39;s hints switch to the decision&#39;s. The switch is in place: the screen keeps its box, its scroll, and its pop-in, and no second screen opens over it. The one screen both paths end at is the one the <a href="./modals.html">decision modal</a> page describes, because it is the same one.</p><table tabindex="0"><thead><tr><th>Key</th><th>What it does</th></tr></thead><tbody><tr><td><code>j</code> / <code>k</code></td><td>Scroll the body, one row down or up</td></tr><tr><td><code>PgUp</code> / <code>PgDn</code></td><td>Scroll the body by a page</td></tr><tr><td><code>Home</code> / <code>End</code></td><td>Jump to the top or the bottom of the body</td></tr><tr><td><code>Up</code> / <code>Down</code></td><td>Move between the choice rows, once the decision is on screen</td></tr><tr><td><code>Enter</code></td><td>The Goto: focus the agent&#39;s pane and close the screen; choose the selected row, in the decision</td></tr><tr><td><code>e</code></td><td>Edit the selected Handoff row&#39;s settings before it starts, in the decision</td></tr><tr><td><code>Esc</code></td><td>Close the screen: nothing runs, the ticket stays where it is</td></tr></tbody></table><p>When the agent leaves herdr, the screen carries the <a href="./modals.html">missing modal</a> in its place. When the ticket leaves the in-flight and awaiting states, the screen closes. A settled turn the factory decides for itself never hands the screen over: the border keeps its <code>Live:</code> title, the pane keeps streaming, and the choice rows stay out.</p><h2 id="the-turn-log" tabindex="-1">The turn log <a class="header-anchor" href="#the-turn-log" aria-label="Permalink to &quot;The turn log&quot;">​</a></h2><p><img src="`+i+`" alt="The same Live view after the turn settles: the border re-titled to
, the Turn log pane, and the choice rows"></p><p>When the turn settles and the factory leaves the decision to the operator, the pane&#39;s body is the turn log of the settled turn, in the same dressing the <a href="./modals.html">decision modal</a> renders: the agent&#39;s text blocks, the tool calls as dim notes, and the choice rows below the pane. The choice rows and their keys are the modal&#39;s: Close, Goto, and the Handoff row of the position the settled turn&#39;s written labels derived, with <code>e</code> on a Handoff row editing that one handoff before it starts. When the choice rows hold more rows than the box has room for, the region shows as many as it can and scrolls, and its range rides the Action bar behind the selection&#39;s hint. The trace rules are the modal&#39;s rules, and the <a href="./../work-flow/completion.html">completion guide</a> records what a decision does to the ticket after.</p>`,11)])])}const f=t(r,[["render",h]]);export{g as __pageData,f as default};
