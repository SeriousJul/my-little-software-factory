import{_ as t,o as a,c as n,a0 as o}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0043: A live agent belongs to a ticket by name, not by pane id","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0043-a-live-agent-belongs-to-a-ticket-by-name-not-by-pane-id.md","filePath":"adr/0043-a-live-agent-belongs-to-a-ticket-by-name-not-by-pane-id.md"}'),s={name:"adr/0043-a-live-agent-belongs-to-a-ticket-by-name-not-by-pane-id.md"};function i(r,e,h,d,l,c){return a(),n("div",null,[...e[0]||(e[0]=[o('<h1 id="adr-0043-a-live-agent-belongs-to-a-ticket-by-name-not-by-pane-id" tabindex="-1">ADR 0043: A live agent belongs to a ticket by name, not by pane id <a class="header-anchor" href="#adr-0043-a-live-agent-belongs-to-a-ticket-by-name-not-by-pane-id" aria-label="Permalink to &quot;ADR 0043: A live agent belongs to a ticket by name, not by pane id&quot;">​</a></h1><p>Status: accepted Date: 2026-09-21</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The operator reported a ticket marked <code>running</code> whose Goto (key <code>g</code>) focused a Consultation&#39;s pane instead of the ticket&#39;s own agent pane. The record held the fact: the ticket&#39;s latest handoff recorded the pane id <code>wPJ:p1</code>, and the agent herdr listed in <code>wPJ:p1</code> ran under the name <code>consultation-27e1542c</code> - a Consultation&#39;s agent, not the ticket&#39;s.</p><p>The cause is herdr&#39;s pane allocation: it hands the id of a closed pane out again. A ticket&#39;s stored handle names an id, not an agent, so a stale handle can name a pane a different agent owns. Every attribution that trusted the pane id alone adopted the foreign agent as the ticket&#39;s own: the reclaim that runs a closed cycle&#39;s ticket again would have claimed the Consultation&#39;s agent for the ticket, the in-flight poll would have settled the ticket&#39;s turn on the foreign agent&#39;s reports, the Parallel seat count would have held the ticket&#39;s seat for it, and the plane&#39;s Goto and missing facts would have read the foreign pane as the ticket&#39;s live agent.</p><p>The agent name is the identity herdr already enforces: herdr refuses to start a second agent under a name a live agent holds, and a handoff asks for the name by candidate until one is free. The name the handoff started the agent under is therefore a fact the handoff can record, and the name herdr lists for a live agent is a fact the poll can read.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A live agent belongs to a ticket only when it runs under the name the ticket&#39;s handoff expects.</strong> The expected name is the name recorded on the ticket&#39;s latest handoff, or, for a handoff without a recorded name - one from before the column, or a Reclaimed handoff recorded before reclaims wrote the name - the stable name the handoff asked for first, derived from the ticket&#39;s title. The identity answers in three:</p><ul><li><code>own</code>: the live agent runs under the expected name. It is the ticket&#39;s own agent, and every attribution that trusted the pane id before stands.</li><li><code>foreign</code>: the live agent runs under any other name. The ticket&#39;s own agent is missing from its pane, the way an empty pane is missing. The reclaim adopts nothing, the in-flight poll runs the missing path, the Parallel count holds no seat for the ticket, the awaiting poll does not resume the ticket&#39;s pending turn, and the list shows the failure badge.</li><li><code>unverifiable</code>: the reader cannot read either name - an older herdr that lists no agent name, or an expected name the record holds no title for. The pane keeps the trust the plane has always given it, except the reclaim, which moves the ticket to <code>running</code> and so fails safe: it reclaims nothing it cannot verify.</li></ul><p><strong>The handoff records the name it started the agent under, and the reclaim records the name of the agent it reclaims.</strong> Every handoff this plane settles stores the name, and a Reclaimed handoff stores the reclaimed agent&#39;s name, so the next poll verifies the same identity instead of inheriting an unnamed record. The state layer is the last gate: the reclaim refuses any name that is not the ticket&#39;s own, whatever the caller believes.</p><p><strong>Goto gives the identity its own refusal (ADR 0033).</strong> The base key refuses an in-flight ticket whose pane holds a foreign agent, the way it refuses one whose pane is empty. An <code>awaiting</code> ticket&#39;s recorded pane stands as ADR 0033 accepted it, except when the pane now holds a live agent that is not the ticket&#39;s own: there the base key refuses, and the decision&#39;s and the Live view&#39;s Goto rows refuse the focus with their own words, so no Goto in the plane lands on a foreign agent&#39;s pane.</p><p>The considered alternatives:</p><ul><li>Keep trusting the pane id and let the reclaim settle it. Rejected: the reclaim moves a closed cycle&#39;s ticket to <code>running</code>, so a wrong adoption is a state corruption, not a display error. The operator&#39;s report was the display error the wrong adoption leaves behind.</li><li>Match the live agent by its session id, the way a Consultation can. Rejected for the ticket side: the handoff records no session id of its own agent, and the id herdr lists is the agent&#39;s, not a handle the plane owns. The name is a handle the plane chose and herdr honors.</li><li>Treat <code>unverifiable</code> as missing everywhere. Rejected: an older herdr that lists no names would read every live ticket as missing and restart them all. The pane keeps its standing where the identity cannot be read; only the state-mutating reclaim fails safe.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A ticket whose pane herdr reused reads <code>missing</code>: the row wears the failure badge, the Live view opens the missing screen, and the operator restarts the ticket or abandons its work cycle from there. In auto mode the missing path restarts the ticket after the startup grace, the way it restarts a truly gone agent.</li><li>The reclaim never adopts a Consultation&#39;s agent or any other agent&#39;s. A closed cycle&#39;s ticket stays open until its own agent is the one the pane holds.</li><li>Handoffs from before the name column keep the pane&#39;s standing everywhere but the reclaim. Their expected name is the stable name derived from the title, which is the name the handoff asked for first.</li><li>The plane&#39;s Goto can no longer focus a foreign agent&#39;s pane for a ticket, in any mode or surface. The refusal names the fact it read: the pane the handoff recorded is no longer the agent&#39;s pane.</li><li>A Consultation still matches its agent by the recorded pane id, with the stable session id as its fallback (issue #24). The same reuse hazard stands for a Consultation whose pane id herdr handed out again; the ticket side holds this identity because the ticket&#39;s handoff records the name. Closing the Consultation side is open.</li></ul>',15)])])}const g=t(s,[["render",i]]);export{p as __pageData,g as default};
