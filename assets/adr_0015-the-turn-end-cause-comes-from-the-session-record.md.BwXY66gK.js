import{_ as t,o,c as a,a0 as r}from"./chunks/framework.ysVzM4be.js";const p=JSON.parse(`{"title":"ADR 0015: The turn end cause comes from the agent's session record","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0015-the-turn-end-cause-comes-from-the-session-record.md","filePath":"adr/0015-the-turn-end-cause-comes-from-the-session-record.md"}`),s={name:"adr/0015-the-turn-end-cause-comes-from-the-session-record.md"};function d(n,e,c,i,h,l){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-0015-the-turn-end-cause-comes-from-the-agent-s-session-record" tabindex="-1">ADR 0015: The turn end cause comes from the agent&#39;s session record <a class="header-anchor" href="#adr-0015-the-turn-end-cause-comes-from-the-agent-s-session-record" aria-label="Permalink to &quot;ADR 0015: The turn end cause comes from the agent&#39;s session record&quot;">​</a></h1><p>Status: accepted Date: 2026-09-02</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0008 builds the Turn log from the agent&#39;s session record, the versioned file the runtime writes for every turn. The control plane reads that record once at settle and takes the log from it. But the same record already says why the turn ended, and the control plane does not look.</p><p>herdr&#39;s <code>agent list</code> status is the wrong fact to build a cause from. It is the herdr integration&#39;s view of the pane, not the runtime&#39;s own record of the turn. A turn can end on an API error the pane still shows as <code>working</code>, or the pane can report <code>done</code> for a turn the runtime cut short. The cause the operator needs is the runtime&#39;s fact: <code>completed</code>, <code>failed</code>, <code>aborted</code>, <code>truncated</code>, or</p><ul><li>when the record says nothing the reader can read - <code>unknown</code>.</li></ul><p>Consultations settle turns too, and their operators see the same screens. A Consultation turn that settled without an answer (the agent errored or was aborted) is a fact the operator must be able to act on, not a silent <code>awaiting-response</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The Turn end cause comes from the agent&#39;s session record, in the same read that already returns the Turn log.</p><p>The seam widens from &quot;the log&quot; to &quot;the log, the cause, and the cause&#39;s detail&quot;: one read of the record, parsed once, and a cause beside the log. The terminal capture never produces a cause. A turn whose session the control plane cannot read - no session reported, no reader for the kind, or a missing or unreadable record - settles with an <code>unknown</code> cause and its log from the capture, exactly as ADR 0008 degrades the log.</p><p>Each per-agent-type reader maps its record&#39;s own end-of-turn fact to the five causes:</p><ul><li>pi reads the last assistant message&#39;s stop reason: <code>stop</code> is <code>completed</code>, <code>error</code> is <code>failed</code> with the record&#39;s error message as the detail, <code>aborted</code> and a mid-turn <code>toolUse</code> are <code>aborted</code>, and <code>length</code> is <code>truncated</code>.</li><li>codex reads the task event: a clean <code>task_complete</code> is <code>completed</code>, one with an error, and a <code>stream_error</code>, are <code>failed</code>, and <code>turn_aborted</code> is read from its own text (<code>truncated</code> when it names the context, <code>aborted</code> when it names an interrupt or a timeout, <code>failed</code> otherwise), and its reason stays the detail verbatim, so the operator reads codex&#39;s own words.</li><li>claude reads the last assistant message: an API error is <code>failed</code>, a <code>max_tokens</code> stop is <code>truncated</code>, and <code>end_turn</code> or <code>stop_sequence</code> is <code>completed</code>; a <code>tool_use</code> stop is a turn still mid-work, not an end.</li></ul><p>A record the reader cannot interpret settles <code>unknown</code>, never a guessed <code>completed</code>.</p><p><strong>Fail open.</strong> <code>unknown</code> is not a held cause. A turn the control plane could not read, or that ended in a way no reader recognizes, auto-decides exactly as it did before this ADR. The cause never blocks work the operator can see; only a cause the record states - <code>failed</code>, <code>aborted</code>, <code>truncated</code> - may hold a turn.</p><p><strong>The staleness guard.</strong> The cause is read for the turn that just settled. A record whose last turn-end event predates the handoff&#39;s start is not this turn&#39;s end: the reader settles it <code>unknown</code>, never <code>completed</code>. An unparseable timestamp is skipped, so a bad record degrades to <code>unknown</code>, not to a wrong <code>completed</code>.</p><p><strong>The detail is bounded.</strong> The cause&#39;s detail is the agent&#39;s or the provider&#39;s own text. It is capped at 2000 characters so a verbose error cannot push a trace past the state the operator already sees.</p><p>The cause and its detail are stored on the Completion trace and on the Consultation turn. A re-settle of the same still-pending turn refreshes both. A Consultation turn that settled <code>failed</code> or <code>aborted</code> is not an answer: it leaves the Consultation in <code>failed</code>, for recovery. Every other cause - <code>completed</code>, <code>truncated</code>, <code>unknown</code> - leaves it <code>awaiting-response</code>, as a turn that settled with its output does today.</p><p>The considered alternatives:</p><ul><li>Read the cause from herdr&#39;s status. Rejected: it is herdr&#39;s view of the pane, not the runtime&#39;s record of the turn, and it can disagree with the record on exactly the turns - the failed and the truncated ones - the cause exists to name.</li><li>A new herdr command that reports the cause. Rejected: ADR 0006 holds, the poll reads herdr and writes nothing to it, and the cause is read, never provoked. The record already reports it, so the control plane takes it without a new dependency.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>ADR 0008 is extended, not replaced: the record that already gives the Turn log now gives the cause beside it, in the same read, and the fail-open and the staleness guard ride the same degradation path.</li><li>The Completion trace and the Consultation turn each grow a <code>cause</code> and a <code>detail</code> cell. A legacy trace predates the cells and reads <code>unknown</code>, which fails open, so an old record behaves exactly as it did before this ADR.</li><li>A Consultation turn that settled without an answer is visible as <code>failed</code>, so the operator recovers it instead of finding it silently waiting.</li><li>A turn the control plane cannot read never blocks: it settles <code>unknown</code> and auto-decides as it did before, so a runtime that changes its record format breaks the cause, not the flow.</li></ul>',21)])])}const m=t(s,[["render",d]]);export{p as __pageData,m as default};
