import{_ as o,o as t,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const g=JSON.parse(`{"title":"ADR 0039: The modal's body is a pane and its decisions a bounded region","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0039-the-modal-body-is-a-pane-and-its-decisions-a-bounded-region.md","filePath":"adr/0039-the-modal-body-is-a-pane-and-its-decisions-a-bounded-region.md"}`),s={name:"adr/0039-the-modal-body-is-a-pane-and-its-decisions-a-bounded-region.md"};function r(i,e,d,h,l,c){return t(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0039-the-modal-s-body-is-a-pane-and-its-decisions-a-bounded-region" tabindex="-1">ADR 0039: The modal&#39;s body is a pane and its decisions a bounded region <a class="header-anchor" href="#adr-0039-the-modal-s-body-is-a-pane-and-its-decisions-a-bounded-region" aria-label="Permalink to &quot;ADR 0039: The modal&#39;s body is a pane and its decisions a bounded region&quot;">​</a></h1><p>Status: accepted Date: 2026-09-20</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The decision modal and the Live view laid their body and their control rows out as one stack: the context row, the log, the optional held cause row, and the action rows, in that order, at the top of a near-fullscreen box, with whatever blank rows were left falling below.</p><p>The control rows are bounded by config, not by the terminal: <code>decisionFor</code> adds one row per outgoing workflow edge target, so a wide workflow graph offers a dozen. At 100x19 such a ticket left the Turn log one row, and at 100x30 the rows sat flush under the last prose line, in the same columns and with no line between them. The rows the operator confirms were the only way out of the modal, and a control region that grows without bound is a log that shrinks without a floor. The frame also offered no name for the log: a box bordered <code>Decision: &lt;title&gt;</code> held both the text and the choices in one region.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The modal&#39;s box is two regions.</strong> A bordered, titled Body pane holds one body by itself: the Turn log, or the Agent view of a live turn. Its border title names the body that shows. A Decision region holds the rows the operator confirms: the held cause row and the decision rows. The pane&#39;s bottom border is the boundary; the region gets no chrome of its own.</p><p><strong>The regions take their rows in a stated order.</strong> The context row, then the held cause row, then the pane&#39;s chrome, then the Decision region, then the log. The held cause row stands in the region, above the rows it refuses, because it qualifies the decision, not the text. The stale-stream note stays the body&#39;s last line, because it qualifies the lines the operator is reading.</p><p><strong>The Decision region is bounded and scrolls.</strong> It shows as many rows as the box has room for once the log has paid its floor, and it scrolls the rest. The log keeps a floor of three rows, drops it to one after the pane has yielded its chrome, and only then does the surface stand down to the size message. This keeps the documented promise that every workflow edge stays reachable.</p><p><strong>The pane yields its chrome before the log yields rows.</strong> Padding first, border second, on the same column floor the box already uses for its own padding. The pane&#39;s chrome, the region&#39;s cap, and the scrollbar are decided at the final size, so the pop-in draws no border the box does not have yet.</p><p><strong>The pane is opt-in per surface.</strong> A surface with no long body: the Missing modal and the Consultation panels: keeps none, and is not forced to draw an empty one. An empty body states its reason as one row inside the pane, and the pane keeps its chrome.</p><p><strong>The availability of the keys follows the facts.</strong> The body&#39;s scroll is unavailable with a reason when the body already fills the pane, or carries nothing. The region&#39;s selection is refused, with a reason on the Message line, when the region holds one row. The region&#39;s range rides the Action bar behind the selection&#39;s hint, on the range anchor the Key guide and the Message view already use. The body keeps its inline thumb.</p><p><strong>The pane and the box paint one ink.</strong> The control ink&#39;s indicator, the checked essential-indicator pair. No modal states its own border color.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><p><strong>Cap the control region without scrolling.</strong> Rejected: a handoff row the operator cannot select is worse than a long frame, and it breaks the documented promise that every edge stays reachable.</p><p><strong>Group the Handoff rows behind one expandable row.</strong> Deferred as its own decision. It keeps Close and Goto two keystrokes away at any size, but it is a second mechanism the floor has not yet needed.</p><p><strong>A second bordered region for the controls.</strong> Rejected: four rows per modal for a symmetry the pane&#39;s own border already earns, and a second chrome-yield case at the declared minimum.</p><p><strong>A proportional log floor.</strong> Rejected: it makes the region&#39;s visible count change with the terminal&#39;s size, which is harder to test and harder for the operator to learn than one stated floor.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The region&#39;s selection, its wrap, its auto-scroll, its visible window, and its range text are a control behavior, so they live in the shared control library, one module beside the field, the selector row, and the form, and the gallery can show a region state without building a modal around it. <code>scroll-turn-log</code> becomes <code>scroll-body</code>, because the body it scrolls may be the Agent view. The in-box hint rows die: the Action bar is the plane&#39;s only place for keys. ADR 0016&#39;s held-turn rule keeps standing; only its row&#39;s place moves, from above the actions to inside the region that explains.</p>',20)])])}const b=o(s,[["render",r]]);export{g as __pageData,b as default};
