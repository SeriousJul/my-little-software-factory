import{_ as t,o as s,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"Mutation testing","description":"What a mutation campaign measures, how to run one, and the budget a run costs.","frontmatter":{"title":"Mutation testing","description":"What a mutation campaign measures, how to run one, and the budget a run costs."},"headers":[],"relativePath":"development/mutation-testing.md","filePath":"development/mutation-testing.md"}'),o={name:"development/mutation-testing.md"};function i(r,e,h,d,c,l){return s(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="mutation-testing" tabindex="-1">Mutation testing <a class="header-anchor" href="#mutation-testing" aria-label="Permalink to &quot;Mutation testing&quot;">​</a></h1><p>A test suite that passes says the tests agree with the code. It does not say the tests would notice a wrong program. Mutation testing answers that: a runner rewrites one expression at a time in <code>src</code>, changes <code>x &gt; y</code> to <code>x &gt;= y</code>, drops a <code>!</code>, empties a string, returns <code>undefined</code> from an arrow function, and runs the tests against the rewritten copy. A mutant the suite fails is <strong>killed</strong>: some test reads the difference. A mutant that passes every test is a hole: the code carried a behavior no test answers on.</p><p>The plane runs mutation testing with <a href="https://stryker-mutator.io" target="_blank" rel="noreferrer">StrykerJS</a> and its Bun test-runner plugin. See <a href="./../adr/0055-mutation-testing-runs-on-the-bun-test-runner.html">ADR 0055</a> for why this shape and what it costs.</p><h2 id="running-a-campaign" tabindex="-1">Running a campaign <a class="header-anchor" href="#running-a-campaign" aria-label="Permalink to &quot;Running a campaign&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mutate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                    # the whole of src: hours</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mutate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mutate=src/domain/ticket.ts</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # one file</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mutate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;--mutate=src/domain/**&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # one directory: quote the glob</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mutate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dryRunOnly</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # one instrumented full run</span></span></code></pre></div><p>The command copies the tree into a sandbox under <code>.stryker-tmp</code>, rewrites the copy, runs <code>bun test</code> against it, and removes that sandbox afterwards: the working tree never holds a mutant. Each campaign gets a directory of its own there, named for its process, so two campaigns in one checkout do not delete each other&#39;s tree; they still share <code>reports/mutation</code>, where the last run to finish owns the report. The copy step ignores that whole temp tree, so a campaign reads none of what the campaigns before it left in it: Stryker excludes only the one temp directory it is handed, and the per-campaign name sits one level deeper. Reports are not committed - <code>mutation.html</code> to read and <code>mutation.json</code> for a program to diff. The run takes the machine: it spawns one <code>bun test</code> process per worker, each about 250 MB, and <code>concurrency</code> in <code>stryker.config.mjs</code> sets how many run at once - a quarter of the machine&#39;s logical cores there, which is the measured 8 on a 32-core machine and scales down on a smaller one.</p><p>To look inside a campaign&#39;s sandbox after it ends, ask Stryker to keep it and the entry point stands down with it: <code>bun run mutate -- --cleanTempDir false</code> (the <code>--cleanTempDir=false</code> form works the same). Those are the only two values that keep it: <code>false</code> and <code>0</code> are exactly what Stryker&#39;s own option parser reads as &quot;do not delete&quot;, and this entry point stands down for no value core does not, so <code>true</code>, <code>always</code>, and <code>never</code> all leave the removal armed. <code>never</code> is the one spelling whose English and whose parsed reading disagree - Stryker&#39;s parser takes any text but <code>false</code> and <code>0</code> as truthy - so the command says so at its gate instead of promising a sandbox that its own runner then takes down.</p><p>The command refuses to start on a Bun older than <code>1.3.7</code>, the release that added the inspector events the runner reads to tie a mutant to its tests. The control plane itself still runs on the <code>1.3.0</code> floor <a href="./../adr/0035-the-control-plane-runs-on-bun.html">ADR 0035</a> set; the higher floor belongs to the mutation harness, not to the app.</p><p><code>--dryRunOnly</code> is the cheap probe. It instruments every file, runs the suite once against the instrumented copy, and stops. It answers whether the campaign can run at all, how many mutants the scope holds, and what one full test run costs - the floor under the campaign&#39;s slowest mutant runs.</p><p>Read the report two ways. The terminal prints the score table and each surviving mutant with the tests that ran for it. The HTML report (<code>reports/mutation/mutation.html</code>) shows the source with every mutant in place, killed or not, so a survivor can be judged where it stands. The JSON report (<code>reports/mutation/mutation.json</code>) holds the same result for a program to read.</p><h2 id="what-bounds-one-run" tabindex="-1">What bounds one run <a class="header-anchor" href="#what-bounds-one-run" aria-label="Permalink to &quot;What bounds one run&quot;">​</a></h2><p>Two clocks run over the campaign&#39;s initial run - the whole suite, instrumented, in one process - and the tighter one governs. <code>dryRunTimeoutMinutes</code> is core&#39;s: it races the dry run it hands the test runner. <code>bun.timeout</code> is the plugin&#39;s kill bound for one child, plus a fixed 30 seconds it allows itself for the inspector drain on that first run. The config sets 10 minutes and 300 seconds against a run that measures 3 minutes 24 seconds to 3 minutes 31 seconds here, so the plugin&#39;s 330 seconds is what stands over it with room. A mutant run is bounded the same two ways: core gives it <code>timeoutFactor * netTime + timeoutMS + overhead</code>, where <code>netTime</code> is what the tests covering that one mutant took in the initial run, and <code>timeoutMS</code> is the flat slack in that sum.</p><h2 id="what-a-campaign-costs" tabindex="-1">What a campaign costs <a class="header-anchor" href="#what-a-campaign-costs" aria-label="Permalink to &quot;What a campaign costs&quot;">​</a></h2><p>Measured on a 32-core machine, Bun 1.4.2, the suite of this repository:</p><table tabindex="0"><thead><tr><th>Thing</th><th>Cost</th></tr></thead><tbody><tr><td><code>bun run test</code></td><td>38 s</td></tr><tr><td>The campaign&#39;s initial run: the whole suite, instrumented, serial</td><td>3 min 24 s to 3 min 31 s</td></tr><tr><td>Mutants in <code>src</code></td><td>27,293 over 75 files</td></tr><tr><td>Mutants in the campaign&#39;s scope (<code>src</code> without the gallery)</td><td>26,177 over 74 files</td></tr><tr><td>814 mutants of session-record and domain logic, 8 workers</td><td>11 min, about 4,400 per hour</td></tr><tr><td>913 mutants of the shared control library, 16 workers</td><td>22 min, about 2,500 per hour</td></tr><tr><td>One campaign over the whole scope</td><td>6 to 11 hours, extrapolated</td></tr></tbody></table><p>Two numbers explain the rest. A mutant run costs the time of the tests that covered it - 10 tests on average for the logic slice, 47 for the library slice - so a module the frame tests all import is expensive per mutant no matter how few mutants it holds. And the campaign is CPU-bound before it is memory-bound: doubling the workers did not double the rate.</p><p>The single most expensive mutants are the static ones: an expression that runs when a module loads is covered by every test that imports it, so its run is practically the whole suite. Stryker reports how many it found and what share of the time they take: 4 % of one measured slice, estimated at 61 % of its running time. <code>--ignoreStatic</code> drops them and takes that saving, and it is a real loss: module-level constants are behavior the frames read. <code>bun run mutate</code> keeps them.</p><h2 id="the-score-and-what-is-not-in-it" tabindex="-1">The score, and what is not in it <a class="header-anchor" href="#the-score-and-what-is-not-in-it" aria-label="Permalink to &quot;The score, and what is not in it&quot;">​</a></h2><p>Two files stay out of the campaign on purpose:</p><ul><li><code>src/components/shared/gallery.ts</code> is the shared control gallery&#39;s own example list. <code>bun run gallery</code> is its only caller, no shipped surface reads it, and it holds 1,116 of the 27,293 mutants in <code>src</code>: 4 percent of the campaign&#39;s work from one module that imports the whole control library.</li><li><code>test/shared-control-architecture.test.ts</code> reads production source as text and counts the shapes it finds. The runner rewrites exactly those shapes, so under mutation the file fails on a text change rather than on a behavior change. It would kill mutants for the wrong reason and inflate the score. It stays in <code>bun run test</code>, which reads the uninstrumented tree.</li></ul><p>The score is a coverage measure of the tests, not a grade of the code. A survivor is only a problem when the mutation it stands on is a behavior the plane should answer on: <code>i -= 1</code> to <code>i += 1</code> in a loop that never runs more than once is a survivor worth leaving. A survivor on a state transition, a settings resolution, or a frame&#39;s cell is a missing test.</p><p>The two measured slices scored <strong>79.48 %</strong> (session-record parsing and the domain modules: of 814 mutants, 645 killed and 2 timed out, 144 survived, and 23 with no coverage) and <strong>63.20 %</strong> (the shared control library: of 913 mutants, 573 killed and 4 timed out, 287 survived, and 49 with no coverage). A timeout counts as a kill, so the scores read as 647 of 814 and 577 of 913. Those are the first places to look; neither is the plane&#39;s score, and no whole-<code>src</code> baseline has been measured yet.</p>`,22)])])}const m=t(o,[["render",i]]);export{p as __pageData,m as default};
