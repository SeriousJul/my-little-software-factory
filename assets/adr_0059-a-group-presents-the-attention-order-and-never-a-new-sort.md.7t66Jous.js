import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0059: A Group presents the attention order and never a new sort","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0059-a-group-presents-the-attention-order-and-never-a-new-sort.md","filePath":"adr/0059-a-group-presents-the-attention-order-and-never-a-new-sort.md"}'),r={name:"adr/0059-a-group-presents-the-attention-order-and-never-a-new-sort.md"};function s(i,e,d,h,l,u){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0059-a-group-presents-the-attention-order-and-never-a-new-sort" tabindex="-1">ADR 0059: A Group presents the attention order and never a new sort <a class="header-anchor" href="#adr-0059-a-group-presents-the-attention-order-and-never-a-new-sort" aria-label="Permalink to &quot;ADR 0059: A Group presents the attention order and never a new sort&quot;">​</a></h1><p>Status: accepted Date: 2026-09-24</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The ticket list already has one order: the attention bands, then newest external update, then ticket identity (ADR 0050). That order is the plane&#39;s answer to &quot;what needs me now&quot;, and the automatic top-up acts on it too (ADR 0051).</p><p>Grouping the list by Repository, Ticket source, Task type, Ticket state, or Workflow position puts the same rows into runs behind headers. Three orders compete for those headers: the attention order the list holds, a name order that reads tidy, and a group-internal order that could sort each run on its own. And a fold, the point of a Group, can hide work: a Group of open tickets folded shut can hide the one ticket whose turn settled and waits for a decision.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>A Group is a presentation of the list order, never a new sort.</strong> The Groups stand by the best attention band among the tickets they hold, then by the newest external update in the Group, then by the group value. Inside a Group the order is exactly the order the flat list holds, band rules and all. The axis at <code>none</code> is the list as it stands today, row for row.</p><p><strong>A fold hides rows, never facts.</strong> The Section header&#39;s counts, the held count, the Parallel limit, the Pickup, the Top-up, the handoff gates, and every Decision route read the same facts with a Group open or shut. A collapsed Group&#39;s own header carries its count and its held count, so the hidden obligation stays named where the fold was made.</p><p><strong>The plane never opens a Group by itself.</strong> A ticket that moves into a folded Group, or a held turn behind a fold, changes no fold. The operator folds and unfolds. This is what makes the fold safe to keep at all: it cannot move under them, and it cannot come back from a restart (ADR 0058).</p><p><strong>A group key is a fact of the ticket, never a face a row wears.</strong> A Queue wait&#39;s <code>queued</code> badge groups under <code>open</code>, a Starting window&#39;s spinner groups under <code>handed-off</code>, and a held turn groups under <code>awaiting</code>, because the badge is a presentation of a state and a poll-time marker, not a Ticket state. The Task type axis reads the row&#39;s own rule for the same reason: a ticket&#39;s task is the recorded Handoff&#39;s task while it is in flight and the Suggested task type while it is <code>open</code>, and each of those is a fact at that position of the ticket. The rule holds everywhere, so the Group a row sits in is always explainable from the row itself.</p><h2 id="considered-options" tabindex="-1">Considered options <a class="header-anchor" href="#considered-options" aria-label="Permalink to &quot;Considered options&quot;">​</a></h2><ul><li><strong>Groups alphabetical by value.</strong> It reads tidier and it costs the ordering its meaning: an <code>awaiting</code> ticket&#39;s Group could sit below a quiet Repository&#39;s, and the operator would have to unfold to find their own decisions.</li><li><strong>A group-internal sort that overrides attention.</strong> It buries decisions inside a Group, which is the one failure this plane should not build.</li><li><strong>Automatic unfold on new attention.</strong> It makes a fold unpredictable and steals rows the operator is looking at, and a fold the plane can lift is not a control the operator can rely on.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Grouping by Ticket state draws four contiguous runs the list already holds, so that axis makes the attention bands visible rather than reordering anything. Grouping by Repository, source, task, or position pulls rows together from across bands, which the header order keeps honest.</li><li>A short terminal can show nothing but Group headers, because each header costs a window row. That is accepted: the headers carry the counts, and one key press returns the flat list.</li><li>The word &quot;group&quot; now names the visible run, so the invisible rank in the list order is the <strong>attention band</strong> in the glossary. ADR 0050 and ADR 0022 keep the wording they were written with.</li><li>The Work queue&#39;s order and the queue&#39;s own keys are untouched: the queue is the order of work, and a Group is the order of attention (ADR 0049, ADR 0050).</li></ul>',14)])])}const f=t(r,[["render",s]]);export{p as __pageData,f as default};
