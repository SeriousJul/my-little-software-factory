import{_ as t,o,c as r,a0 as n}from"./chunks/framework.ysVzM4be.js";const p=JSON.parse(`{"title":"ADR 0020: The control plane resolves the herdr theme from herdr's config file","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0020-the-control-plane-resolves-the-herdr-theme-from-herdrs-config-file.md","filePath":"adr/0020-the-control-plane-resolves-the-herdr-theme-from-herdrs-config-file.md"}`),a={name:"adr/0020-the-control-plane-resolves-the-herdr-theme-from-herdrs-config-file.md"};function s(i,e,h,d,l,c){return o(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0020-the-control-plane-resolves-the-herdr-theme-from-herdr-s-config-file" tabindex="-1">ADR 0020: The control plane resolves the herdr theme from herdr&#39;s config file <a class="header-anchor" href="#adr-0020-the-control-plane-resolves-the-herdr-theme-from-herdr-s-config-file" aria-label="Permalink to &quot;ADR 0020: The control plane resolves the herdr theme from herdr&#39;s config file&quot;">​</a></h1><p>Status: accepted Date: 2026-09-14</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The control plane runs inside a herdr pane, surrounded by herdr&#39;s own chrome. herdr themes that chrome from the <code>[theme]</code> section of its <code>config.toml</code>: a built-in theme name (18 names, including light variants), an <code>auto_switch</code> that follows the host terminal appearance, and per-token <code>theme.custom</code> overrides. The control plane painted a fixed dark palette of its own, so the pane it draws never matched the chrome around it, and its light presentation was an explicit pin (<code>FACTORY_PRESENTATION</code>), not something an operator could set.</p><p>herdr gives a child process no channel to learn the active theme: no environment variable carries it, and the socket API snapshot exposes no theme field. The config file is the only record of the choice. The built-in theme definitions live inside the herdr binary, and herdr has no command that prints them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The control plane resolves the Theme itself, at startup, from herdr&#39;s config file.</strong> It reads the file herdr reads (<code>$XDG_CONFIG_HOME/herdr/config.toml</code>, falling back to <code>~/.config/herdr/config.toml</code>) when it runs inside herdr (<code>HERDR_ENV</code> set). It carries its own copy of the 18 built-in theme definitions and herdr&#39;s name and alias rules, and it records the herdr version the copy was taken from.</p><p><strong>Resolution mirrors herdr.</strong> <code>auto_switch</code> selects the <code>dark_name</code> theme: the control plane does not follow the host light appearance. An unknown theme name, a missing section, or an unparseable config falls back to <code>catppuccin</code>, the same result herdr shows, with one warning on the Message line. A missing config inside herdr also gives <code>catppuccin</code>, herdr&#39;s built-in default. Outside herdr, a missing config gives the standalone fixed dark palette, the colors the control plane has always used. <code>theme.custom</code> overrides apply per token on top of the base theme; a bad or unknown override value drops only that token.</p><p><strong>The theme owns the colors; the no-color presentation stays.</strong> The shared presentation module&#39;s role set becomes the theme&#39;s color roles, named after herdr&#39;s tokens. The roles a value can hold are hex, <code>rgb(r,g,b)</code>, the 16 named ANSI colors, and <code>reset</code>, which means the control plane emits no color code so the terminal default shows through. The no-color presentation remains a separate axis on top of any theme, and written prefixes and markers keep meaning off color. Inherited color pairs are not contrast-tested; the verification record says so.</p><p><strong>The whole plane follows the theme.</strong> The base panes move from their own fixed palette to the shared module, and the light/dark presentation pin is removed: a light appearance is a light theme name, and there is one source of brightness.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><ul><li>Ask upstream herdr to expose the resolved theme (an environment variable or a socket API field) and read only that. Cleanest channel, but it depends on a change the control plane does not control, and herdr 0.9.0 has none. Revisit if herdr adds one: the resolver keeps its shape, and the config read is replaced by the new channel.</li><li>Honor only the <code>theme.custom</code> overrides and ignore built-in names. Rejected: it does nothing for the common case, an operator who set only <code>name</code>.</li><li>Re-read the config at runtime, or watch it for changes. Rejected for the first version: the control plane restarts often in this workflow, and watching a file the control plane does not own adds a failure path for a rare benefit.</li><li>Follow the host light appearance when <code>auto_switch</code> is on (a terminal background query). Rejected: it needs raw terminal I/O through the UI kit for a case that almost never fires; the documented limit is to take the <code>dark_name</code> theme.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The vendored theme definitions can drift when herdr changes a theme. The module records the herdr version they were copied from, so the drift is detectable and the fix is a data update, not a design change.</li><li>A theme change in herdr&#39;s config takes effect at the next control plane startup, not live.</li><li>The light/dark presentation concept is superseded by the Theme; the no-color presentation survives as the only remaining axis, and <code>FACTORY_PRESENTATION</code> is removed.</li><li>The open item in the verification record that the base panes paint the fixed dark color system is closed by this decision&#39;s implementation.</li></ul>',14)])])}const f=t(a,[["render",s]]);export{p as __pageData,f as default};
