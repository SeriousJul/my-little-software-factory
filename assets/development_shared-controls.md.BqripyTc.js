import{_ as t,o as a,c as r,a0 as o}from"./chunks/framework.ysVzM4be.js";const u=JSON.parse('{"title":"Shared control standard","description":"","frontmatter":{},"headers":[],"relativePath":"development/shared-controls.md","filePath":"development/shared-controls.md"}'),n={name:"development/shared-controls.md"};function i(s,e,d,l,c,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="shared-control-standard" tabindex="-1">Shared control standard <a class="header-anchor" href="#shared-control-standard" aria-label="Permalink to &quot;Shared control standard&quot;">​</a></h1><p>Status: accepted, and the baseline is implemented for every editable field, selector, search, form action, and form focus route the control plane owns. Confirmed on 2026-09-09; implemented in the shared control library under <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/src/components/shared" target="_blank" rel="noreferrer">src/components/shared</a>.</p><p>The current results for every acceptance target, with their versions, are in <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">the verification record</a>: the keyboard and visual targets ran in Ghostty and foot, and a tmux path runs in the suite; a screen reader has never read this application, and no claim of screen-reader support is made anywhere in this repository.</p><p>This is the required baseline for human and agent contributors.</p><p>See <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/CONTEXT.md" target="_blank" rel="noreferrer">the glossary</a> for domain terms, <a href="./../adr/0014-shared-modules-own-control-behavior.html">ADR 0014</a> for ownership, and <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/research/terminal-accessibility.md" target="_blank" rel="noreferrer">the accessibility research</a> for evidence.</p><h2 id="scope-and-ownership" tabindex="-1">Scope and ownership <a class="header-anchor" href="#scope-and-ownership" aria-label="Permalink to &quot;Scope and ownership&quot;">​</a></h2><p>Every control owned by the control plane must use shared modules: Text fields, Draft fields, selectors, actions, list navigation, modal focus, and help and message controls. The library stays inside this repository. It is not a separate published package or a library for other projects.</p><p>Controls inside an external Agent&#39;s own application are excluded. The control plane still owns its way into and out of the Agent terminal. Shared editing bindings must not replace the keys that this mode forwards to the Agent.</p><p>The shared modules own:</p><ul><li>Editing, caret movement, selection, undo and redo, and safe paste.</li><li>Focus movement, modal focus containment, and focus restoration.</li><li>Labels, hints, error presentation, state indicators, and sizing.</li><li>Integration with the Control catalogue, Action bar, and Key guide.</li></ul><p>Screens supply values, validation rules, availability facts, and domain actions. Draft storage and Agent operations stay outside the shared modules. Callers must not need to manage renderer buffers, keyboard subscriptions, or caret repair to use a field correctly.</p><p>Reuse the existing shared control and modal code where it fits this standard. OpenTUI&#39;s field primitives are candidates for the shared implementation, not public escape routes for separate screen-specific editors. Do not create a second key definition system that can disagree with dispatch or help.</p><h2 id="editing-and-keyboard-ownership" tabindex="-1">Editing and keyboard ownership <a class="header-anchor" href="#editing-and-keyboard-ownership" aria-label="Permalink to &quot;Editing and keyboard ownership&quot;">​</a></h2><p>Both field kinds must support normal caret and word movement, Home and End, Backspace and Delete, selection, undo and redo, and bracketed paste. Unicode editing must not corrupt characters or place the visible caret at a different location from the next edit. The focused field must remain visible as its text scrolls and the terminal resizes.</p><table tabindex="0"><thead><tr><th>Control</th><th>Required behavior</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Move to the next or previous field or action.</td></tr><tr><td>Arrows in a Draft field</td><td>Move the caret, not the form&#39;s focus.</td></tr><tr><td>Enter in a Draft field</td><td>Insert a newline.</td></tr><tr><td>Send / Launch</td><td>Visible, keyboard-reachable actions that submit the draft.</td></tr><tr><td>Modified Enter</td><td>May supplement submission; never its only route.</td></tr><tr><td>F1</td><td>Open the Key guide without modifying the field.</td></tr><tr><td>F2</td><td>Open the Message view when available.</td></tr><tr><td>Ctrl+C</td><td>Emergency exit for control-plane input; selection does not change it into Copy.</td></tr><tr><td>Copy selection</td><td>A keyboard-reachable action, separate from emergency exit.</td></tr></tbody></table><p>Ordinary printable characters belong to a field that accepts typing, including letters that act as shortcuts outside that field. Support terminal paste. Essential actions must remain available with ordinary terminal key sequences, without an enhanced keyboard protocol.</p><p>A modal keeps focus inside itself and restores the previous focus when closed. Opening and closing Help must preserve the draft, caret, selection, and undo history. Input must not reach a field or screen behind an active modal.</p><h2 id="auto-copy" tabindex="-1">Auto copy <a class="header-anchor" href="#auto-copy" aria-label="Permalink to &quot;Auto copy&quot;">​</a></h2><p>The control plane turns on terminal mouse reporting for its click, wheel, and scrollbar controls, which takes the host terminal&#39;s native text selection away from the operator. Auto copy is the replacement for what that gives up, and it is a global mouse control: it runs over every surface the control plane paints, with no per-surface exception and no setting to turn it on or off. It is owned by the shell as one subscription to the renderer&#39;s ended-selection event, not by any surface. See <a href="https://github.com/SeriousJul/my-little-software-factory/issues/10" target="_blank" rel="noreferrer">the host-selection issue it resolves</a>.</p><p>Required behavior:</p><ul><li>The operator drags with the mouse over any surface and the selection highlights while the drag runs; releasing the drag copies the selected text to the system clipboard through the renderer&#39;s OSC 52 write, the same write the field&#39;s keyboard Copy control uses.</li><li>A click that does not drag ends an empty selection and copies nothing, so selecting a row never clobbers the clipboard.</li><li>A copy the terminal refused warns on the Message line with the shared refusal wording; a copy that takes is silent and leaves the Message line alone.</li><li>A drag release over a list row fires no row action beyond the press that started the drag, so copying text does not move the row selection.</li><li>The ended selection clears its own highlight where the copy is made, so the release never leaves the drag&#39;s highlight painted until the next click.</li><li>Text fields and Draft fields keep their own in-field selection and their keyboard Copy control unchanged; the shared form route is untouched.</li></ul><p>The field&#39;s keyboard Copy control keeps working exactly as it does, so field editing keeps its own copy path beside Auto copy.</p><h2 id="values-validation-and-paste" tabindex="-1">Values, validation, and paste <a class="header-anchor" href="#values-validation-and-paste" aria-label="Permalink to &quot;Values, validation, and paste&quot;">​</a></h2><p>Every field has a visible label. Errors identify the affected field and explain why the value cannot be used. State and errors must not depend on color alone. Validation rules belong to the caller; their presentation and editing behavior belong to the shared module.</p><p>A Context Text field still takes digits only. Reject a paste containing any non-digit as a whole, preserve the previous value and selection, and explain the rejection. Never turn <code>1e3</code> into <code>13</code> by removing the letter. Keep the existing count validation and leading-zero normalization: <code>007</code> and <code>7</code> represent the same count, while <code>0</code> is not a valid count.</p><p>This refines the entry behavior in <a href="./../adr/0009-handoff-setting-resolution.html">ADR 0009</a>. It does not change setting resolution, the meaning of a count, or the rule that an invalid setting cannot start Agent work.</p><p>Keep an oversized Draft field editable and state its size and limit. Do not silently truncate it to make submission succeed. Paste is text entry, not an instruction to submit or invoke application shortcuts.</p><h2 id="type-ahead" tabindex="-1">Type-ahead <a class="header-anchor" href="#type-ahead" aria-label="Permalink to &quot;Type-ahead&quot;">​</a></h2><p>Keep substring matching against the Model list. Show the editable search text. When no Model matches, retain that text and show <code>no match</code>; do not silently restart the search from the last character. Backspace edits the search. Provide an explicit clear action.</p><p>The search is distinct from the selected Model value. This changes the old invisible-search definition; it does not change how the Agent supplies its Model list or which Models are valid.</p><h2 id="draft-retention" tabindex="-1">Draft retention <a class="header-anchor" href="#draft-retention" aria-label="Permalink to &quot;Draft retention&quot;">​</a></h2><p>Closing an editor and discarding its content are different actions.</p><ul><li>Escape leaves a Response draft saved as it is today.</li><li>Closing and reopening the Consultation launcher during one application run restores the complete unfinished form: text, Repository, and Consultation type.</li><li>The launcher states that this form is not saved across application restarts. Durable launcher drafts are outside this baseline.</li><li>Provide an explicit Discard action. Closing the launcher must not silently discard its text or restore it with a different Repository or type.</li></ul><p>Draft persistence belongs to the screen&#39;s domain state, not to the field module.</p><h2 id="presentation" tabindex="-1">Presentation <a class="header-anchor" href="#presentation" aria-label="Permalink to &quot;Presentation&quot;">​</a></h2><ul><li>The plane inherits its Theme from herdr: the shared Theme module reads herdr&#39;s config at startup, resolves the active theme (a light or dark appearance), and every surface paints from that theme&#39;s roles. Outside herdr the plane keeps its own fixed dark theme. A no-color presentation, selected by the <code>NO_COLOR</code> environment variable, works on top of any theme and is the only presentation axis that is not a theme. See <a href="./../adr/0024-the-control-plane-inherits-the-theme-from-herdr.html">ADR 0024</a>.</li><li>Use tested foreground/background pairs with at least 4.5:1 text contrast and 3:1 contrast for essential control indicators. The inheritance does not contrast-check a herdr theme: the operator picks it for the terminal they run in, and the plane&#39;s own themes keep the tested pairs.</li><li>Light and dark are the theme&#39;s appearance, inherited from herdr&#39;s theme; the plane does not pick between them. No-color presentation is the plane&#39;s one remaining presentation axis, and it works on top of any theme. Preserve labels, focus markers, and written state and error indicators when color is absent.</li><li>Keep the focused control and the way out visible at supported small sizes. Below a usable size, preserve editing state and provide a readable size message and a way out rather than allowing overlapping or hidden controls.</li><li>Decorative animation and caret blinking are optional and off by default.</li><li>Progress remains understandable through text and static indicators.</li></ul><p>These are testable application requirements, not a claim of WCAG conformance or of compatibility with every terminal color override.</p><h2 id="contributor-examples-and-enforcement" tabindex="-1">Contributor examples and enforcement <a class="header-anchor" href="#contributor-examples-and-enforcement" aria-label="Permalink to &quot;Contributor examples and enforcement&quot;">​</a></h2><p>Provide one runnable terminal gallery that uses the real shared modules. It must show normal, focused, invalid, unavailable, loading, and narrow-size states where applicable. Use those examples in automated tests so examples cannot become a separate imitation of the production controls.</p><p>Human and agent contributor instructions link to this standard and to the gallery. The gallery command is <code>npm run gallery</code>, or <code>node bin/factory-gallery.mjs [example]</code>, and it draws the production modules. The same examples are driven by <code>test/shared-gallery.test.ts</code>, so an example cannot become an imitation of a control.</p><p>Automated checks must reject new separate field implementations and bypasses of the shared control modules. Add behavior tests through the modules&#39; public interfaces and end-to-end tests through real application flows. A screenshot alone does not prove keyboard ownership, editing, or screen-reader access.</p><h2 id="required-verification" tabindex="-1">Required verification <a class="header-anchor" href="#required-verification" aria-label="Permalink to &quot;Required verification&quot;">​</a></h2><p>The initial acceptance targets are:</p><table tabindex="0"><thead><tr><th>Environment</th><th>Required checks</th><th>Current result</th></tr></thead><tbody><tr><td>Linux with Ghostty</td><td>Keyboard and visual checks</td><td><a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">The verification record</a></td></tr><tr><td>Linux with foot</td><td>Keyboard and visual checks</td><td><a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">The verification record</a></td></tr><tr><td>A tmux path on Linux</td><td>Keyboard, paste, focus, and rendering checks</td><td><a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">The verification record</a></td></tr><tr><td>Separate GNOME Terminal and Orca environment</td><td>Screen-reader operation</td><td>Not verified</td></tr></tbody></table><p>The results live in <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">the verification record</a>, which states what was measured, on what, and what was not measured: the screen-reader path is the target that has never run. Record the exact OS, terminal, multiplexer, renderer, and screen-reader versions used, as applicable. Other platforms remain unverified, not implicitly supported.</p><p>Recorded on 2026-07-11 on Arch Linux, node v26.8.1, tmux 3.7c, Hyprland 0.56.2 (Wayland), Ghostty 1.3.1-arch2, foot 1.28.0, grim 1.5.0. The tmux-path checks are repeatable with <code>test/acceptance/main-view-acceptance.sh</code>, which needs a valid config file with a live source and prints one PASS or FAIL line per check. The real-terminal checks ran the production executable (<code>node src/factory.ts --config</code>) with a real state database, a shadowed <code>herdr</code> so the observation loop holds, and the live github-issues source. Inside Ghostty and foot the app ran in a tmux session attached to the emulator&#39;s own pty, so keystrokes and bracketed paste traveled through the real emulator. The tmux-path checks covered both sections and the action bar, section, list, and detail movement, the F1 key guide and Escape, launcher opening, tab order, the ticket Override modal and its Escape restoration, bracketed paste into the draft field with Unicode and a correct byte count, multi-line paste, ignored paste on a selector field, an oversized draft, narrow single-pane rendering, and clean Ctrl+C exit. In Ghostty and foot the same rendering, keyboard, paste, and exit checks passed. The pixel capture of the emulator window could not be reviewed in that session: the compositor displayed a standby surface (the captured region showed the desktop wallpaper, and workspace focus did not make the window visible), so the visual review is recorded as blocked, not passed. The no-color check ran only through tmux, which passed 24 SGR sequences through with <code>TERM=vt100</code>; a no-color verdict needs a raw pty without a multiplexer. Orca was not installed, so screen-reader operation remains unverified.</p><p>The checks must cover:</p><ul><li>Caret and word movement, selection, deletion, undo and redo, and Unicode text.</li><li>Normal and enhanced key sequences, bracketed paste, numeric paste rejection, oversized drafts, and prevention of accidental submission.</li><li>Tab order, modal containment and restoration, and Help without loss of editing state.</li><li>Draft retention, explicit discard, and Repository/type identity on reopening.</li><li>Type-ahead feedback and correction, including no-match searches.</li><li>Light, dark, and no-color output, contrast, resize, and narrow/short terminals.</li><li>Auto copy: a drag release over a surface ends with the selected text on the clipboard, a click that did not drag copies nothing, a refused write warns on the Message line while a copy that takes is silent, a drag release over a list row leaves the row selection as the press set it, and the ended selection clears its own highlight.</li><li>A real screen reader&#39;s access to labels, values, focus and caret location, selection, errors, modal changes, and progress feedback.</li><li>Agreement between available actions, dispatched keys, the Action bar, and the Key guide.</li></ul><p>Use rendered-frame tests, the production executable&#39;s PTY tests, visual review, and a real screen-reader test. These checks complement each other. PTY bytes and frame snapshots cannot establish what Orca receives. A required test that is skipped or cannot run is not a pass.</p><h2 id="migration-and-completion" tabindex="-1">Migration and completion <a class="header-anchor" href="#migration-and-completion" aria-label="Permalink to &quot;Migration and completion&quot;">​</a></h2><ol><li>Reproduce the reported field failures through real application flows with isolated state and fake external operations. Record failing regression tests. Done: <code>test/shared-field-editing.test.ts</code>, <code>test/consultation-launcher-editing.test.ts</code>, and <code>test/executable-fields.test.ts</code>.</li><li>Test screen-reader feasibility early, before broad migration. Upstream still lists screen-reader support as future work. If the current renderer prevents the agreed access, return for agreement on a renderer change or an equivalent accessible interaction mode. Do not silently remove the requirement.</li><li>Build the shared field behavior and real examples. Migrate Text fields and Draft fields first, including the override panel, Consultation launcher, and response editor. Done: the library, the gallery, and all three surfaces, with the replaced local implementations removed.</li><li>Migrate selectors and the remaining owned controls in small changes. Remove replaced implementations rather than retaining permanent alternatives.</li><li>Add and enforce the architecture checks. Update current-behavior documentation and contributor instructions as each migration lands. Done: the architecture test, this standard, <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/README.md" target="_blank" rel="noreferrer">the README</a>, and <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/AGENTS.md" target="_blank" rel="noreferrer">the contributor instructions</a>.</li><li>Complete all acceptance checks and record their results.</li></ol><p>The migration is complete only when every owned control follows this standard and lint, type checks, behavior tests, terminal tests, visual review, and screen-reader checks pass. Documentation and a new component directory alone do not meet this condition.</p><p>The verification commands are <code>npm run lint</code>, <code>npm run typecheck</code>, <code>npm test</code>, and <code>npm run gallery</code>. The architecture rule is checked by <code>test/shared-control-architecture.test.ts</code>, which rejects a separate field implementation, a hand-edited draft string, and a screen that names a renderer field instead of the library. The screen-reader procedure is written down in <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/shared-controls.md" target="_blank" rel="noreferrer">the verification record</a>; it has not been run, and no result is claimed for it.</p>',53)])])}const m=t(n,[["render",i]]);export{u as __pageData,m as default};
