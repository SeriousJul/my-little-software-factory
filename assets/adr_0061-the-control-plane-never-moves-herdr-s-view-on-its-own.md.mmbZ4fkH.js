import{_ as o,o as t,c as a,a0 as s}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse(`{"title":"ADR 0061: The control plane never moves herdr's view on its own","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0061-the-control-plane-never-moves-herdr-s-view-on-its-own.md","filePath":"adr/0061-the-control-plane-never-moves-herdr-s-view-on-its-own.md"}`),n={name:"adr/0061-the-control-plane-never-moves-herdr-s-view-on-its-own.md"};function r(i,e,h,c,l,d){return t(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0061-the-control-plane-never-moves-herdr-s-view-on-its-own" tabindex="-1">ADR 0061: The control plane never moves herdr&#39;s view on its own <a class="header-anchor" href="#adr-0061-the-control-plane-never-moves-herdr-s-view-on-its-own" aria-label="Permalink to &quot;ADR 0061: The control plane never moves herdr&#39;s view on its own&quot;">​</a></h1><p>Status: accepted Date: 2026-09-25</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The operator works in one herdr window, watching an agent&#39;s pane. When the control plane ends an environment, herdr&#39;s view jumped to the plane&#39;s own workspace. The jump was a two-hop: the view landed on a neighbour workspace, because herdr moves its focus when a workspace disappears, and was then dragged back to the plane by the <code>workspace focus</code> command the plane sent after every successful close. The operator lost their place twice for a bookkeeping act they could not see.</p><p>The worst case has no keypress beside it. A route whose start waited in the Work queue closes the previous handoff&#39;s environment when a seat frees, so the focus command ran at a moment the operator had asked for nothing (ADR 0046). The Consultation close did the same whenever its plan command named a workspace, and the Close cleanup did it on all three of its workspace branches.</p><p>The compensating call was written against a herdr that had one focus for the whole server. That is no longer the model, and the facts are worth recording because the fix looks wrong from the plane&#39;s side: removing a focus command after a close that moved one feels like the bug, not the cure.</p><p>The behavior was read from herdr 0.9.1 source, at two places:</p><ul><li><code>src/server/headless/client_views.rs</code>, in <code>handle_api_request_with_shutdown_check</code>: a request on the public socket moves every attached shell client only for <code>workspace.focus</code>, <code>tab.focus</code>, <code>pane.focus</code>, <code>agent.focus</code>, and a create that asks for focus. <code>workspace.close</code>, <code>tab.close</code>, and <code>worktree.remove</code> are not in that set; they only reconcile client locations.</li><li><code>src/server/clients.rs</code>, in <code>ClientView::reconcile</code>: a client keeps its viewed workspace while that workspace exists, and falls back to the session focus only when it no longer does. This is the one surviving jump, and it is herdr&#39;s own: no client is ever shown a destroyed workspace.</li></ul><p>Two more herdr facts shape the decision:</p><ul><li>Each herdr client keeps its own view, and a focus request from outside a client moves every client&#39;s view. Per-client views are herdr&#39;s shipped model, delivered in herdr 0.9.0 (#3526), and the request to target one client from a command was closed as not planned (#4014): a custom command uses the public socket, and the socket moves every client. There is no command that can aim only the operator&#39;s own window.</li><li>herdr&#39;s own session focus after a worktree close lands on a neighbour, and that is an open upstream issue (#1303). Nothing in the plane reads the session focus, so it stays cosmetic here.</li></ul><p>The plane already asked for no focus when it built an environment: every workspace create, worktree create, worktree open, and tab create carries an explicit <code>--no-focus</code>. Keeping that flag is a contract, not noise: herdr&#39;s changelog records that the create-focus default regressed once and came back (#3766, v0.9.1).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The control plane never moves herdr&#39;s view on its own.</strong> It asks for no focus when it builds an environment, and it asks for none when it ends one.</p><p>The one focus move the plane makes is <strong>Goto</strong>, key <code>g</code>, which the operator presses and which asks herdr to take the view to the agent&#39;s pane. It is navigation the operator chose (ADR 0033), and it stays exactly as it is, including the confirmation that names the workspace it moved to.</p><p>Three consequences follow for the code:</p><ul><li>The handoff module&#39;s shared &quot;return herdr&#39;s focus to the plane&#39;s workspace&quot; helper is deleted, not silenced, with all five of its call sites: the three branches of the Close cleanup that follow a worktree removal or a workspace close, the route close at the ask, and the Consultation operations&#39; close. Nothing replaces them.</li><li>The plane holds no id for the workspace it runs in. The close-cleanup options, the dispatch module&#39;s options, and the Consultation operations&#39; options each carried a control-plane workspace id, and the app read it once from <code>HERDR_WORKSPACE_ID</code> at start-up. Every one of those is gone, so no code path can aim herdr&#39;s focus at it again. The herdr environment mark stays: the plane still detects that it runs inside herdr, and still reads herdr&#39;s config for the Theme (ADR 0024).</li><li>A static check over the plane&#39;s own sources refuses a workspace focus command anywhere in it, allows an agent focus command only at the Goto seam, refuses a herdr create that does not state its no-focus default, and refuses herdr&#39;s <code>--focus</code> flag wherever a source carries it. The last two are one rule read from both sides: herdr takes the last focus flag it reads, so a create that states <code>--no-focus</code> and then asks for focus still moves every attached client.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The view never moves on a close. The two-hop is gone because the plane sends no second hop, and the first hop does not happen: herdr 0.9.1 leaves a client&#39;s view alone for a close of a workspace the client is not viewing.</li><li>The one surviving jump is hers, not the plane&#39;s. When the plane closes the one workspace the operator is viewing, herdr moves that client to its session focus, because the workspace the client was looking at no longer exists. The operator is never shown a destroyed workspace, and no plane code can prevent the move.</li><li>Removing the compensating focus call costs no bookkeeping. A herdr refusal of a close still comes back as one readable reason, still stands on the Message line, and still records the surviving environment as the ticket&#39;s leftover fact (ADR 0012).</li><li>The tab and pane close paths keep their shape. They never sent a focus command; the assertion that they stay that way now covers the workspace paths too, so the rule has one negative assertion per close, not a rule per branch.</li><li>The plane depends on nothing herdr reports as the session focus. Reuse finds a workspace by its checkout path, grouping and naming go by the handles the plane recorded, and every herdr command the plane sends names an explicit workspace, tab, or pane id. Focus drift after a close is therefore harmless to the plane&#39;s own work.</li><li>Goto becomes load-bearing in a new way. Because it is the only focus move the plane makes, it is the only way the operator can get the plane to bring the view to an agent, and its wording still has to name the workspace the view landed in. The earlier note that a CLI focus is only a hint, because each client keeps its own view, is wrong for herdr 0.9.1: a focus request on the public socket moves every client, and that is what makes Goto a move rather than a hint. The operation pages and the verification record carry the corrected wording.</li><li>The rule has no mode. There is no setting that asks the plane to return the focus, because a setting would mean a mode in which the plane moves the operator&#39;s view without being asked.</li><li>What a close destroys is unchanged. The seat, the leftover-environment accounting, the worktree-versus-tab reach, and the route-close decision all stand as ADR 0046 and ADR 0012 write them. <strong>This amends the close behavior ADR 0046 describes; it does not change that decision.</strong> ADR 0046&#39;s route still closes the previous handoff&#39;s environment at the ask; only the focus command that followed the close is retired.</li><li>The ADR index is hand-maintained: <code>docs/adr/index.md</code> is a written list, not a generated one, and a new decision takes the next free number and is appended at the end of the list rather than inserted by date or title. This entry is ADR 0061 because 0057 to 0060 were already taken by the time it landed, and the index carries it as the last line.</li><li>The suite cannot observe a window, so the live herdr 0.9.1 walk is not claimed by any test here. It stays with the operator: a Close cleanup of a worktree workspace while another workspace is viewed, and a queued route item&#39;s close that lands while the view is elsewhere. Until the operator runs it, the verification record names it incomplete.</li></ul>',18)])])}const w=o(n,[["render",r]]);export{u as __pageData,w as default};
