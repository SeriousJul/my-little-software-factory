import{_ as t,o as r,c as a,a0 as o}from"./chunks/framework.ysVzM4be.js";const p=JSON.parse(`{"title":"ADR 0008: The turn log comes from the agent's session record, not the terminal","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0008-turn-log-comes-from-the-session-record.md","filePath":"adr/0008-turn-log-comes-from-the-session-record.md"}`),n={name:"adr/0008-turn-log-comes-from-the-session-record.md"};function s(i,e,l,h,d,c){return r(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="adr-0008-the-turn-log-comes-from-the-agent-s-session-record-not-the-terminal" tabindex="-1">ADR 0008: The turn log comes from the agent&#39;s session record, not the terminal <a class="header-anchor" href="#adr-0008-the-turn-log-comes-from-the-agent-s-session-record-not-the-terminal" aria-label="Permalink to &quot;ADR 0008: The turn log comes from the agent&#39;s session record, not the terminal&quot;">​</a></h1><p>Status: accepted Date: 2026-09-02</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>When a turn settles, the decision modal shows the agent&#39;s work as a Turn log. Today the control plane captures that work as the agent pane&#39;s last 200 terminal rows (<code>herdr agent read</code>). Terminal rows are rendered UI: for pi and codex they mix the agent&#39;s text with tool call lines, tool output, spinner rows, and prompt echo. The operator asked for the agent&#39;s messages without that garbage.</p><p>The same observation poll that reports agent state (<code>herdr agent list</code>) also reports each agent&#39;s native session record when the matching herdr integration is installed: for pi a JSONL session file, for codex and claude a session reference of the same shape. The session record stores every event of the turn in order: the agent&#39;s text (raw markdown), tool calls, tool results, and thinking blocks. It is a versioned file format the runtime writes, not a screen scrape.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The Turn log is built from the agent&#39;s session record.</p><p>At settle time the control plane takes the session path from the poll&#39;s <code>agent list</code> result, hands it to a per-agent-type reader (the pi reader first, codex and claude when needed), and stores the resulting entries in the Completion trace. The trace&#39;s last message becomes the agent&#39;s final text, so the restart prompt and the detail pane read the same clean message.</p><p>A terminal capture remains as the fallback: when herdr reports no session, when the record is missing or unreadable, or when the agent type has no reader, the turn&#39;s log degrades to the capture, switched to herdr&#39;s <code>recent-unwrapped</code> source.</p><p>The considered alternatives:</p><ul><li>Heuristic filtering of the terminal text. Rejected: it keeps the control plane runtime-free, but every runtime that changes its screen layout can break the filter, and a rendered screen cannot say where one message ends and the next begins.</li><li>A new herdr command that returns a clean message. Rejected for now: it would keep the control plane thin, but it waits on a herdr release and pushes the same runtime knowledge into herdr. The session record is already reported by herdr today, so the control plane can take it without a new dependency.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The control plane now parses runtime file formats. The parsers are keyed by the agent type&#39;s kind and live behind one seam, so a runtime that changes its format breaks one reader, not the observation loop, and degrades to the terminal capture instead of failing the turn.</li><li>The Completion trace grows a turn log column. Old traces carry no log, so the reader derives a plain-text log from their last message.</li><li>The restart prompt and the detail pane read the clean final text instead of raw terminal rows.</li><li>An agent type without a reader still works, on the fallback.</li></ul>',13)])])}const u=t(n,[["render",s]]);export{p as __pageData,u as default};
