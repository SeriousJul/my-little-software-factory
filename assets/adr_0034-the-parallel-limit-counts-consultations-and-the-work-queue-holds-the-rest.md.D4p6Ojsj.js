import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0034: The Parallel limit counts Consultations and the Work queue holds the rest","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0034-the-parallel-limit-counts-consultations-and-the-work-queue-holds-the-rest.md","filePath":"adr/0034-the-parallel-limit-counts-consultations-and-the-work-queue-holds-the-rest.md"}'),n={name:"adr/0034-the-parallel-limit-counts-consultations-and-the-work-queue-holds-the-rest.md"};function i(r,e,l,h,d,u){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0034-the-parallel-limit-counts-consultations-and-the-work-queue-holds-the-rest" tabindex="-1">ADR 0034: The Parallel limit counts Consultations and the Work queue holds the rest <a class="header-anchor" href="#adr-0034-the-parallel-limit-counts-consultations-and-the-work-queue-holds-the-rest" aria-label="Permalink to &quot;ADR 0034: The Parallel limit counts Consultations and the Work queue holds the rest&quot;">​</a></h1><p>Status: accepted Date: 2026-09-18</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Parallel limit counted ticket seats only and gated the automatic origins only: a manual handoff was always allowed, and a Consultation never entered the count. The number of agents the factory ran was bounded on one side only. A full factory still accepted every manual start, and a Consultation could be opened unbounded, so the operator had no single answer to &quot;how much work may run at once&quot; and no surface to stage work that could not start yet.</p><p>The operator&#39;s need: one configurable cap over all running work, a manual start that cannot take a seat waiting in a visible, ordered queue instead of being refused or starting anyway, an automatic pickup when a seat frees, and a way to push one queued item to the front over the cap.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Parallel limit is the one cap over all running work.</strong> It keeps the ticket seats of ADR 0021 - an in-flight ticket whose agent the latest poll listed, every in-progress handoff, a started agent inside its Startup grace - and gains the Consultation seats: a Consultation in <code>opening</code> or <code>working</code> holds one, the other states hold none. It gates every start:</p><ul><li>A manual start that cannot take a seat enters the Work queue instead of starting: the handoff from the ticket detail, the route from the decision row, the restart from the Missing modal, and the Consultation launcher submit.</li><li>An automatic start - the open dispatch, the workflow route, the restart - waits for a seat as before, reading the combined count.</li></ul><p><strong>The Work queue.</strong> A durable, ordered list of starts waiting for a seat: the manual Handoff intents (ticket, origin, the operator&#39;s choice) and the Consultations in <code>queued</code> state, one shared order across both kinds. When a seat frees, the observation cycle - the single owner of &quot;what may start now&quot; - starts the queue before auto-dispatch, up to the free seats, and only then lets auto-dispatch fill what remains. The operator reorders the items, removes an item, and force-dispatches an item over the cap. Force-dispatch re-runs every start check the normal pickup runs and skips only the cap.</p><p>A pickup is a manual start: every hard check still runs (the ticket still holds the state the item&#39;s origin requires, the source is healthy, the Setting fit passes), but no automatic gate holds it - the Dispatch pause and the Same-type hold gate the automatic origins, and the item is the operator&#39;s own ask, exactly as a direct manual handoff is today. A pickup that fails a check leaves the queue with a Message line warning: the ticket keeps its state, and a Consultation whose start fails becomes a <code>failed</code> record, as it does today.</p><p><strong>A queued Consultation is a record, not a bare intent.</strong> A launcher submit into a full cap creates the durable Consultation record in <code>queued</code> state; it holds no environment and no agent until pickup, which re-reads the Consultation type&#39;s settings from the config. Removing it from the queue unschedules it: the record keeps an <code>unscheduled</code> state, the Consultation section lists it, and the operator schedules it back, starts it, or deletes the record there. Removing a Handoff item cancels it: the intent is deleted and the ticket keeps its state. One queue item per ticket: a second add of the same ticket is refused while the first waits.</p><p>The considered alternatives:</p><ul><li>Two caps, the ticket cap and a combined cap. Rejected: two knobs with overlapping meaning, and the operator reads the true ceiling from neither alone.</li><li>A Consultation queue item as a bare intent, the record created at pickup. Rejected: removing the item from the queue would delete the drafted intent, and the item would live outside the Consultation list. The record in <code>queued</code> state lets unschedule keep the work and lists the item like every other Consultation.</li><li>Auto-dispatch items visible in the same queue, reorderable by the operator. Rejected: the queue stages work the operator asked for. Auto-dispatch already orders by Ticket priority and re-checks every poll, and mixing the two ordering rules into one list made neither readable.</li><li>Refuse a manual start at a full cap. Rejected: the operator would watch for a seat and re-issue the same work; the queue waits for them.</li><li>A hard cap with no bypass. Rejected: urgent work that arrives while the factory is full had no path to the front other than reorder; the force-dispatch gives the direct override with every other check still running.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A manual handoff is no longer always allowed: at a full cap it queues. The &quot;manual handoff is always allowed&quot; clause of the Parallel limit is superseded by this ADR. The Handoff limit, the Dispatch pause, and the Same-type hold keep their roles; the pause and the hold simply never see a queued item&#39;s pickup, because the pickup is a manual start.</li><li>The seat count gains a state-based Consultation side. A stuck <code>opening</code> holds its seat, so its recover is never capped and can never push the count past the cap.</li><li>The mode line counts both kinds against the one cap, and the left pane gains the queue as a third Section: always visible, its header carrying the depth, its rows selectable into the detail pane.</li><li>The schema grows the durable queue - the Handoff intents and the shared order - and the Consultation states gain <code>queued</code> and <code>unscheduled</code>.</li><li>With the cap at 0 (unlimited) the queue never engages and every start behaves as before this ADR.</li></ul>',15)])])}const m=t(n,[["render",i]]);export{p as __pageData,m as default};
