import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"Ticket labels","description":"","frontmatter":{},"headers":[],"relativePath":"development/labels.md","filePath":"development/labels.md"}'),r={name:"development/labels.md"};function n(i,e,l,d,h,c){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="ticket-labels" tabindex="-1">Ticket labels <a class="header-anchor" href="#ticket-labels" aria-label="Permalink to &quot;Ticket labels&quot;">​</a></h1><p>The control plane reads labels as source facts, and it writes them through the transitions of the <a href="./../adr/0027-the-plane-owns-the-workflow-machine-and-label-transitions.html">workflow machine</a>. After a transition, the label set matches the machine&#39;s spec, whatever labels the agents or humans wrote before. The agents no longer write workflow labels: the task templates carry no label instructions. A human label write stands until the next transition.</p><table tabindex="0"><thead><tr><th>Label</th><th>Meaning</th></tr></thead><tbody><tr><td><code>ready-for-agent</code></td><td>An open GitHub issue is ready for implementation or another configured task. No transition writes it, so the plane neither adds nor removes it: it is a rank and an operator&#39;s signal, not a gate.</td></tr><tr><td><code>blocked</code></td><td>The item is not ready for handoff. The default GitHub sources exclude it, and the machine owns no <code>blocked</code> fact, so a write of it is the operator&#39;s. A native <code>blocked by</code> link to an open issue excludes the issue the same way, whatever its labels. The app never sees the issue. A closed blocking issue unblocks it.</td></tr><tr><td><code>ready-for-review</code></td><td>A non-draft pull request is ready for review.</td></tr><tr><td><code>ready-to-ship</code></td><td>A non-draft pull request passed review with a score that reached the review transition&#39;s configured threshold (90 in the default machine). It is ready to be squash-merged.</td></tr><tr><td><code>needs-work</code></td><td>A pull request needs rework. This takes priority over <code>ready-for-review</code> and can apply to a draft. A blocked merge of a <code>ready-to-ship</code> pull request lands here.</td></tr></tbody></table><p>A pull request that carries none of these labels is on the machine&#39;s parking state: the default source lists it, because that is how the implement transition reaches the pull request the agent just opened, and the plane suggests nothing for it until a transition or a human labels it.</p><p>A label a state match names but no transition writes is a scoping label the operator owns: the fire leaves it on the surface, so a state may gate on a label the machine never touches, such as a <code>labels-all = [&quot;factory&quot;]</code> filter that keeps the machine to one project&#39;s items.</p><h2 id="creating-the-machine-s-labels" tabindex="-1">Creating the machine&#39;s labels <a class="header-anchor" href="#creating-the-machine-s-labels" aria-label="Permalink to &quot;Creating the machine&#39;s labels&quot;">​</a></h2><p>The plane writes its labels with <code>gh issue edit</code> and <code>gh pr edit</code>, and GitHub refuses a write of a label the repository does not have: a fire that names a missing label fails the whole write, records the failure on the turn&#39;s trace, and routes nothing from it. Create the labels your machine writes before the first fire can succeed. The default machine writes <code>ready-for-review</code>, <code>ready-to-ship</code>, and <code>needs-work</code> on pull requests; <code>ready-for-agent</code> and <code>blocked</code> stand on the source side and the plane never writes them in the default machine. Create them in the repository&#39;s labels page, or with <code>gh label create &lt;name&gt; --repo &lt;owner&gt;/&lt;name&gt;</code> for each one your states or transitions name.</p>',7)])])}const b=t(r,[["render",n]]);export{p as __pageData,b as default};
