import{_ as t,o as n,c as o,a0 as s}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse(`{"title":"ADR 0044: The Consultation close verifies the Agent's identity before it takes anything down","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0044-the-consultation-close-verifies-the-agents-identity-before-it-takes-anything-down.md","filePath":"adr/0044-the-consultation-close-verifies-the-agents-identity-before-it-takes-anything-down.md"}`),a={name:"adr/0044-the-consultation-close-verifies-the-agents-identity-before-it-takes-anything-down.md"};function i(r,e,d,h,l,c){return n(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0044-the-consultation-close-verifies-the-agent-s-identity-before-it-takes-anything-down" tabindex="-1">ADR 0044: The Consultation close verifies the Agent&#39;s identity before it takes anything down <a class="header-anchor" href="#adr-0044-the-consultation-close-verifies-the-agent-s-identity-before-it-takes-anything-down" aria-label="Permalink to &quot;ADR 0044: The Consultation close verifies the Agent&#39;s identity before it takes anything down&quot;">​</a></h1><p>Status: accepted Date: 2026-09-22</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The operator closed a <code>missing</code> Consultation in the morning after a herdr restart. The record&#39;s stored handles named the tab <code>wAR:t29</code> and the pane <code>wAR:p29</code>. Herdr&#39;s restore had handed the same public ids to a restored environment: the tab stood again, its pane was a bare shell, and the operator ran the control plane in it. The Consultation&#39;s own Agent was not there; the observation loop had already marked the record <code>missing</code>.</p><p>The close planned its cleanup from the stored ids alone. The topology probe asked, &quot;does the owned tab hold only the owned pane id?&quot;, and herdr answered yes, so the close issued <code>tab close wAR:t29</code>. The id was exclusive and stale at once: the pane the id named no longer held the Consultation&#39;s Agent, it held the control plane&#39;s own environment. The tab close took the control plane down with it, and the record died in <code>closing</code> with its resources unconfirmed.</p><p>The defect was the guard. ADR 0043 established the identity rule for tickets: a live agent belongs to a ticket by name, not by pane id, because herdr hands a closed pane&#39;s id out again. The Consultation close had not received the same rule: it verified the topology by id and trusted the result. ADR 0037 promised that a <code>missing</code> or a <code>failed</code> Consultation &quot;holds nothing live that should be stopped&quot; and closes directly; the close kept issuing destructive commands anyway, and the ids alone decided what they took down.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The close verifies the Consultation&#39;s own Agent before it may take anything down.</strong> Before the first destructive command, the close probes herdr&#39;s agent list and matches the record&#39;s Agent in this order:</p><ol><li><strong>The name the Agent runs under.</strong> The name is the identity herdr enforces: it refuses to start a second Agent under a name a live Agent holds, so one named match is the record&#39;s Agent. A named match whose session contradicts the stored session, and a name held by more than one Agent, are ambiguous: the close takes nothing down and leaves the record in <code>closing</code> for a retry or a Force-close.</li><li><strong>The stored stable session id</strong>, when this herdr omits names from the list. One session match is the Agent; more than one is ambiguous.</li><li><strong>The stored pane</strong>, only when no Agent in the list reports a name: the weak match the observation loop uses, its <code>unverifiable</code> tier (ADR 0043). An Agent in the stored pane whose session contradicts the stored one is a foreign Agent, and the record&#39;s own is then absent.</li></ol><p><strong>The close follows a verified Agent.</strong> When the matched Agent&#39;s handles differ from the stored ones, the close retargets the record&#39;s handles and its owned resource rows to the Agent, then plans the cleanup from the environment the Agent holds. It closes what the Agent is in, never what the Agent left.</p><p><strong>A missing Agent closes nothing.</strong> When no Agent matches, the close issues no destructive command:</p><ul><li>A record that began the close in <code>opening</code> is unverified, because the Agent may still be starting. The close takes nothing down and leaves the record in <code>closing</code> with a recovery warning; a retry or a Force-close decides it.</li><li>Any other state retires the record: it closes with no command, records its owned resources as remaining, and leaves herdr untouched. The operator sees what stands, and the ADR 0037 promise holds: there was nothing to stop.</li></ul><p><strong>An unverifiable probe is a recovery, never a blind close.</strong> A failed or unreadable agent list leaves the record in <code>closing</code> with a warning, the way an unverifiable topology already does. A close that recorded no pane, a record that never started an Agent, still closes with no command at all, as before.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The close&#39;s first herdr call is an <code>agent list</code> probe whenever the record owns a pane. The probe is read-only; a close that retires the record issues exactly one command, the probe itself.</li><li>A stale handle can no longer steer the close at a foreign environment, because the stored ids decide the plan only after the Agent is verified at them. A restored tab that reuses an old id holds a bare shell or a foreign Agent, and both answers close nothing.</li><li>A Consultation whose Agent moved is closed where the Agent stands. The record&#39;s handles and resource rows move with it, so the close, the remaining resources, and the detail all name the same environment.</li><li>An interrupted opening cannot be closed blind. Its close waits on a retry or a Force-close, and a Force-close on it records the environment it left standing instead of taking it down on an unverified id.</li><li>The close and the observation loop now hold the same identity rule for Consultations: name first, session second, pane only as the unnamed tier. A herdr that reports names keeps the strong match; an older herdr that does not falls back in the tested order instead of trusting ids.</li></ul>',15)])])}const m=t(a,[["render",i]]);export{u as __pageData,m as default};
