import{_ as t,o as a,c as s,a0 as o}from"./chunks/framework.D4fUSYzX.js";const l=JSON.parse('{"title":"ADR 0052: The queue pause is factory state","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0052-the-queue-pause-is-factory-state.md","filePath":"adr/0052-the-queue-pause-is-factory-state.md"}'),n={name:"adr/0052-the-queue-pause-is-factory-state.md"};function i(r,e,h,u,d,c){return a(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="adr-0052-the-queue-pause-is-factory-state" tabindex="-1">ADR 0052: The queue pause is factory state <a class="header-anchor" href="#adr-0052-the-queue-pause-is-factory-state" aria-label="Permalink to &quot;ADR 0052: The queue pause is factory state&quot;">​</a></h1><p>Status: accepted Date: 2026-09-22</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The operator wanted a brake on the Work queue itself: stop the starts that leave the queue without touching the Auto-handoff mode, the Dispatch pause, or the Parallel limit. The Dispatch pause is automatic: a held turn set it, and a completed settle or the operator&#39;s decision ends it (ADR 0016). The Parallel limit is the cap over running work, not a stop on the queue. Neither is the operator&#39;s on or off on the queue&#39;s drain.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The queue pause is factory state on the state file</strong>, the way the Auto-handoff mode is (ADR 0036): a fresh state file starts resumed, the <code>p</code> key in the Work queue section writes the new value at once, and a restart finds the brake where the operator left it.</p><p><strong>The brake holds the drain and the top-up.</strong> While it stands, the pickup takes no item, Consultations included, the automatic top-up adds none (ADR 0051), and a manual enqueue that lands while paused sits in the queue without starting. Resuming triggers an immediate pickup pass, so the queued work starts in the same keystroke.</p><p><strong>The brake does not hold the ask.</strong> A force-dispatch still starts its item over the cap while the queue is paused: the operator&#39;s explicit intent passes the brake the way it passes the cap (ADR 0049).</p><p><strong>The two pauses stay two facts.</strong> The mode line&#39;s <code>paused</code> word remains the Dispatch pause&#39;s, and the queue pause shows on the Work section&#39;s header, where the pane owns the state. Both can stand at once, and each displays in its own place. The <code>p</code> toggle joins the Action bar and Key guide from the queue section as every other queue key does.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>p</code> was unbound in the Control catalogue; the queue section takes it, and the sections that do not own it refuse it in their own words and name it nowhere.</li><li>A paused queue reads as waiting, not as stuck: the items hold their places for the resume, the header names the pause, and nothing in the queue is in a failure state (ADR 0049), so the resume drains whatever the pickup can start.</li><li>The two pauses can stand together: the automatic pause holds the top-up&#39;s adds, the brake holds the drain, and the operator lifts each in its own way, decide the held turn or press <code>p</code>.</li></ul>',11)])])}const f=t(n,[["render",i]]);export{l as __pageData,f as default};
