import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"Completion","description":"What happens after a turn settles: the completion trace, auto-handoff, held turns, and the Dispatch pause.","frontmatter":{"title":"Completion","description":"What happens after a turn settles: the completion trace, auto-handoff, held turns, and the Dispatch pause."},"headers":[],"relativePath":"work-flow/completion.md","filePath":"work-flow/completion.md"}'),s={name:"work-flow/completion.md"};function i(d,e,l,r,h,c){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="completion" tabindex="-1">Completion <a class="header-anchor" href="#completion" aria-label="Permalink to &quot;Completion&quot;">​</a></h1><h2 id="completion-and-auto-handoff" tabindex="-1">Completion and auto-handoff <a class="header-anchor" href="#completion-and-auto-handoff" aria-label="Permalink to &quot;Completion and auto-handoff&quot;">​</a></h2><p>The control plane polls herdr for its agents every <code>agent-poll-interval-seconds</code>. The poll reads the agent list, and it reads the last message of an agent that has settled its turn. It never writes to herdr.</p><p>An agent can outlive the work cycle that started it: the Close cleanup cannot remove a dirty checkout, and the operator can re-prompt a settled agent in its herdr pane. The cycle is closed, so the ticket rests <code>open</code>, and the next handoff of that ticket meets the herdr agent name the live agent still holds. The poll reclaims it (ADR 0011): a working or blocked agent in the pane of a ticket&#39;s last closed handoff records a handoff of the current cycle and runs the ticket again, with a warning line that names the ticket. An idle, done, or unknown report reclaims nothing. The reclaimed agent settles, awaits, and closes like any other, and it holds a parallel slot while it works.</p><p>What the poll cannot reclaim stays as a fact on the ticket (ADR 0012): a Close cleanup that fails, or a handoff that meets its own leftover name, records the herdr environment that is still alive, and the ticket wears the <code>leftover</code> marker until the operator ends the environment in herdr - the control plane keeps no clear of its own (ADR 0032). That handoff still starts: it takes the ticket&#39;s cycle name, and it works beside the leftover agent in the reused workspace until someone ends it.</p><p>When the agent settles its turn (herdr reports it as done, or it is idle at the end of the turn), the ticket moves to <code>awaiting</code>. The completion trace records the task type, the agent, the Model, Thinking level, and context window that handoff started with, the completion time, the last message, the turn end cause, and the decision that ends the awaiting state. A workflow handoff that follows an awaiting ticket renders its prompt with the <code>{previous-message}</code> placeholder filled from that last message, so the next agent reads what the previous one left behind.</p><p>In manual mode, <code>awaiting</code> waits for the operator. Enter opens the decision modal. The operator routes the ticket to a workflow target or closes the work cycle.</p><p>Auto-handoff mode decides without the operator, within the configured limits:</p><ul><li>With auto-handoff on, the control plane decides every settled turn without the operator: it routes when the task type has exactly one outgoing workflow edge with exactly one target, and the parallel limit has room. At the per-ticket handoff limit the route degrades to close. Zero or multiple edges close the cycle. A full parallel limit leaves the ticket awaiting until a slot frees. The route&#39;s <code>auto-handed-off</code> decision lands the same way the operator&#39;s does: only once the routed handoff has started the agent. A route that cannot start - because its Agent takes one of the settings its target Task profile names - records nothing on the turn, says why on the status line, and leaves the turn undecided, so the next poll can route it once the config or the panel fixes the pair.</li><li>With auto-handoff off, the same decisions apply to auto-close task types only. A task type that is not auto-close leaves its settled turn awaiting for the operator.</li><li>Every eligible open ticket is handed off with the config defaults when the parallel limit allows it. The limit counts the live agents: the in-flight tickets in <code>handed-off</code> or <code>running</code> whose agent was alive in the latest poll. A blocked agent still counts; a missing agent holds no slot.</li><li>The per-ticket handoff limit stops the close-and-rehandoff loop. When a ticket reaches it, auto-handoff leaves it open. A manual handoff may pass the limit.</li></ul><p>Both limits gate auto-handoff only. A manual handoff is always allowed.</p><h2 id="held-turns-and-the-dispatch-pause" tabindex="-1">Held turns and the Dispatch pause <a class="header-anchor" href="#held-turns-and-the-dispatch-pause" aria-label="Permalink to &quot;Held turns and the Dispatch pause&quot;">​</a></h2><p>The completion trace records the turn end cause beside the last message, read from the agent&#39;s session record in the same read that gives the turn log (ADR 0015): <code>completed</code>, <code>failed</code>, <code>aborted</code>, <code>truncated</code>, or <code>unknown</code>. The readers know three kinds: <code>pi</code>, <code>codex</code>, and <code>claude</code>. Every other kind, a missing or unreadable record, and a malformed record settle <code>unknown</code>, which fails open and auto-decides exactly as it did before: the absence of evidence is not evidence of failure. A record whose last turn-end event predates the handoff is not this turn&#39;s end: it settles <code>unknown</code>, never <code>completed</code>.</p><p>An upgrade holds nothing. A trace that settled before the upgrade carries no cause and reads <code>unknown</code>, so the factory does not freeze on install. The accepted consequence: a turn that failed before the upgrade and is still <code>awaiting</code> after it can still be closed automatically, because the control plane holds no cause for it.</p><p>A turn that settled <code>failed</code>, <code>aborted</code>, or <code>truncated</code>, and that no decision has landed on, is held (ADR 0016). No automatic decision runs on a held turn: the control plane does not close its cycle and does not route it, in auto mode or manual mode. The ticket rests in <code>awaiting</code>, shown held in the ticket list, in the detail pane, and in the attention line, until the operator decides it. Once the operator decides the held turn, it is no longer held.</p><p>A held turn that settled <code>failed</code>, with no <code>completed</code> settle since it, also pauses the dispatch: while the pause is on, auto mode starts no agent by itself. The pause is derived from the completion traces on every cycle and never stored, so it survives a restart. It holds only the automatic origins - the open handoff, the workflow route, and the restart of a missing agent - and it ends at the next <code>completed</code> settle, or when the operator decides the held turn that started it. It never blocks a manual handoff. The open handoff and the restart run only in auto mode; the route block applies in manual mode too, where the auto-close types still route without the operator, exactly like the Parallel limit. A Consultation never contributes to the pause.</p><p>A missing agent in auto mode restarts the handoff once, with the last message as the previous message. At the per-ticket handoff limit, the cycle is abandoned instead. When the agent is missing again, or the parallel limit is full, the control plane stops: the <code>missing</code> badge stays until the operator restarts or abandons from the missing modal. In manual mode the control plane never touches a missing agent. Abandon ends the work cycle, closes the handoff&#39;s environment, and returns the ticket to open with its cycle number incremented.</p>',16)])])}const f=t(s,[["render",i]]);export{u as __pageData,f as default};
