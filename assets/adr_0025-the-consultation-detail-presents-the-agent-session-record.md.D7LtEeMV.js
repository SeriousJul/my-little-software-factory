import{_ as t,o as a,c as s,a0 as n}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse(`{"title":"ADR 0025: The Consultation detail presents the Agent's session record","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0025-the-consultation-detail-presents-the-agent-session-record.md","filePath":"adr/0025-the-consultation-detail-presents-the-agent-session-record.md"}`),o={name:"adr/0025-the-consultation-detail-presents-the-agent-session-record.md"};function r(i,e,h,d,l,c){return a(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0025-the-consultation-detail-presents-the-agent-s-session-record" tabindex="-1">ADR 0025: The Consultation detail presents the Agent&#39;s session record <a class="header-anchor" href="#adr-0025-the-consultation-detail-presents-the-agent-s-session-record" aria-label="Permalink to &quot;ADR 0025: The Consultation detail presents the Agent&#39;s session record&quot;">​</a></h1><p>Status: accepted Date: 2026-09-15</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Consultation detail&#39;s body is the Agent view: the capped plain-text read of the agent pane (<code>herdr agent read</code>, the <code>recent-unwrapped</code> source), refreshed every second while the Consultation is selected. It is a screen scrape. It is capped, it strips the terminal&#39;s styling, and it mixes the runtime&#39;s own screen (spinner rows, tool output, prompt echo) into the exchange. The operator asked for the exchange itself in the detail: the operator&#39;s inputs and the agent&#39;s messages, in order.</p><p>The agent&#39;s real pane cannot be shown inside the detail. The control plane owns the whole terminal it runs in, and the agent pane is a separate herdr pane. herdr offers snapshots of that pane, a focus of it, and a full-screen attach that takes over the running terminal. None of these renders inside the control plane&#39;s own surface. The operator&#39;s path to the real pane is a focus, the same one the Ticket Live view&#39;s Goto row already runs.</p><p>The session record is already the control plane&#39;s source of truth for the turn: the Turn log (ADR 0008) and the turn end cause (ADR 0015) both come from it, and the observation reads it while the work is still live. The record file grows while the agent works, so a live read returns the whole exchange so far: every operator input and every agent message, with the tool calls between them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Consultation detail body is the Session view.</strong> It presents the agent&#39;s session record: the operator&#39;s inputs and the agent&#39;s messages in order, with one short note per tool call. It is the default body for open and closed Consultations alike, and the detail&#39;s border title states which body shows: &quot;Session view&quot; when the record renders, &quot;Agent view&quot; when the fallback does.</p><p><strong>The read goes through the per-agent-kind session reader.</strong> The module that already builds the Turn log (ADR 0008) gains a full-session read, the <code>pi</code> kind first. The control plane adds no runtime package: an Agent type&#39;s kind selects the reader, and a kind without one simply has no Session view.</p><p><strong>The terminal stays the fallback.</strong> When the kind has no reader, herdr reports no session path, or the record is unreadable, the body degrades to what the detail shows today: the plain-text pane read for an open Consultation, the Captured history for a closed one. The pane read keeps running either way: it feeds the Stale Agent output fact and the fallback body, so a failing terminal is still reported while the Session view is fresh.</p><p><strong>A Goto control reaches the real pane.</strong> Key <code>g</code> in both Consultation base modes, available while the selected Consultation&#39;s pane is alive in the last poll. It runs <code>herdr agent focus</code> and changes no Consultation state: it is navigation, not a decision. A note confirms it on the Message line.</p><p><strong>Nothing else moves.</strong> The Agent interaction mode keeps the visible ANSI screen, its fast refresh, and its key forwarding. The Ticket side keeps its Live view stream and the Turn log in the Decision modal.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><p><strong>Embed the agent&#39;s real pane in the detail.</strong> Rejected: not buildable. The control plane renders the whole terminal it runs in, and herdr offers no way to render another pane inside a third program&#39;s surface. The focus is the real pane.</p><p><strong>Make the visible ANSI re-render the primary body.</strong> The mechanism exists - the interaction mode uses it - but it is still a screen scrape: the visible screen is a cap, and the runtime&#39;s UI mixes into the exchange. The session record is complete and structured. The ANSI screen stays where the operator types: the interaction mode.</p><p><strong>Depend on the runtime&#39;s own package to read the record.</strong> Rejected: it couples every Agent type to one runtime and breaks the agent-agnostic rule for a capability the per-kind reader already provides. A format change breaks one reader and degrades to the fallback, as ADR 0008 already bounds.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The per-kind reader grows from a settle-time read to a live read on the detail&#39;s refresh tick. A runtime that changes its record format now also affects the live body, but the failure is a fallback to the terminal body, never a dead detail.</li><li>The Consultation detail shows the exchange, not the screen: tool calls read as one note each, and an operator input appears where the agent&#39;s answer to it appears.</li><li>An Agent type without a session reader is unchanged in everything the operator sees: its detail keeps the terminal body, the Goto, and the interaction mode.</li><li>The Stale Agent output fact keeps its meaning: the pane read still runs, so a terminal that fails is reported even while the Session view is fresh.</li></ul>',18)])])}const g=t(o,[["render",r]]);export{u as __pageData,g as default};
