import{_ as t,o as a,c as n,a0 as s}from"./chunks/framework.ysVzM4be.js";const p=JSON.parse('{"title":"ADR 0030: A starting ticket wears a spinner in place of its state badge","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0030-a-starting-ticket-wears-a-spinner-in-place-of-its-state-badge.md","filePath":"adr/0030-a-starting-ticket-wears-a-spinner-in-place-of-its-state-badge.md"}'),o={name:"adr/0030-a-starting-ticket-wears-a-spinner-in-place-of-its-state-badge.md"};function i(r,e,d,h,c,l){return a(),n("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0030-a-starting-ticket-wears-a-spinner-in-place-of-its-state-badge" tabindex="-1">ADR 0030: A starting ticket wears a spinner in place of its state badge <a class="header-anchor" href="#adr-0030-a-starting-ticket-wears-a-spinner-in-place-of-its-state-badge" aria-label="Permalink to &quot;ADR 0030: A starting ticket wears a spinner in place of its state badge&quot;">​</a></h1><p>Status: accepted Date: 2026-09-18</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A Handoff moves the ticket&#39;s state in two steps after the operator presses the key. The claim records the Handoff attempt and leaves the ticket where it stands; only when the agent starts does the settle move the ticket to <code>handed-off</code>, and the first observation that sees the agent working moves it to <code>running</code>.</p><p>The list gave the operator no part of that story. The row stayed <code>[open]</code> from the keypress until the agent started, and the only feedback was the Message line&#39;s progress. Then the badge flipped to <code>[handed-off]</code>, and flipped again to <code>[running]</code> at the next observation. The start window read as a silent row and two status flips: the operator could not tell from the row that the key had registered, and the flips said nothing the operator could act on.</p><p>The facts the face needs already exist. The unresolved-attempt fact says a handoff is in progress from the claim until the agent starts or the start fails, and the <code>handed-off</code> state says the agent has started and the work is not yet observed. Nothing drew either.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Starting window.</strong> The window during which a ticket&#39;s Handoff is claimed and not yet settled, or the ticket is <code>handed-off</code>. One face wears the whole window: an animated spinner with the written word <code>starting</code>, in the state badge slot of the ticket row and of the ticket detail, in place of the state badge. The <code>[handed-off]</code> badge is never drawn. The row timeline becomes <code>[open]</code>, spinner, <code>[running]</code>: one face on the way in, one flip when the work is observed.</p><p>The face is a presentation of two existing facts. The domain keeps the <code>handed-off</code> state for durability, the Startup grace, and the observation, and the section header counts keep counting a <code>handed-off</code> ticket as running. No new state, no migration.</p><p>The window is bounded by the claim the run made, not by the raw unresolved-attempt fact. A crashed hand-off leaves an unresolved attempt that stays blocked across a restart, and the ticket shows its recovery fact. A spinner driven by the raw fact would spin forever on such a ticket. So the window is: a claim made in this run, reported by the hand-off dispatch on claim and on settle, or the <code>handed-off</code> state. After a restart, a <code>handed-off</code> ticket wears the spinner until the first observation sees its work, which is the truth: the control plane has not yet observed it.</p><p>A failure marker (blocked, missing) beats the spinner the way it beats the state badge today. A dead or blocked agent is never hidden behind a spinner, and the recovery screens stay reachable.</p><p>Every Handoff origin wears the same face: manual hand-off, workflow route, restart, and Auto-handoff. The face states the ticket&#39;s fact, not who asked.</p><p>The spinner carries its written word the way the shared state words do, so the NO_COLOR presentation removes nothing. It is a control of the shared control library with a gallery example, and it drives itself the way the Decision modal pop-in does, so the deterministic test renderer paints the first frame and the snapshots hold.</p><p>The considered alternatives:</p><ul><li>A spinner only during the claim window, then the <code>[handed-off]</code> badge. Rejected: the row still flips twice inside the start window, spinner to badge to running. The ask was a coherent start, and this keeps two of the flips.</li><li>No face at all; the Message line carries the feedback. Rejected: the row the operator acted on stays <code>[open]</code>, and the feedback lives on a line the operator may not be watching.</li><li>Drive the spinner from the raw unresolved-attempt fact. Rejected: it spins forever on a crashed run&#39;s remnant.</li><li>A new ticket state <code>starting</code> in the durable state. Rejected: the window is a presentation of two existing facts. A stored state adds a fourth badge to manage, a migration, and transitions the domain does not need.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The operator sees the key register at once: the row leaves <code>[open]</code> for the spinner on the keypress, and stays one face until the work is observed.</li><li>The <code>[handed-off]</code> badge is no longer drawn by any surface. The state remains in the domain, the counts, and the observation, and the state badge mapping keeps its entry for the state.</li><li>A start that fails ends the face at once: the row returns to <code>[open]</code> with the error line, as today.</li><li>A turn that settles while the ticket is <code>handed-off</code> ends the face at the <code>awaiting</code> or held face, as the state rules already decide.</li><li>The Message line keeps its own progress line beside the face; the two say the same fact in two places.</li><li>The animated frame itself is not something the frame snapshots verify. The verification record states what the terminal walk covered.</li></ul>',17)])])}const w=t(o,[["render",i]]);export{p as __pageData,w as default};
