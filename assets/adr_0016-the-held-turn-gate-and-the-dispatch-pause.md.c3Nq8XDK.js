import{_ as t,o as a,c as o,a0 as n}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0016: The held turn gate and the Dispatch pause","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0016-the-held-turn-gate-and-the-dispatch-pause.md","filePath":"adr/0016-the-held-turn-gate-and-the-dispatch-pause.md"}'),s={name:"adr/0016-the-held-turn-gate-and-the-dispatch-pause.md"};function d(r,e,i,l,h,c){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0016-the-held-turn-gate-and-the-dispatch-pause" tabindex="-1">ADR 0016: The held turn gate and the Dispatch pause <a class="header-anchor" href="#adr-0016-the-held-turn-gate-and-the-dispatch-pause" aria-label="Permalink to &quot;ADR 0016: The held turn gate and the Dispatch pause&quot;">​</a></h1><p>Status: accepted Date: 2026-09-02 Superseded in part by ADR 0051: the Dispatch pause&#39;s hold of the automatic route in manual mode no longer stands, because manual mode no longer routes by itself. Its held turn gate and its hold of the top-up&#39;s adds stand.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0015 gives every settled turn a cause the agent&#39;s own record states. Today the control plane treats a settled turn as finished work and moves on: in auto-handoff mode it closes the cycle or routes the ticket, and it starts whatever the next work is. A turn that settled <code>failed</code> - the build broke, the API rejected the request, the context filled - is not finished work. The control plane still treats it as such, and the operator is left to notice a failure the record plainly recorded.</p><p>Two different failures the operator needs to see, each at its own scope:</p><ul><li>The <strong>ticket&#39;s own turn</strong> ended without completing. No automatic decision should run on it: the operator must decide the held turn, not have the control plane close the cycle on a failed build.</li><li>A failure has happened and nothing has settled <code>completed</code> since. The control plane should not keep starting more agents into a condition it is already failing on.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The held turn gate.</strong> A turn is held when its cause is <code>failed</code>, <code>aborted</code>, or <code>truncated</code> and no decision has landed on it. <code>unknown</code> is never held; it fails open (ADR 0015). While a ticket&#39;s latest turn is held, no automatic completion decision runs on that ticket: the control plane does not close its cycle and does not route it. The ticket rests in <code>awaiting</code>, shown held, until the operator decides it. Once the operator decides the held turn, it is no longer held, and the automatic flow resumes on the ticket&#39;s next turn.</p><p>The gate runs in auto-handoff mode and manual mode alike, because it is a per-turn fact about the settled work, not about the mode. A manual operator already decides every turn; the gate only adds the fact - the cause - to what they see, and it holds the auto-close task types a manual operator might not notice, like a <code>review</code> that fails.</p><p>The gate reads the stored cause of the ticket&#39;s latest completion. A turn settled <code>completed</code> or <code>unknown</code> passes it and auto-decides exactly as before.</p><p>A held turn settles at once, without waiting out the Startup grace: the grace defends against a silent idle that may still be booting, and a record that names a refusal is not silent. A <code>completed</code> or <code>unknown</code> settle still waits the grace out, so a booting agent&#39;s first idle report is not read as a turn end.</p><p>The held badge, the <code>held</code> count on the attention line, and the detail-pane warning show only while the ticket rests in <code>awaiting</code>. A held turn whose agent reports working again reopens as it always does: the ticket leaves <code>awaiting</code> for <code>running</code>, and the row shows its state badge, never <code>held</code> over an agent that is visibly working. Its next settle overwrites the same trace, cause and detail included.</p><p><strong>The Dispatch pause.</strong> The pause is on when the newest held turn settled <code>failed</code> and no turn has settled <code>completed</code> since it. It is derived from the completion traces on every cycle and never stored, so it survives a restart and cannot drift from the fact it describes. While it is on, auto-handoff mode starts no agent by itself. It ends at the next <code>completed</code> settle, or the moment the operator decides the held turn that started it.</p><p>The pause holds only the three automatic origins: the open handoff, the workflow route, and the restart of a missing agent. It never blocks a manual handoff, and it never blocks the operator&#39;s explicit close, goto, or route.</p><p>The pause is a state fact, not an auto-mode state. The open handoff and the restart exist only in auto-handoff mode, where the pause holds all three origins. In manual mode the one automatic start that still runs is the auto-close types&#39; route, and the pause holds it there too, exactly as the Parallel limit already does: a full limit waits in awaiting in both modes, because the auto-close type routes without the operator. A completed turn that cannot route during a pause rests in awaiting with its trace undecided, the way a full Parallel limit already leaves it, and the next cycle routes it once the pause ends.</p><p><strong>Why hold rather than retry.</strong> The control plane does not re-run a failed turn. A failed build or a rejected request will fail again on a blind retry, and the operator&#39;s decision - close it, go to the agent, reroute it - is the fact only the operator can make. The held gate and the pause hold the work and surface the cause; they do not guess the next move.</p><p>The pause reads only the ticket completion traces. A Consultation never contributes to it and never enters the Parallel limit count, per ADR 0007: a Consultation is a deliberately separate surface, and its failure must not gate ticket work.</p><p>The considered alternatives:</p><ul><li>Retry a failed turn automatically. Rejected: it repeats the failure the record already recorded, hides the cause from the operator, and burns an agent slot on work that needs a decision, not a rerun.</li><li>Store the pause as a flag the control plane clears. Rejected: a stored flag can drift from the trace that set it, and it would not survive a crash between the write and the clear. Deriving it from the traces makes it exact.</li><li>Pause on every held cause, not just <code>failed</code>. Rejected: <code>truncated</code> and <code>aborted</code> are one agent&#39;s own pressure - a full context, an interrupt - and a local event. Holding every unrelated agent because one context filled would gate the whole factory on a local fact. Only a <code>failed</code> turn says the work itself is failing.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A settled turn that failed, aborted, or truncated rests held in <code>awaiting</code>, shown as held in the ticket list, the detail pane, and the attention line, and its cause and detail named in the decision modal. The operator decides it instead of the control plane deciding for them.</li><li>A <code>failed</code> turn with no <code>completed</code> settle since pauses the automatic open handoff, the workflow route, and the restart. The open handoff and the restart run only in auto-handoff mode; the route block applies in manual mode too, because the auto-close types route there. A manual handoff always starts.</li><li>A turn the control plane could not read is never held: <code>unknown</code> fails open (ADR 0015), so a runtime that changes its record format holds nothing.</li><li>The pause is never stored. It is recomputed from the completion traces each cycle, so a restart cannot lose it, and a decided held turn ends it in the same cycle the decision lands.</li><li>ADR 0006 holds: the poll reads herdr and writes nothing to it. The cause is read, and the gate and the pause are derived, never provoked. ADR 0007 holds: a Consultation never gates ticket work. ADR 0011 and ADR 0012 are untouched: the Reclaim rule and the Leftover environment fact are unchanged.</li></ul>',21)])])}const f=t(s,[["render",d]]);export{p as __pageData,f as default};
