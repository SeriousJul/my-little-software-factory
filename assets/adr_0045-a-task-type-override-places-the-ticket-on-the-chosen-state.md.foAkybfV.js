import{_ as t,o as a,c as s,a0 as n}from"./chunks/framework.D4fUSYzX.js";const f=JSON.parse(`{"title":"ADR 0045: A task type override places the ticket on the chosen task's state","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0045-a-task-type-override-places-the-ticket-on-the-chosen-state.md","filePath":"adr/0045-a-task-type-override-places-the-ticket-on-the-chosen-state.md"}`),o={name:"adr/0045-a-task-type-override-places-the-ticket-on-the-chosen-state.md"};function i(r,e,h,l,c,d){return a(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0045-a-task-type-override-places-the-ticket-on-the-chosen-task-s-state" tabindex="-1">ADR 0045: A task type override places the ticket on the chosen task&#39;s state <a class="header-anchor" href="#adr-0045-a-task-type-override-places-the-ticket-on-the-chosen-task-s-state" aria-label="Permalink to &quot;ADR 0045: A task type override places the ticket on the chosen task&#39;s state&quot;">​</a></h1><p>Status: accepted Date: 2026-09-22</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The workflow machine places a ticket by its labels: the position is the first state whose match holds on the ticket, re-derived on every refresh (ADR 0027). In the machines the operators run, the entry into the machine is a hand-applied label - <code>ready-for-agent</code> on an issue, the states matched in order on <code>labels-any</code> - and no transition writes that label back.</p><p>The Override panel lets the operator change the task type of one Handoff. The completion behavior already follows the override: the chosen task type&#39;s Transition fires on its <code>completed</code> settle. What the override does not change is the entry side. The ticket keeps the label that put it on the old state, the old state still matches first, and the machine re-derives the ticket&#39;s position where it started. The operator&#39;s skip is not durable: the next suggestion, and any auto-advance, points back at the step the operator just skipped, and an auto-handoff re-runs it.</p><p>The plane&#39;s write path so far is the settle-time fire, which owns the transition-written label set and removes nothing else. The entry labels are outside that set, so no fire can move the ticket between hand-applied positions, and the override has no effect the labels can see.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Placement.</strong> When a manual Handoff&#39;s final task type differs from the task the ticket&#39;s current position suggests, the control plane writes the ticket&#39;s labels before the agent starts: a <strong>Placement</strong>. The target is the first state, in machine order, that offers the chosen task and whose non-label conditions (source name, kind, repository) hold for the ticket. The write adds the target state&#39;s all and any labels and removes the placement labels the state does not name. After it runs, the ticket&#39;s derived position offers the chosen task, whatever writers ran before.</p><p><strong>The entry is derived from the match spec, not configured.</strong> ADR 0027 makes the labels the states and names no destination. The Placement extends the same principle to the handoff side: the match spec is the single source of a ticket&#39;s position, so the entry to a state is what the match says. No config key names the entry. A <strong>placement label</strong> is a label named in a state&#39;s all or any match set; the none set names exclusion, not ownership, and a label no state names - a priority or severity label, an operator label - is never touched by the write.</p><p><strong>The detection and the Placement share one computation.</strong> The Placement reuses the detection&#39;s ordered state walk and its match test. The post-write label set is the ticket&#39;s labels minus the placement labels, plus the target state&#39;s all and any labels, and the first state to match that set must offer the chosen task. When it does not, the Placement is infeasible: the task is offered by no state that matches the ticket&#39;s kind, a label the target state excludes stands on the ticket, two states claim one label, or no state matches the post-write set. The override&#39;s confirm refuses an infeasible Placement with a readable reason that names the cause, and the override panel&#39;s Task row carries that reason as a warning sentence, the way an unfit setting row does. The infeasible types stay selectable: the panel offers every configured task type, and the refusal is the fact.</p><p><strong>The write sits in the Handoff&#39;s external sequence.</strong> It runs after the durable claim and before the agent start, through the same command runner path the fire uses, as the source the item lists on - the membership the target state matches on, the newest one when several match. A failed write refuses the start with a readable reason: the ticket keeps its position, and the attempt records as a failed start. A start failure after a good write leaves the write standing: the operator&#39;s assertion holds, and the next handoff of the ticket converges at its settle. The write is idempotent: a label set that already matches the spec writes nothing, so a Restart that repeats the interrupted choices re-runs the rule and takes egress only when the labels still differ.</p><p><strong>The scope is the ticket, on the trigger the override makes.</strong> The write touches the ticket being handed off, never its fixing pull request: the pull request&#39;s position follows its own transitions. The trigger is the difference between the final task type and the ticket&#39;s current suggestion, on both panel origins: the open ticket, and the route edit from the Decision modal or the Live view. A Restart re-runs the same rule, with no special case. An auto-handoff, a workflow advance, and the default handoff of a parked ticket never differ from the suggestion, so they never fire. From a parked ticket, picking a task a state offers places the ticket into the machine; picking the default task type writes nothing.</p><p>The alternatives:</p><ul><li><strong>A configured entry list per state</strong> (an <code>entry</code> key beside the match). Rejected: it adds a second source of position next to the match spec, and the entry is already determined by the spec the detection reads. A config key the machine derives for free is drift waiting for an edit.</li><li><strong>The write at settle time, as an extension of the fire.</strong> Rejected: the fire&#39;s facts are the task&#39;s completion facts, and folding the entry move into them would make one task type&#39;s transition responsible for another task&#39;s entry, and for the position the ticket wore before this turn.</li><li><strong>Proceed on a failed write, report it as a fact.</strong> Rejected: the agent runs on the chosen task while the labels still name the old position, and a failed turn leaves the ticket where the machine re-suggests the skipped step. The operator&#39;s assertion is either made or not made.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The plane now holds two external write paths with two ownership sets: the fire owns the transition-written set at settle time (ADR 0027), the Placement owns the placement label set at handoff time. A gate label named in one state&#39;s all set alone is a placement label and travels with the state: a Placement strips it when the ticket leaves the state. A machine-wide gate, named in every state&#39;s all set, sits in every target&#39;s add set and is never stripped.</li><li>The operator&#39;s skip is durable. After the Placement, the position offers the chosen task, and the machine cannot re-suggest the skipped step. When the chosen task&#39;s Transition leaves the position where the Placement put it, the Same-type hold (ADR 0026) catches the repeat the way it already does.</li><li>A Placement failure refuses a start the operator asked for. The refusal is a readable fact on the Handoff, the ticket stays open on its old position, and a retry re-runs the write.</li><li>A ticket listed on more than one source takes one write, on the membership the target state matches; the other memberships converge on the next refresh, the same lag a fire&#39;s write carries.</li><li>The override panel&#39;s Task row can now carry a reason sentence for this ticket&#39;s infeasible choices, and a confirm can refuse on it. The row&#39;s options stay the full task type list, per ticket kind.</li></ul>',16)])])}const m=t(o,[["render",i]]);export{f as __pageData,m as default};
