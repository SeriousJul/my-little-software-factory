import{_ as t,o,c as n,a0 as a}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0031: A work cycle can close while the agent is still working","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0031-a-work-cycle-can-close-while-the-agent-is-still-working.md","filePath":"adr/0031-a-work-cycle-can-close-while-the-agent-is-still-working.md"}'),s={name:"adr/0031-a-work-cycle-can-close-while-the-agent-is-still-working.md"};function i(r,e,c,h,l,d){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="adr-0031-a-work-cycle-can-close-while-the-agent-is-still-working" tabindex="-1">ADR 0031: A work cycle can close while the agent is still working <a class="header-anchor" href="#adr-0031-a-work-cycle-can-close-while-the-agent-is-still-working" aria-label="Permalink to &quot;ADR 0031: A work cycle can close while the agent is still working&quot;">​</a></h1><p>Status: accepted Date: 2026-09-18</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A ticket&#39;s work cycle could only end from the Decision modal: the operator pressed Enter on an <code>awaiting</code> ticket, read the turn log, and picked the Close row. The state layer needs the settled turn&#39;s pending trace row to apply a <code>closed</code> decision, so a ticket whose agent is still working - <code>handed-off</code> or <code>running</code> - had no close at all. The operator&#39;s only routes to stop it were the emergency exit, or the manual work in herdr that the emergency exit warns about.</p><p>The two sections diverged on the shape of the action. A Consultation close was a base-mode key with a confirmation dialog while the agent was alive. A Ticket close was a modal row behind Enter, with no dialog of its own. The <code>w</code> key in the Ticket section belonged to the leftover clear, the least used control of the plane, which held the key this action wanted.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Key <code>w</code> closes the work cycle of the selected ticket, in both Ticket base modes.</strong> It is available on an in-flight ticket and on an <code>awaiting</code> ticket, and it refuses on an <code>open</code> one: there is no work in flight to close.</p><p><strong>The close asks for confirmation whenever it stops a live agent, which is every ticket state the close runs on.</strong> The dialog is the shared confirmation panel. Its first line states who is alive - the agent working, or the turn settled - and the rest states what survives: the worktree environment removes the checkout, a dirty checkout stays open as a leftover, the live-worktree environment closes the herdr tab and keeps the checkout and workspace, and the git branch stays in every case. The ticket returns to <code>open</code> with the cycle incremented.</p><p><strong>An in-flight close is its own state operation.</strong> The cycle ends, the ticket moves to <code>open</code>, and no completion trace is written: the turn never settled, so there is no cause, no turn log, and no message to record. The Close cleanup runs exactly as it runs for the decision&#39;s close - the same commands, the same seat, the same leftover recording when herdr refuses. The state line gains the moves <code>handed-off -&gt; open</code> and <code>running -&gt; open</code>.</p><p><strong>An <code>awaiting</code> close is the decision modal&#39;s close.</strong> It records the <code>closed</code> decision on the pending trace, then runs the same cleanup. The Decision modal keeps its Close row: it is the close with the turn log beside it. <code>w</code> is the direct route to the same action.</p><p><strong>A close that meets an in-flight handoff of the same ticket queues behind it.</strong> The seat of ADR 0012 already serializes environment changes behind handoffs. The queued close runs when the handoff settles, so a hung start still ends in the close the operator asked for, and no cleanup runs under a handoff that is still building its agent.</p><p>The considered alternatives:</p><ul><li>Keep the close in the modal and give it another key. Rejected: the operator must open the decision surface to stop the work, and the Ticket keeps diverging from the Consultation, whose close is one key deep.</li><li>Reuse <code>abandoned</code> for the in-flight close. Rejected: abandon is the recovery of a missing agent, and it writes its own trace row. A close over a live turn is an operator decision, and the record should read closed, not abandoned.</li><li>Write a completion trace row for the unsettled turn, with <code>aborted</code> as its cause. Rejected: the turn never settled, and a cause word asserts a fact about a turn that did not happen. The handoff row keeps the record of the work.</li><li>Refuse the close while the ticket&#39;s handoff is in flight. Rejected: a hung start would leave the operator with no way out but the emergency exit, and the seat already makes the ordered wait safe.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The operator stops an in-flight agent from one key and one confirmation. The emergency exit is no longer the only route, and the plane no longer needs the operator to reach for herdr mid-cycle.</li><li>The state line reads <code>handed-off/running -&gt; open</code> beside the settled path. The cycle number increments on the unsettled close, and the Handoff limit counts it like any other cycle end.</li><li>The re-verify gate and the Same-type hold (ADR 0026) read the newest cycle-end decision and its cause. A cycle that ends without a trace row holds nothing and re-verifies nothing, the way an abandon row without a cause does. The gates must treat the absent row and the cause-less row alike.</li><li>The <code>w</code> key leaves the leftover clear, which ADR 0032 removes.</li><li>The Action bar and the Key guide state the new key from the catalogue. Close shows while the ticket is in flight or awaiting, and Goto, ADR 0033, shows beside it while the agent&#39;s pane is alive.</li></ul>',15)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
