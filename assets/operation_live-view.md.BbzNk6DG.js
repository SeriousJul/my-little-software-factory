import{_ as t,o,c as n,a0 as a}from"./chunks/framework.ysVzM4be.js";const s="/my-little-software-factory/assets/live-view.DFsZdZtu.png",r="/my-little-software-factory/assets/turn-log.Dd8bXJOu.png",w=JSON.parse('{"title":"Live view","description":"Watch an agent that is running, and the decision the same screen carries when the turn settles.","frontmatter":{"title":"Live view","description":"Watch an agent that is running, and the decision the same screen carries when the turn settles."},"headers":[],"relativePath":"operation/live-view.md","filePath":"operation/live-view.md"}'),i={name:"operation/live-view.md"};function d(h,e,c,l,p,m){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="live-view" tabindex="-1">Live view <a class="header-anchor" href="#live-view" aria-label="Permalink to &quot;Live view&quot;">​</a></h1><p><img src="'+s+`" alt="The Live view on an in-flight ticket: the agent&#39;s terminal output in the
left box and its context above"></p><p>Enter on an in-flight ticket opens the Live view: a near-fullscreen screen that pops in over the app with the same fade and grow as the decision modal, one cell of margin on every side. Its border reads <code>Live: &lt;ticket title&gt;</code> in every state the screen carries, and the first row under the border names the context: repository, task type, agent, and <code>blocked</code> when the ticket wears the blocked marker.</p><p>The body is the agent&#39;s terminal output as plain text. The screen reads the ticket&#39;s agent pane on a one-second cadence and keeps the newest <code>completion-message-lines</code> of it. New output pins the stream to the bottom; a scroll releases the pin, and reaching the bottom by scrolling pins it again. A failed read keeps the last good lines under a dim stale note. A ticket without a recorded pane shows a note in place of the stream and reads nothing.</p><p>The action row under the stream is the Goto, and it is pure focus: it focuses the agent&#39;s pane and closes the screen. It changes no state and records no decision. The screen follows the ticket: when the turn settles and the factory leaves the decision to the operator, the same box turns into the decision, with the turn log and the choice rows; when the agent leaves herdr, it carries the missing modal; when the ticket leaves the in-flight and awaiting states, the screen closes. A settled turn the factory decides for itself never hands the screen over: the stream stands where the operator left it.</p><table tabindex="0"><thead><tr><th>Key</th><th>What it does</th></tr></thead><tbody><tr><td><code>j</code> / <code>k</code></td><td>Scroll the stream, one row down or up</td></tr><tr><td><code>PgUp</code> / <code>PgDn</code></td><td>Scroll the stream by a page</td></tr><tr><td><code>Home</code> / <code>End</code></td><td>Jump to the top or the bottom of the stream</td></tr><tr><td><code>Up</code> / <code>Down</code></td><td>Move between the choice rows, in the decision sub-mode</td></tr><tr><td><code>Enter</code></td><td>Focus the agent&#39;s pane and close the screen; choose the selected row, in the decision sub-mode</td></tr><tr><td><code>e</code></td><td>Edit the selected Handoff row&#39;s settings before it starts, in the decision sub-mode</td></tr><tr><td><code>Esc</code></td><td>Close the screen: nothing runs, the ticket stays where it is</td></tr></tbody></table><h2 id="the-turn-log" tabindex="-1">The turn log <a class="header-anchor" href="#the-turn-log" aria-label="Permalink to &quot;The turn log&quot;">​</a></h2><p><img src="`+r+`" alt="The same Live view after the turn settles: the turn log above the
decision rows"></p><p>When the turn settles and the factory leaves the decision to the operator, the stream in the left box is replaced by the turn log of the settled turn, in the same dressing the <a href="./modals.html">decision modal</a> renders: the agent&#39;s text blocks, the tool calls as dim notes, and the choice rows under them. The choice rows and their keys are the modal&#39;s: Close, Goto, and one Handoff row per outgoing workflow edge, with <code>e</code> on a Handoff row editing that one handoff before it starts. The trace rules are the modal&#39;s rules, and the <a href="./../work-flow/completion.html">completion guide</a> records what a decision does to the ticket after.</p>`,9)])])}const f=t(i,[["render",d]]);export{w as __pageData,f as default};
