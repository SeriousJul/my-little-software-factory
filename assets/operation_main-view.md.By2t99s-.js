import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const s="/my-little-software-factory/assets/main-view.B-RsBR63.png",f=JSON.parse(`{"title":"Main view","description":"The Main view's three sections, its counts, its controls, and the layout the terminal shows.","frontmatter":{"title":"Main view","description":"The Main view's three sections, its counts, its controls, and the layout the terminal shows."},"headers":[],"relativePath":"operation/main-view.md","filePath":"operation/main-view.md"}`),i={name:"operation/main-view.md"};function h(r,e,d,c,l,p){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="main-view" tabindex="-1">Main view <a class="header-anchor" href="#main-view" aria-label="Permalink to &quot;Main view&quot;">​</a></h1><p><img src="'+s+`" alt="The Main view: the Ticket, Consultation, and Work sections on the left,
the detail of the selected ticket on the right"></p><p>The Main view holds three list sections on the left, the Ticket section on top, the Consultation section below it, and the Work queue section below that, and one context-dependent detail pane on the right that shows the detail of the item the cursor holds. One control catalogue, one Action bar, and one Message line answer for all three. All three sections start expanded; <code>x</code> or a click on a section header collapses the section under the cursor to its header row, and the same toggle restores it. Up and down move the cursor through the visible rows and cross to the next section the terminal shows, so a collapsed section in between does not stop the walk. The mode the bar and the guide state derives from the section that holds the cursor and its focused pane.</p><p>The Ticket header always shows the pipeline counts - open, running, and awaiting - with the held count appended only when it is non-zero. The Consultation header carries its attention facts (awaiting response, recovery). The Work queue header carries the queue&#39;s depth, the items waiting for a free Parallel limit seat. A manual handoff that cannot take a seat enters that queue instead of starting, and the queue holds at most one item per ticket: a handoff of a ticket that already waits is refused, the first item keeps its place, and the Message line says so. The counts are computed from the in-memory projection on each render; none queries the state.</p><p>The control plane keeps a contextual Action bar in the last row of the terminal. It shows the controls the current interaction mode can run, dims one it will not run in the present state, and names the reason on the Message line when the operator presses it anyway.</p><p>The in-app Key guide lists the controls of the modes the app dispatches from its catalogue. Press <code>?</code> or <code>F1</code> to open it from anywhere, including the panes and the modals. It carries the Ticket list, the Ticket detail, the Consultation list, the Consultation detail, the Work queue list, the Work queue item detail, the Agent terminal, the response editor, the override panel in both of its row kinds, the decision modal, the missing modal, the guide and the Message view, the controls that are only reachable from another mode, Quit, and the <code>Ctrl+C</code> emergency exit, each with what it does and, where the app will not run it, why. Press <code>Esc</code>, <code>F1</code>, or <code>?</code> to close it.</p><p>This guide does not repeat that list. A table of keys here went stale twice: the guide and the Action bar are generated from one control catalogue (<code>src/components/controls.ts</code>), so what the app shows is what the app runs.</p><p>The Ticket list and detail move with the row, page and jump keys, focus the detail with <code>l</code> or <code>Right</code> and the list with <code>h</code> or <code>Left</code>, hand an open ticket off with <code>Enter</code>, open the decision modal on an awaiting one, the missing modal on a ticket whose agent is gone, and the override panel with <code>e</code>. The Work queue&#39;s list and its item detail answer the same row, page, jump, and focus keys. On a queue row, <code>u</code> and <code>d</code> move that item up or down the shared order, and <code>Del</code> takes it out of the queue: a removed Handoff item is cancelled, and the ticket it asked for keeps its state. <code>=</code>, <code>+</code>, and <code>-</code> bump a ticket&#39;s priority up and down through the configured rank, and Backspace clears it to the label rank or unranked. <code>a</code> toggles auto-handoff, <code>r</code> refreshes, <code>g</code> goes to the agent&#39;s pane, <code>w</code> closes the work cycle of the selected ticket behind a confirmation (ADR 0031), and <code>q</code> quits.</p><p>When the Message line is truncated, press <code>m</code> in a base pane or <code>F2</code> in any mode to read the captured message in the Message view. The Message line and the Action bar reserve the two bottom rows at every terminal size. Each list section reserves its header row at every size, and its box reserves three content rows while the terminal can pay for them; a section that holds no rows reserves one content row instead. When the body cannot pay the claims, the sections below the cursor give up their rows in turn, from the bottom of the column up, and a section that cannot hold even its one content row collapses to its header rather than vanishing, so all three headers keep their counts. The section under the cursor never gives way: it holds its three content rows, and a click on a collapsed header moves the cursor there, which brings its box back and hands the rows another section gives up. Below the smallest useful frame (40 columns by 19 rows) the panes give way to a size message and a compact Help control, and a surface that cannot draw its own rows says so instead of painting them over its border. One hint holds the row&#39;s end cells: Help on a bar that can open the Key guide, and the overlay&#39;s own Close on a utility overlay. A frame too narrow for that hint states one of its whole keys, so the way out of a screen is named at any width and never cut in half.</p><p>The keys the override panel answers with live on the <a href="./consultation.html">Consultation page</a>, under Entry controls.</p><h2 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to &quot;Layout&quot;">​</a></h2><p>The Main view is one surface with three list sections, the Ticket section on top, the Consultation section below it, and the Work queue section below that, and one context-dependent detail pane on the right (ADR 0019, extended by ADR 0034). All three sections start expanded, and the detail pane shows the detail of whichever item the cursor holds: the ticket detail on a ticket, the Consultation detail on a Consultation, and the captured facts of the queued start on a Work queue item. <code>x</code> or a click on a header toggles the section under the cursor: it shrinks to its header row and its rows leave the navigation flow, and the same toggle restores it. A collapsed section keeps its list selection, and the selection and detail of a collapsed section survive the collapse, so a re-expand shows the same place. The rows run: the mode line (while the control plane has state to observe), the Ticket header across the full terminal width, the three sections&#39; list panes stacked on the left with the Consultation and Work headers between them, the detail pane on the right, the Message line, and the Action bar. The focused section takes the remaining rows after the other sections claim their minimum of three content rows - one content row while their list is empty - so the list the operator works in gets the room, and a section the body cannot pay for at all shows its header alone. The Ticket header always shows the pipeline counts - open, running, and awaiting, in the labelled form on a terminal of at least 60 columns and the short form below - and appends the held count with its bell marker only when it is non-zero. The Consultation header carries that section&#39;s attention facts, its awaiting-response and recovery counts, the bell marker while the bell rings, and &quot;new output&quot; while that fact holds, so a Consultation that needs the operator is visible whether the section is expanded or collapsed and no free-standing attention line exists. A section that cannot hold its minimum collapses rather than vanishing, so all three headers keep their counts; below the smallest useful frame the compact frame drops the panes with a size message.</p><p>Two panes side by side, flex-sized to the terminal. The list pane on the left shows the tickets of the Ticket section with their state badge, task type badge, title, and repository. The task type badge is the type the control plane would hand off: an open ticket shows its suggested task type, every other ticket shows the task type its recorded handoff started with. A non-open ticket without a recorded handoff shows <code>[unknown]</code>, and only that badge wears a warning color; every configured task type uses one neutral style. The detail pane on the right shows the full detail of the selected ticket: repository, ticket state, the Agent its handoff runs on with its Environment and that handoff&#39;s Model, Thinking level, and context window, source name, source kind, external key, source state, URL, labels, and source health. An open ticket shows the settings its suggested task type&#39;s Task profile resolves to, which is what Enter starts, including the Environment it starts in; a ticket inside a work cycle shows the settings its own handoff started with. A close returns a ticket to open and keeps that handoff&#39;s record as history, so the rows follow the ticket&#39;s state rather than whichever record survives. A setting left to the agent reads <code>left to agent</code> in the dim color. The detail carries one explicit task type line for every ticket: <code>Suggested task type:</code> for an open ticket, <code>Handoff task type:</code> for every other, with <code>Handoff task type: unknown</code> when the handoff data is absent. The detail also carries the ticket&#39;s handoff count against its per-ticket limit, counting the handoffs of every work cycle the ticket ran, and, when one exists, the last completion: its date, the task type, the agent, and the recorded decision. Factory ticket state and external source state stay separate. The Work queue&#39;s rows carry the origin the pickup re-checks and the ticket the item asks for; its detail carries the ticket, that origin, the time the item entered the queue in UTC, and the choice the operator made at the handoff - the agent, environment, task type, model, thinking level, and context window - with a setting left to the agent saying so in the same words the override panel uses. A row the store damaged keeps its place and states its damage in place of the choice it cannot read, and the observation cycle refuses to start it with that reason. An empty queue states that fact on its one line. The panes share one focus. Switching focus never moves the selection.</p><p>The vertical keys act on the focused pane. With the list focused, they move the selection and cross the section boundary to the next section the terminal shows: from the last row of the Ticket list the next down lands on the Consultation list, from its last row the next down lands on the Work queue list, and a step back up from either first row lands on the section above. A section the operator collapsed, or that the body could not pay for, is stepped over rather than stopped at, so the cursor reaches every section the frame holds. Page keys move by one visible list page, and Home and End select the list edges. With the detail focused, the row keys move at the configured speed, PageUp and PageDown retain one row of context, and Home and End move to the detail edges. Every base detail, the Work queue item&#39;s facts included, owns that scroll, so a long ticket identity that wraps past the pane stays readable. A new selection starts the detail at the top.</p><p>The detail is a native OpenTUI viewport. Its complete content stays mounted, so wheel bursts translate one stable surface instead of rebuilding visible rows. When the content overflows, its right inner column has a proportional scrollbar. The gutter is always reserved when width permits, so wrapped text does not reflow as the bar appears. Click or drag the scrollbar, or use the wheel or trackpad over any part of the detail. Fast vertical wheel events accelerate to the configured limit. Horizontal and Shift-wheel input is ignored. A click or wheel action focuses its pane. Clicking a visible row in either list selects it, a list wheel event selects one adjacent row, and a click on a section header toggles that section.</p><p>Mouse reporting takes the host terminal&#39;s native text selection with it, so the control plane gives it back as Auto copy: drag with the mouse over any surface and the text highlights, and releasing the drag copies the selection to the system clipboard. A click that does not drag copies nothing, a copy the terminal refuses warns on the Message line, and a copy that takes is silent. Auto copy is always on, over every surface, with no setting. The keyboard Copy control on a field selection keeps its own path.</p><p>When the terminal is too narrow for a field, the field drops out of the row instead of wrapping it. The repository drops before the title does, and the task type badge is complete or absent: the row keeps the whole badge with a readable title, or the badge drops and the title takes the cells. A partial badge could read as another task type, so it never truncates; the full value stays in the detail pane.</p><p>Above the Action bar sits the Message line. It carries the progress and the outcome of the last handoff: <code>Working: handing off &quot;...&quot;...</code> while one is in flight, <code>Warning:</code> for a sibling clone or other recoverable issue, and <code>Error:</code> for a failed handoff. A clean handoff clears its Working line, and a warning the operator drew while it ran - a refused key or clear - then takes the line. While a handoff is in flight keys keep working, and <code>e</code> is refused with a warning. A second handoff claim records its attempt, which blocks a further claim on the same ticket, and queues its external work until the in-flight handoff settles; the ticket moves to <code>handed-off</code> only when the handoff settles and its agent starts, so claims never race each other. The Close cleanup of any path that runs one holds that same seat: an environment change and a handoff never work beside each other.</p><p>Above the panes sits a mode line. It shows the auto-handoff state and the Parallel limit seat count against the parallel limit: <code>auto: on 2/2</code>, or <code>auto: off 1</code> when no limit is set. The count is the one shared seat-count source the automatic start gates read: the in-flight tickets whose agent was alive in the latest herdr poll, or is still inside its startup grace, the handoffs still in progress, and every Consultation in <code>opening</code> or <code>working</code>. The gates and the mode line read the same source, so the two never disagree. The <code>a</code> key in the Ticket section toggles the mode and writes it to the state file at once, so a restart or a dev reload finds the mode where the operator left it; a state file the plane has just created starts with the mode off (ADR 0036). A write the state file refuses reports on the Message line, and the flip stands for the session.</p><p>A ticket in the Starting window wears the window&#39;s face in the state badge slot: the Handoff is claimed and not yet settled, or the ticket is <code>handed-off</code>. The face is an animated spinner with the written word <code>starting</code>, and it stands in the list row and the detail pane&#39;s state line in place of the state badge. The <code>[handed-off]</code> badge is never drawn: the face wears the badge&#39;s slot from the keypress, and the row&#39;s timeline is <code>[open]</code>, face, <code>[running]</code> - one face on the way in, one flip when the work is observed. The face ends when the observation moves the ticket to <code>running</code>, when a settle moves it to <code>awaiting</code> or held, or when a start fails and returns the row to its state with the error line. Every origin wears the same face: a manual hand-off, a workflow route, a restart, and an auto hand-off. The face is a shared control with a gallery example, and the animated glyph is not what the frame snapshots verify: the checks run on the written word beside any glyph of the face.</p><p>A blocked agent replaces the state badge in the list row with a <code>blocked</code> badge: the agent shows an approval or question UI and waits for a human. The ticket stays in flight, in <code>handed-off</code> or <code>running</code>, and still counts against the parallel limit. A missing agent replaces the state badge with a <code>missing</code> badge: the stored pane is gone or holds no agent, so the work stops there until the operator restarts or abandons the cycle. Both badges clear when the next poll no longer shows the condition. The first poll has not landed yet, so no badge appears before it. The failure markers beat the Starting window&#39;s face the way they beat the state badge: a dead or blocked agent is never hidden behind a spinner, and a crash remnant shows its recovery fact, not the spinner. A <code>handed-off</code> ticket under a marker wears that marker&#39;s own word in the detail&#39;s state line too: the <code>[handed-off]</code> badge is drawn by no surface, and the row and the detail never disagree. A ticket that has used up its per-ticket handoff limit wears a trailing <code>handoff limit</code> marker at the end of the row, and the detail pane shows the count as <code>Handoffs: 2/2</code>. Auto-handoff leaves such a ticket open; a manual handoff may still pass the limit. A ticket whose previous herdr environment is still alive wears a trailing <code>leftover</code> marker, and its detail pane names the workspace, tab, and pane that remain, the reason the control plane knows, and since when. The detail states that the cleanup runs in herdr, not in the control plane (ADR 0032).</p>`,21)])])}const u=t(i,[["render",h]]);export{f as __pageData,u as default};
