import{_ as t,o,c as n,a0 as s}from"./chunks/framework.D4fUSYzX.js";const a="/my-little-software-factory/assets/decision-modal.B-rV7t0M.png",u=JSON.parse('{"title":"Modals","description":"The decision modal, the Ticket close confirmation, the missing modal, and the leftover environment fact.","frontmatter":{"title":"Modals","description":"The decision modal, the Ticket close confirmation, the missing modal, and the leftover environment fact."},"headers":[],"relativePath":"operation/modals.md","filePath":"operation/modals.md"}'),r={name:"operation/modals.md"};function i(h,e,d,c,l,w){return o(),n("div",null,[...e[0]||(e[0]=[s('<h1 id="modals" tabindex="-1">Modals <a class="header-anchor" href="#modals" aria-label="Permalink to &quot;Modals&quot;">​</a></h1><h2 id="decision-modal" tabindex="-1">Decision modal <a class="header-anchor" href="#decision-modal" aria-label="Permalink to &quot;Decision modal&quot;">​</a></h2><p><img src="'+a+`" alt="The decision modal on an awaiting ticket: the turn log of the settled
turn above the choice rows"></p><p>Enter on an <code>awaiting</code> ticket opens the decision modal: a near-fullscreen modal that pops in over the app with a short fade and grow, one cell of margin on every side. In auto mode, and on a transition that auto-advances, the factory decides the ticket itself: Enter only reports that, and the modal stays closed. Its border reads <code>Decision: &lt;ticket title&gt;</code>, and the first row under the border names the context: repository, task type, agent, completion time.</p><p>The body is the turn log of the settled turn, in its own pane titled <code>Turn log</code>. The log holds the agent&#39;s messages in order. The agent&#39;s text blocks carry light markdown dressing: headings render bright without their hashes, bold renders bright, code renders dim, lists keep their markers and indent per level, links keep their label. Each tool call is one dim note, <code>▸ name: target</code>; a failed call wears the warning color. The agent&#39;s thinking text is not shown. The log comes from the agent&#39;s session record (ADR 0008); when no session is known, the terminal capture stands in. The pane opens at the bottom, where the agent&#39;s conclusion is, and a proportional scrollbar shows the position when the log is longer than the window. A log the trace does not record shows its reason as one dim row in the pane, and the pane keeps its border and title.</p><p>Below the pane, the choice rows stand in their region, pinned to the box&#39;s floor. The region is bounded: it shows as many rows as the box has room for, and when it holds more rows than that it scrolls, with the range of the rows it shows riding the Action bar behind the selection&#39;s hint. A held turn stands one warning row between the pane and its rows: the cause the turn ended on, and the agent&#39;s text for it.</p><p>The rows pay in a stated order: the log keeps a floor of three rows, the pane yields its padding before its border, and only then does the log yield rows to the region. A box that cannot hold the floor, the pane&#39;s border, and the region&#39;s one row draws the size message instead of a clipped pane, and the modal&#39;s own bar keeps the way out.</p><p>The modal offers the choices the state allows. <code>Up</code> and <code>Down</code> move between the choice rows; <code>j</code> and <code>k</code> scroll the body one row, with the page and jump keys as aliases. <code>Enter</code> chooses the selected row, <code>e</code> edits the selected Handoff row&#39;s settings before it starts, and <code>Esc</code> closes the modal: nothing runs, and the ticket stays awaiting. The shared Action bar at the terminal bottom names these controls. The first row, &quot;Close&quot;, ends the work cycle: the ticket returns to open with its cycle number incremented, and the handoff&#39;s environment is closed without touching the git branch, so pushed work and pull requests survive: a worktree handoff loses its worktree checkout and its herdr workspace, a live worktree handoff loses its tab. The second row, &quot;Goto&quot;, focuses the agent&#39;s pane so the operator can steer it; the ticket stays where it is, <code>awaiting</code> until the poll moves it or the operator decides. The confirmation on the Message line names the workspace the pane lives in, since herdr 0.9 keeps each client&#39;s own view and a CLI focus no longer moves the operator&#39;s view (the Live view page carries the full note). Goto is navigation, not a completion decision, so the trace does not record it: the turn&#39;s pending trace stays pending, and the next settle refreshes it with the agent&#39;s new last message. The settled turn&#39;s transition facts stand above those rows (ADR 0027): one line per surface the fire wrote on, the ticket&#39;s reading <code>ticket · added ready-for-review · removed ready-for-agent</code> and the pull request&#39;s the same shape behind its external key; one line for a label write that failed; and one for a fire whose judgment never held or that found no linked pull request. The rows decide on those facts, so the turn log yields its rows to them. Then one &quot;Handoff: <code>&lt;task type&gt;</code>&quot; row when the fire&#39;s written labels put a ticket in a state that offers a task. The row stands only while the ticket the position sits on still lists in a source: a refresh that finds the ticket gone - a merged or closed pull request, a closed issue - withdraws the row, and a fact line takes its place: the position&#39;s ticket left its source, and no handoff stands (ADR 0048). The Close and the Goto keep. The row&#39;s detail shows the Agent the arriving handoff resolves to (the fired branch&#39;s pin, else the target Task profile&#39;s agent, else <code>default-agent</code>), and the branch&#39;s pinned environment when it defines one. Choosing a row hands that ticket off - the linked pull request when the written labels sit there, else the ticket whose turn settled. <code>e</code> on such a row opens the override panel on the choice the position resolved, so the operator can change the agent, environment, Model, or Thinking for this one handoff before it starts; the override outranks the Transition pin, the Task profile, and the defaults. Moving that panel to another Task type re-derives the rows the operator never touched from that type&#39;s own profile, so the route&#39;s Agent pin goes with the position the fire named. <code>Esc</code> there closes the panel back to the decision: nothing is claimed and nothing runs. Enter on an awaiting ticket keeps the direct route, so a route the operator wants unchanged stays one press. The row&#39;s &quot;handed-off&quot; decision lands on the turn&#39;s trace only when the routed handoff settles with the agent started; a failed route leaves the trace pending. Close and Goto keep working on the awaiting ticket either way: a close of a turn the route decided ends the cycle and leaves the recorded decision standing (ADR 0005, amendment), so the routed issue&#39;s work cycle ends with key <code>w</code> and its environment with the Close cleanup. The automatic route decides the same way: its <code>auto-handed-off</code> record waits for the same start. Then a &quot;Re-fire&quot; row when the turn&#39;s transition did not complete the machine&#39;s work: no branch held, or the label write failed (ADR 0054). A complete outcome shows none. Confirming it reads the source as it stands now - the pull request sources refresh first - and fires the turn&#39;s task type transition again, the same fire the settle ran. The re-fired outcome takes the recorded one&#39;s place on the trace, and the rows re-render on it: the fact lines show what the fire wrote, the row stands down when the outcome completes, and a &quot;Handoff&quot; row appears when the re-fire derived a position. The turn stays <code>awaiting</code> with no decision change: no cycle ends and nothing hands off. The working word and the landing ride the Message line.</p><h2 id="missing-modal" tabindex="-1">Missing modal <a class="header-anchor" href="#missing-modal" aria-label="Permalink to &quot;Missing modal&quot;">​</a></h2><p>Enter on a ticket whose agent is missing opens the missing modal. It shows the badge fact and the handoff count, and it offers &quot;Restart&quot; and &quot;Abandon&quot;. <code>Up</code> and <code>Down</code> move between the choice rows, <code>j</code> and <code>k</code> scroll its message, <code>Enter</code> chooses the selected row, and <code>Esc</code> closes the modal: nothing runs, and the badge stays. The shared Action bar at the terminal bottom names these controls. &quot;Restart&quot; hands the ticket off again with the same choices, in the workspace the handoff recorded, and the last completion&#39;s message as the previous message. &quot;Abandon&quot; ends the work cycle: the ticket returns to open with its cycle number incremented, the handoff&#39;s environment is closed, and the missing badge clears.</p><h2 id="ticket-close" tabindex="-1">Ticket close <a class="header-anchor" href="#ticket-close" aria-label="Permalink to &quot;Ticket close&quot;">​</a></h2><p>Key <code>w</code> in either Ticket pane ends the work cycle of the selected ticket (ADR 0031). The same key closes the selected Consultation in the Consultation section (ADR 0037): a section owns its per-mode keys, so <code>w</code> always means the Close of the section the cursor is in. On an <code>open</code> ticket it refuses with its reason: no work is in flight to close. On an in-flight ticket, and on an <code>awaiting</code> one, it opens the shared confirmation panel first, because every one of those states has work behind it to stop.</p><p>The panel&#39;s first line names who is alive: the Agent working, the Agent that has started but is not seen yet, the pane herdr no longer lists, the Agent waiting for input, or the turn that settled with nothing working. The rest states what survives, read off the environment that ticket&#39;s own handoff runs in: a worktree close removes the checkout and the herdr workspace behind it, and a checkout herdr refuses to remove stays open as a leftover; a live-worktree close closes the Agent&#39;s tab and keeps the checkout, the workspace, and the tabs beside them. The git branch stays in every case, so pushed work and pull requests survive, and the ticket returns to open with its next cycle number. <code>Esc</code>, or the &quot;Cancel&quot; row, leaves the ticket and its work exactly as they were, and that row states the same fact about the pane the body&#39;s first line states.</p><p>The confirmed answer runs one of two closes:</p><ul><li>On an <code>awaiting</code> ticket, the &quot;Close&quot; row records the <code>closed</code> decision on the settled turn&#39;s trace and runs the Close cleanup: the same action the Decision modal&#39;s Close row offers, one key deep. The modal keeps its row, because it is the close with the turn log beside it.</li><li>On an in-flight ticket, the cycle ends with no completion trace at all. The turn never settled, so there is no cause, no turn log, and no message to record, and the handoff row stays the record of the work. The ticket returns to open with its cycle incremented, the Agent stops through the same Close cleanup, and the ended cycle counts toward the Handoff limit like any other. Because no row ends that cycle, the re-verify gate and the Same-type hold read it as holding nothing, the way they read an abandon without a cause: no finished turn is asserted, so nothing waits and nothing repeats.</li></ul><p>A close that meets a Handoff still building its agent does not refuse and does not race it: the whole close takes the shared environment seat, and runs when that Handoff settles, so a hung start still ends in the close the operator asked for and no cleanup tears down an environment herdr is mid-way through building.</p><h2 id="leftover-environment" tabindex="-1">Leftover environment <a class="header-anchor" href="#leftover-environment" aria-label="Permalink to &quot;Leftover environment&quot;">​</a></h2><p>The control plane offers no clear for a leftover environment (ADR 0032). The leftover stays a durable, visible fact - the <code>leftover</code> marker in the row, and the detail block that names the workspace, tab, and pane that remain, the reason the control plane knows, and since when. The block states that the cleanup runs in herdr, not in the control plane, the way the Consultation detail does for its remaining resources, so the operator knows where the environment still lives and goes there to remove it.</p><p>Every close still runs the Close cleanup: a removal that herdr refuses records the leftover with its reason, and the close stands. A handoff beside its ticket&#39;s leftover still starts under the cycle name (ADR 0012): the leftover&#39;s agent holding the stable name is the case the fact makes visible, not a failure of the start.</p>`,19)])])}const p=t(r,[["render",i]]);export{u as __pageData,p as default};
