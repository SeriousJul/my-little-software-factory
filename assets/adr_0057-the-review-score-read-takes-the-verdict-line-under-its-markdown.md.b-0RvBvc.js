import{_ as t,o as r,c as a,a0 as o}from"./chunks/framework.D4fUSYzX.js";const p=JSON.parse('{"title":"ADR 0057: The review score read takes the verdict line under its markdown","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0057-the-review-score-read-takes-the-verdict-line-under-its-markdown.md","filePath":"adr/0057-the-review-score-read-takes-the-verdict-line-under-its-markdown.md"}'),s={name:"adr/0057-the-review-score-read-takes-the-verdict-line-under-its-markdown.md"};function n(i,e,d,h,c,l){return r(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="adr-0057-the-review-score-read-takes-the-verdict-line-under-its-markdown" tabindex="-1">ADR 0057: The review score read takes the verdict line under its markdown <a class="header-anchor" href="#adr-0057-the-review-score-read-takes-the-verdict-line-under-its-markdown" aria-label="Permalink to &quot;ADR 0057: The review score read takes the verdict line under its markdown&quot;">​</a></h1><p>Status: accepted Date: 2026-09-24</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0053 made the score judgment decide on the newest record of the pull request&#39;s two verdict timelines that carries the template&#39;s fixed score line. The read took the line&#39;s words and one spelling of them: <code>\\*\\*Score:\\*\\*</code>, the bold the seed template writes in its Output Format.</p><p>A review agent posts that line under the markdown its own hand picks. Two real review turns on <code>SeriousJul/my-little-software-factory</code> pull request #157 posted their verdict as a heading, and the read took nothing from either: the review of 2026-09-23T19:58:07Z wrote <code>## Score: 83 / 100</code>, and the review of 2026-09-23T22:07:40Z wrote <code># Score: 85 / 100</code>. Both settles recorded the no-fire &quot;the pull request carries no review score&quot;, neither score branch held, no label was written, and the pull request rested in the turn&#39;s awaiting state for the operator&#39;s hand. The third review on the same pull request posted <code>- **Score:** 95 / 100</code>, the one spelling the read knew, and the machine routed it. A survey of the verdicts posted on the three repositories the development machine works found the fixed line under a heading six times, and every one of them read as no score.</p><p>The same survey showed the line worn other ways the read also misses: the colon inside the bold rather than outside it (<code>**Score: 85 / 100**</code>), the number bolded and the label plain (<code>Score: **88/100**</code>), the line as a table cell (<code>| **Score:** 84 / 100 |</code>), and the qualified label (<code>**Review score:** 90 / 100</code>).</p><p>The read had one gap of its own beside the pattern. Each verdict timeline is one page: <code>?per_page=100</code>, no walk. GitHub answers a comment list and a review list oldest first, so on a pull request with more than a hundred comments or reviews the plane reads the first hundred records and never sees the verdict the thread posted last: the newest-record rule decides on a list that cannot hold the newest record. The security feeds read their lists with <code>gh api --paginate</code> for exactly this reason, and the verdict reads never got the walk.</p><p>The line&#39;s words are still the contract, and the reason is ADR 0053&#39;s: a number in loose prose is not the agent&#39;s verdict. The survey&#39;s own counter-examples are the guard. <code>The mutation score: 79.48 % across the slice.</code> names its number after a word, and <code>Score: 92 out of 100.</code> writes its scale in words instead of in the line&#39;s own form. Neither is a posted verdict line, and the read takes neither.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The score read takes the verdict line for its words, not for its decoration.</strong> The line is the score label, its separator, and its number. The read takes the markdown a post wears in front of the line (a heading marker, a list bullet, a quote marker, a table cell&#39;s bar) and inside it (the emphasis runs around the label, around the separator, and around the number) as decoration, and then reads the label, its separator (<code>:</code>, <code>=</code>, or the bar of the cell that holds it), and the number. The scale is <code>%</code> or <code>/ 100</code> in either spacing, and a line that names another total scales to the 100 the threshold stands on, so <code>Score: 18 / 20</code> is 90.</p><p><strong>The label still decides what is a score line.</strong> The line carries its label at the line&#39;s start, or after a lead-in that ends in a mark, and the number carries the line&#39;s own scale or nothing after it but the line&#39;s punctuation. A score named in prose reports no score, exactly as before. The newest-record rule across both timelines, the last-occurrence rule within one body, and the 0 to 100 range guard are unchanged.</p><p><strong>Each verdict timeline is read whole.</strong> The comment read and the review read each walk every page of its list, and the judgment sorts the records the two walks collected. The reads stay two, gated the way ADR 0053 gates them, and each still fails open on its own timeline.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A verdict posted as a heading, a table cell, or with its colon inside the bold decides the branch: the parked no-fire on a decoration-posted score is gone, and the turn that posted it routes the pull request the way the template&#39;s own bold does. The two #157 turns are the measured case.</li><li>On a pull request with more than 100 comments or reviews, the newest verdict stands in the list the judgment sorts. A long thread costs the walk its pages, and no fire pays for a timeline it does not read.</li><li>The shipped template is unchanged and stays the ask: the plane reads the line wherever an agent puts its decoration, and an install&#39;s own review prompt never changes under this decision.</li><li>The read still takes one verdict line per record, the last one in the body, and it still ignores a scale written in words. A review that reports its score only as prose reports nothing, and the fire records the no-fire the way ADR 0053&#39;s reason does.</li><li>A score the read cannot find routes nothing: the fire records &quot;the pull request carries no review score&quot;, and the Decision region&#39;s re-fire row stands on that outcome (ADR 0054).</li></ul>',14)])])}const w=t(s,[["render",n]]);export{p as __pageData,w as default};
