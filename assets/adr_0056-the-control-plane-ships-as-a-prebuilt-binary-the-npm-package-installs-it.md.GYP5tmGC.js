import{_ as t,o as a,c as n,a0 as i}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0056: The control plane ships as a prebuilt binary the npm package installs","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0056-the-control-plane-ships-as-a-prebuilt-binary-the-npm-package-installs-it.md","filePath":"adr/0056-the-control-plane-ships-as-a-prebuilt-binary-the-npm-package-installs-it.md"}'),s={name:"adr/0056-the-control-plane-ships-as-a-prebuilt-binary-the-npm-package-installs-it.md"};function o(r,e,h,l,d,c){return a(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0056-the-control-plane-ships-as-a-prebuilt-binary-the-npm-package-installs" tabindex="-1">ADR 0056: The control plane ships as a prebuilt binary the npm package installs <a class="header-anchor" href="#adr-0056-the-control-plane-ships-as-a-prebuilt-binary-the-npm-package-installs" aria-label="Permalink to &quot;ADR 0056: The control plane ships as a prebuilt binary the npm package installs&quot;">​</a></h1><p>Status: accepted Date: 2026-09-22</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0035 swapped the runtime to Bun and left the distribution on npm: the package carries the app&#39;s source, and <code>npx mlsf</code> bootstraps on Node and re-execs the app under a Bun found on the <code>PATH</code>. The <code>npx</code> path therefore needs both runtimes installed before the control plane can start, on a machine that only wants to run it.</p><p>Bun compiles a TypeScript entry to a standalone executable (<code>bun build --compile</code>): the output embeds the Bun runtime, the app, and the files the build imports, and runs with neither Node nor Bun on the machine. The control plane&#39;s one machine-local read - the shipped Default configuration - is made to reach the copy the build embeds, and the OpenTUI native core the target runs is the file the compile embeds on its own. OpenCode, the production OpenTUI application, distributes this way.</p><p>The release already exists (ADR 0020): a <code>v*</code> tag runs the checks and publishes both packages to npm through trusted publishing. This ADR adds to that release the binaries the package installs, and changes what the package carries.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The release builds one prebuilt binary per supported target</strong> and uploads them to the GitHub Release beside the checksums file: <code>linux-x64</code>, <code>linux-x64-musl</code>, <code>linux-arm64</code>, <code>linux-arm64-musl</code>, <code>darwin-x64</code>, <code>darwin-arm64</code>, and <code>windows-x64</code>. Each leg compiles on a clean runner with only its own operating system&#39;s and architecture&#39;s OpenTUI cores installed. That is one core for <code>darwin-x64</code>, <code>darwin-arm64</code>, and <code>windows-x64</code> and two for each Linux target. <code>@opentui/core</code>&#39;s loader names eight platform packages in literal dynamic imports, and the bundler prunes the <code>process.platform</code> and <code>process.arch</code> branches to the <code>--target</code> it is given, so a leg needs only the cores its target can reach. What no target settles is the libc test inside the Linux branches, <code>process.env.OPENTUI_LIBC === &quot;musl&quot;</code>: an environment read is not knowable at build time, so both siblings of the target&#39;s architecture must resolve. A Linux binary therefore carries both libc variants, about 6.3 MB of native library the machine it runs on never loads. The measured core count and byte size per target are in the verification record. The build stamps the package&#39;s version into the binary, where the new <code>factory --version</code> flag reads it; a source run reads the version from the repository&#39;s package.json instead.</p><p><strong>The npm package complements the binary; the package is the installer.</strong> The npm package no longer carries the app&#39;s source. It carries the installer bin, which runs on Node - the runtime <code>npx</code> already provides - resolves the machine&#39;s target, downloads this version&#39;s binary from the GitHub Release of the same tag, verifies its SHA-256 against the release&#39;s checksums file, and caches it under the data home (<code>~/.local/share/my-little-software-factory</code>, or <code>%LOCALAPPDATA%</code> on Windows) in a directory per target, beside an install note that names the version, the target, and the digest it was verified against. A second run finds the cached binary, checks its bytes against that note, and skips the network. An empty or relative data home from the environment is no data home: the run ignores it the way the Config paths do, and a cache never lands in the working directory, where a planted binary could arrive with a note that agrees with it. <code>npx mlsf</code> and <code>npx my-little-software-factory</code> stay the one command the operator types; a machine with no Bun and no Node of its own runs the control plane once the binary is installed, and a machine that keeps the binary cached needs neither.</p><p><strong>The installed binary is the package&#39;s only runtime content.</strong> The app&#39;s libraries - the OpenTUI core and React, the TOML reader, the width reader - are development dependencies of the repository: the build embeds them in the binary, and an operator who installs the package installs the installer alone and nothing else.</p><p><strong>The checksums are verified on every install; the binaries are not signed yet.</strong> The checksums file is fetched before the asset and a mismatch installs nothing. Code signing and notarization are a follow-up: until it lands, macOS Gatekeeper and Windows SmartScreen show their first-run warning on a freshly downloaded binary, and the operator confirms it. The checksums file carries the name to verify.</p><p><strong>The <code>bun build --compile</code> build is part of the tag release.</strong> The release workflow checks the tree once, then publishes npm and builds the binaries in parallel; the release job waits on the native-OS smoke jobs - the <code>darwin-arm64</code> binary runs on macOS, the <code>windows-x64</code> binary on Windows, the <code>linux-x64</code> binary on its build runner, and the <code>linux-x64-musl</code> binary in an alpine container - before it uploads anything. A target whose smoke fails is not uploaded, and a missing target stops the upload while the npm installer degrades to its readable incomplete-release line.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A machine runs the control plane with neither Node nor Bun: the binary is the whole runtime. The <code>npx</code> path needs Node only; Bun is a development requirement, not an operator one.</li><li>The npm package shrinks to the installer and its decisions. The source leaves the tarball; the repository and the docs remain the source of truth for the app.</li><li>Each Linux binary carries the glibc and the musl OpenTUI core, and the dead variant is most of the size difference between a Linux binary and a Darwin or Windows one. It is not a flag the build can drop: measured on this branch, <code>--external</code> on the sibling left the artifact byte-for-byte unchanged, and a Linux tree without the sibling failed the compile with an unresolved import. Taking it out needs the loader to make the libc choice statically knowable - the platform and architecture branches already are, which is why a non-Linux leg builds with one core - and that is upstream&#39;s change to make. The release accepts the extra 6.3 MB, and the record states the core count and the byte size per target.</li><li>Every update re-downloads the binary for the new version, because the install note beside the cached binary names the version it came with. The note also names the target and the digest, so a cache that cannot account for itself - another machine&#39;s note in a shared home, a file written over - re-downloads instead of failing every later run. The download is the size of the binary for the machine&#39;s target, once per version.</li><li><code>--version</code> is answered by the installer while no binary is cached, and by the binary once one is. The flag exists to work on a machine with no state, so a cold run must not pay for a ~100 MB download to print one line; the installer holds the version it is about to install, and the line is the text the compiled binary prints for the same flag.</li><li>The shipped Default configuration is embedded in the binary and is seeded verbatim, as before: the read that seeds a missing config file is the file import&#39;s read, which reaches the embedded copy in a compiled binary and the repository file in a source run.</li><li>The installer resolves the Linux target by the runtime&#39;s glibc fact: a machine without it takes the musl binary. A machine the release does not build for - riscv64 Linux, arm Windows - gets the readable no-binary line naming the supported targets. The musl binary links the GNU C++ runtime, which an Alpine machine does not carry by default, so the prerequisites name <code>libstdc++</code> as a requirement of that machine.</li><li>The first-run warnings of macOS Gatekeeper and Windows SmartScreen are real until signing lands. The follow-up is signing and notarization of the release artifacts, which the checksums verification is built to sit beside.</li><li>A release whose leg fails after the npm publish still leaves a usable npm package: the installer names the missing asset and installs nothing. Re-running the leg and the release job completes the release, because the release job creates the release only where it is missing and uploads with <code>--clobber</code> either way.</li><li>The <code>linux-arm64</code> and <code>darwin-x64</code> binaries are built by the same leg that is smoke-tested for their siblings; a runner runs them in the verification record&#39;s first-publish pass, until the record gains a runner for them.</li></ul>',14)])])}const m=t(s,[["render",o]]);export{u as __pageData,m as default};
