import{_ as a,o as t,c as o,a0 as i}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0020: The control plane publishes from a version tag with a short alias package","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0020-the-control-plane-publishes-from-a-version-tag-with-a-short-alias-package.md","filePath":"adr/0020-the-control-plane-publishes-from-a-version-tag-with-a-short-alias-package.md"}'),s={name:"adr/0020-the-control-plane-publishes-from-a-version-tag-with-a-short-alias-package.md"};function n(r,e,l,h,c,d){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0020-the-control-plane-publishes-from-a-version-tag-with-a-short-alias-package" tabindex="-1">ADR 0020: The control plane publishes from a version tag with a short alias package <a class="header-anchor" href="#adr-0020-the-control-plane-publishes-from-a-version-tag-with-a-short-alias-package" aria-label="Permalink to &quot;ADR 0020: The control plane publishes from a version tag with a short alias package&quot;">​</a></h1><p>Status: accepted Date: 2026-09-14</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The control plane could only be run by cloning this repository. An operator who wanted the factory had no install path: no package on the public npm registry, no release mechanism, and no sensible default configuration.</p><p>Two constraints shaped the decision. The npm name <code>factory</code> is taken, so the short access path cannot be the main package name. And a maintainer account should not carry a long-lived npm token: the publish permission should live in the relationship between the npm account and this repository, not in a secret.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The control plane is published to the public npm registry under <code>my-little-software-factory</code>, and a second package <code>mlsf</code> is its short alias. An operator installs nothing and starts the app with <code>npx my-little-software-factory</code> or <code>npx mlsf</code>.</p><p>The release is tag driven. A GitHub Actions workflow runs on tags matching <code>v*</code>, runs lint, typecheck, the test suite, and the docs build, then publishes both packages. Any failed check stops the publish. The tag must equal the version both package manifests declare, so a tag and a manifest can never name different releases.</p><p>Publishing uses OIDC trusted publishing with <code>npm publish --provenance</code>. No npm token is stored in the repository or the account. The npm account grants publish permission for both package names to this repository once, and the registry checks the workflow&#39;s identity for every publish. The provenance attestation names this repository and the commit, so a consumer can verify what built a package.</p><p>The alias package lives in the <code>packages/</code> folder of this repository. It is two files: a manifest with a pinned dependency on <code>my-little-software-factory</code>, and a bin script that reads the main package&#39;s own bin declaration and re-execs that bin with the operator&#39;s arguments. The alias adds no behavior of its own, and it pins the exact version it ships with, so <code>npx mlsf</code> and <code>npx my-little-software-factory</code> are the same command.</p><p>On first run the control plane seeds its Config file from a Default configuration checked in and shipped inside the package, at the standard path, and loads the seeded file through the normal parse and validate path. The in-code default config object is deleted so the two copies can never drift. The standard paths live under the project name: <code>~/.config/my-little-software-factory/config.toml</code>, and the state file under <code>~/.local/state/my-little-software-factory/state.sqlite</code>, honoring the XDG state home. No migration code is written: the package was never published, and the maintainer&#39;s live config and state get a one-time manual move.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><ul><li>Publishing under the name <code>factory</code> was not possible: the name is taken on the public registry.</li><li>A personal npm token in a GitHub secret was rejected because it is a long-lived credential that grants publish rights outside this repository&#39;s workflow, and it outlives any single release.</li><li>Making <code>mlsf</code> a full copy of the package was rejected because two copies of the same app can drift, and the copy would need its own release.</li><li>A version range in the alias manifest was rejected because a range pulls a newer main package at install time without the alias being republished, so the alias would silently change behavior under the same version.</li><li>Publishing on push to <code>main</code> was rejected because a release is a decision, and one tag must name exactly one release.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A release is one tag. The workflow is the only publish path, and a broken build never reaches the registry.</li><li>The published package exposes the <code>factory</code> bin only. The gallery bin stays a development command of this repository.</li><li>The bin entry checks the Node version before it launches, with a helper the refused runtimes can still load, so an operator below the floor gets the required version and the reason.</li><li>The first publish verifies the parts no unit seam covers: the alias launcher, the bin spawn path, and the provenance attestation. The results live in the <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/docs/verification/release.md" target="_blank" rel="noreferrer">release verification record</a>.</li><li>The maintainer performs the one-time npm trusted publishing setup before the first tag. Until then the workflow would fail at the publish step, not before the checks.</li></ul>',15)])])}const m=a(s,[["render",n]]);export{u as __pageData,m as default};
