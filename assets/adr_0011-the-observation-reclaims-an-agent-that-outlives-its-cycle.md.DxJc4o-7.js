import{_ as t,o as a,c as o,a0 as n}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0011: The observation reclaims an agent that outlives its work cycle","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0011-the-observation-reclaims-an-agent-that-outlives-its-cycle.md","filePath":"adr/0011-the-observation-reclaims-an-agent-that-outlives-its-cycle.md"}'),i={name:"adr/0011-the-observation-reclaims-an-agent-that-outlives-its-cycle.md"};function s(r,e,c,l,h,d){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0011-the-observation-reclaims-an-agent-that-outlives-its-work-cycle" tabindex="-1">ADR 0011: The observation reclaims an agent that outlives its work cycle <a class="header-anchor" href="#adr-0011-the-observation-reclaims-an-agent-that-outlives-its-work-cycle" aria-label="Permalink to &quot;ADR 0011: The observation reclaims an agent that outlives its work cycle&quot;">​</a></h1><p>Status: accepted Date: 2026-09-02</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0005 ends a work cycle at close: the ticket returns to <code>open</code> at once with an incremented cycle number. ADR 0006 keys every later read on the pane id the handoff recorded, and the loop only looks at panes of tickets in <code>handed-off</code>, <code>running</code>, or <code>awaiting</code>. Together these two rules assume that a closed cycle leaves no live agent behind.</p><p>It does not always. The Close cleanup of a worktree handoff removes the herdr workspace with its checkout, and herdr refuses to remove a dirty checkout without <code>--force</code>. The control plane keeps the state transition and warns on the message line (the rule from the auto-handoff spec, issue #7), so the cycle closes and the agent&#39;s pane stays open. The operator can also re-prompt a settled agent in herdr after the close, which the control plane cannot see: it never re-prompts an agent it handed off.</p><p>Observed on a live ticket: the agent settled a cut-short turn, auto-handoff closed the cycle in the same poll, the cleanup failed on the dirty checkout, and the operator typed <code>continue the work of previous agent</code> into the herdr pane. herdr reported that agent <code>working</code> for the next three hours. The control plane showed the ticket as <code>open</code>, did not count its agent against the parallel limit, and a fresh handoff of the same ticket failed with herdr&#39;s <code>agent_name_taken</code>: the live agent still held the name derived from the ticket&#39;s branch, and no later handoff of that ticket could ever start.</p><p>The considered alternatives:</p><ul><li>Do not end the cycle when the Close cleanup fails: keep the ticket <code>awaiting</code> with a pending trace, so the existing reopen path (issue #7) catches the re-prompted agent. Rejected: a cleanup that never succeeds (always a dirty checkout) strands the ticket in <code>awaiting</code> with no exit, and the automatic rule retries the same failing cleanup every poll.</li><li>Refuse the close until the operator cleans up the checkout. Rejected: it makes close conditional on an external tool&#39;s opinion about the working tree, and it blocks the operator from ever closing a cycle.</li><li>Give each handoff a unique herdr agent name, so a leftover agent cannot block the next handoff. Rejected as the fix: it hides the collision instead of seeing the agent, and the live agent stays invisible to the operator.</li><li>Track the agent by its name or its checkout path instead of its pane id. Rejected: ADR 0006 already settled the pane id as the only reliable handle.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The observation loop reclaims a live agent whose work cycle has closed.</p><p>Each poll, after a successful <code>herdr agent list</code> and before it counts agents against the parallel limit, the loop takes every ticket that rests in <code>open</code> and whose latest handoff recorded a pane. When herdr reports an agent <code>working</code> or <code>blocked</code> in a pane no in-flight or awaiting ticket holds, the loop records a Reclaim: a handoff attempt and a handoff row in the ticket&#39;s current work cycle, with the previous handoff&#39;s choices and the same herdr handles, and the ticket in <code>running</code>. It runs no external command, and it rewrites nothing: the closed cycle keeps its handoff row and its decided completion trace.</p><p>The reclaim counts as a handoff, so the per-ticket handoff limit bounds a close-and-reclaim loop. An idle, done, or unknown report reclaims nothing: only herdr&#39;s evidence of live work opens a handoff. The warning names the ticket on the message line, so the operator sees why a closed ticket is running again.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The ticket list shows reality for an agent the control plane started, with no new state and no new polling command. When the reclaimed agent settles, its turn follows the normal path: <code>awaiting</code>, a completion trace, and the operator&#39;s or the automatic rule&#39;s decision.</li><li>A closed ticket can become <code>running</code> without a handoff the operator asked for. The durable record says so: its attempt carries the <code>reclaimed</code> stage, and its <code>started_at</code> is the poll that saw the agent, not the moment the agent started.</li><li>A reclaimed agent holds a parallel slot, so auto-handoff starts one fewer agent while it works.</li><li>The <code>agent_name_taken</code> dead end goes away for the common case: the reclaimed ticket is no longer <code>open</code>, so nothing re-hands it off. A leftover agent that only reports idle still holds its name; a failed cleanup stays visible on the message line, and the operator clears it in herdr.</li><li>The control plane trusts the pane id, so it treats any live agent in a pane it opened as that ticket&#39;s work. That is the intended reading: the pane sits in the workspace herdr made for the ticket&#39;s branch.</li></ul>',14)])])}const f=t(i,[["render",s]]);export{u as __pageData,f as default};
