import{_ as t,o as r,c as s,a0 as i}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"Prerequisites","description":"What the machine needs before the first start of the control plane.","frontmatter":{"title":"Prerequisites","description":"What the machine needs before the first start of the control plane."},"headers":[],"relativePath":"getting-started/prerequisites.md","filePath":"getting-started/prerequisites.md"}'),n={name:"getting-started/prerequisites.md"};function a(o,e,h,l,d,p){return r(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h1><p>The control plane ships through npm, and the npm package installs a prebuilt binary for the machine (ADR 0056). You install nothing: <code>npx</code> runs the current published version. Before the first start, the machine needs:</p><ul><li>Node, for the <code>npx</code> that bootstraps the installer. The installer runs on Node, downloads the prebuilt binary for the machine from the release, and hands it the command. The binary itself needs neither Node nor Bun: it carries its own runtime.</li><li><a href="https://github.com/seriousjul/herdr" target="_blank" rel="noreferrer">herdr</a> on the <code>PATH</code>. The control plane drives it through its CLI and never starts an agent process itself.</li><li>The agent CLI of each agent type you use, with its own provider auth applied.</li></ul><p>Bun is a development requirement of the repository, not an operator one: it builds, tests, and runs the control plane from source, and it compiles the prebuilt binary the release publishes.</p><p>On macOS and Windows, the first run of a freshly downloaded binary shows the operating system&#39;s first-run warning - Gatekeeper on macOS, SmartScreen on Windows - until the release&#39;s binaries are signed (ADR 0056). The binary is verified against the release&#39;s SHA-256 checksums before the installer keeps it.</p><p>On a musl Linux machine - Alpine and the Alpine-derived images - the binary needs the GNU C++ runtime, which the base image does not carry: install <code>libstdc++</code> (<code>apk add libstdc++</code>) before the first run. The release&#39;s musl smoke runs in a container that installs it, and measured without it the binary stops on a relocation error before it answers anything.</p><p>Next: <a href="./first-launch.html">first launch</a>.</p>',7)])])}const f=t(n,[["render",a]]);export{u as __pageData,f as default};
