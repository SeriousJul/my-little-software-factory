import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0038: Enter answers a Consultation with the surface its state needs","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0038-enter-answers-a-consultation-with-the-surface-its-state-needs.md","filePath":"adr/0038-enter-answers-a-consultation-with-the-surface-its-state-needs.md"}'),s={name:"adr/0038-enter-answers-a-consultation-with-the-surface-its-state-needs.md"};function r(d,e,c,i,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-0038-enter-answers-a-consultation-with-the-surface-its-state-needs" tabindex="-1">ADR 0038: Enter answers a Consultation with the surface its state needs <a class="header-anchor" href="#adr-0038-enter-answers-a-consultation-with-the-surface-its-state-needs" aria-label="Permalink to &quot;ADR 0038: Enter answers a Consultation with the surface its state needs&quot;">​</a></h1><p>Status: accepted Date: 2026-09-20</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Enter carried two meanings in the Consultation section: the response on an <code>awaiting-response</code> record, and Agent interaction on a <code>working</code> or a blocked one. Every other state answered the key with the response control&#39;s reason, so an operator holding a broken Consultation read &quot;only an awaiting Consultation can receive a response&quot; over a record that could never answer that.</p><p>The recovery itself already existed, but it reached the operator through three unrelated keys: <code>r</code> retried an interrupted opening, <code>c</code> opened the Replacement launcher over a <code>missing</code> or a <code>failed</code> record, and <code>w</code> opened the close panel whose <code>closing</code> shape carries the Retry and the Force-close (ADR 0037). Each one worked, and none of them was discoverable from the key the operator already presses to make a Consultation do something.</p><p>The Ticket section had already settled this shape: Enter means the thing the selected record&#39;s state asks for - Hand off on an open Ticket, Decide on a settled one, the Live view on an in-flight one - and the shared catalogue owns the resolution, so the Action bar, the Key guide, and the dispatch cannot disagree.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Enter answers every Consultation state with the surface that state needs</strong>, and the catalogue owns the resolution:</p><table tabindex="0"><thead><tr><th>State</th><th>Enter</th></tr></thead><tbody><tr><td><code>working</code></td><td>Interact, unchanged</td></tr><tr><td><code>awaiting-response</code></td><td>Respond, unchanged</td></tr><tr><td><code>awaiting-response</code> with a blocked Agent</td><td>Interact, unchanged</td></tr><tr><td><code>opening</code></td><td>the recovery panel: Recover, Close</td></tr><tr><td><code>missing</code>, <code>failed</code></td><td>the recovery panel: Replace, Close</td></tr><tr><td><code>closing</code></td><td>the existing close panel: Retry, Force-close, Cancel</td></tr><tr><td><code>closed</code></td><td>a refusal: the selected Consultation is already closed</td></tr></tbody></table><p><strong>The recovery is one new control with one panel module.</strong> <code>Recovery</code> joins the two live <code>return</code> controls in the shared catalogue, so the bar, the guide, and the dispatch read one source. The panel&#39;s copy lives in <a href="https://github.com/SeriousJul/my-little-software-factory/blob/main/src/components/consultation-recovery-panel.ts" target="_blank" rel="noreferrer">consultation-recovery-panel.ts</a> beside the close panel&#39;s, and the record&#39;s state names its rows, so the panel cannot offer a choice the record denies. It renders through the same shared <code>ActionPanel</code> the close and delete confirmations use.</p><p><strong>The panel runs the existing operations; it stores nothing.</strong> Recover calls the same recovery <code>r</code> calls, Replace takes the same path <code>c</code> takes onto the Replacement launcher with the durable recovery context and the link to the replaced record, and Close calls the same close path <code>w</code> calls, so a live Agent still confirms and a record with no Agent still closes directly. A <code>closing</code> record does not get a second recovery screen: Enter opens the close panel that already holds its rows.</p><p><strong>The <code>closed</code> refusal comes from the catalogue&#39;s order, not from a screen.</strong><code>Recovery</code> is the first <code>return</code> candidate in the Consultation section: a live record still resolves to an available Respond or Interact, and a record with no available Enter meaning reads this control&#39;s reason. A refusal an operator can act on beats a reason that belongs to a different state.</p><p>The considered alternatives:</p><ul><li>Keep Enter silent on a broken record and leave recovery to <code>r</code>, <code>c</code>, and <code>w</code>. Rejected: the operator presses Enter on a stuck Consultation because they want it to move, and the answer they got named a state they were not in.</li><li>Run the state&#39;s recovery directly on Enter, with no panel. Rejected: the same key would then destroy a record on a <code>failed</code> row and resume an Agent on an <code>opening</code> one, and neither choice deserves a single unconfirmed press.</li><li>Draw one panel for <code>closing</code> as well. Rejected: the close panel already carries the retry and the force-close, and a second screen for the same decision is a second thing to keep honest.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The Consultation section has three meanings of Enter, and the Key guide names all three: the guide row for the new control states the recovery meaning, and the gallery&#39;s examples hold the recovery panel&#39;s states, drawn from the production module.</li><li>The Action bar and the Key guide name the row <code>Enter Recovery</code>, and the guide states what it opens, so it cannot be taken for the <code>r</code> recovery of an interrupted opening.</li><li>A recovery panel that loses its states releases itself and says so on the Message line, the way the close panel does: a panel that paints nothing must not keep holding the keys the panels swallow.</li><li><code>r</code> stays the direct recovery of an interrupted opening and <code>c</code> stays the direct Replacement: the panel is the discoverable route, not the only one.</li></ul>',16)])])}const g=t(s,[["render",r]]);export{u as __pageData,g as default};
