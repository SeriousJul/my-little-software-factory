import{_ as t,o,c as a,a0 as n}from"./chunks/framework.D4fUSYzX.js";const s="/my-little-software-factory/assets/main-view.CzDQJ6HM.png",f=JSON.parse(`{"title":"Main view","description":"The Main view's two sections, its counts, its controls, and the layout the terminal shows.","frontmatter":{"title":"Main view","description":"The Main view's two sections, its counts, its controls, and the layout the terminal shows."},"headers":[],"relativePath":"operation/main-view.md","filePath":"operation/main-view.md"}`),i={name:"operation/main-view.md"};function h(r,e,d,c,l,p){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="main-view" tabindex="-1">Main view <a class="header-anchor" href="#main-view" aria-label="Permalink to &quot;Main view&quot;">​</a></h1><p><img src="'+s+`" alt="The Main view: the Ticket and Consultation sections on the left, the
detail of the selected ticket on the right"></p><p>The Main view holds two list sections on the left, the Ticket section on top and the Consultation section below, and one context-dependent detail pane on the right that shows the detail of the item the cursor holds. One control catalogue, one Action bar, and one Message line answer for both. Both sections start expanded; <code>x</code> or a click on a section header collapses the section under the cursor to its header row, and the same toggle restores it. Up and down move the cursor through the visible rows and cross the section boundary when the sections are adjacent. The mode the bar and the guide state derives from the section that holds the cursor and its focused pane.</p><p>The Ticket header always shows the pipeline counts - open, running, and awaiting - with the held count appended only when it is non-zero. The Consultation header carries its attention facts (awaiting response, recovery). Both counts are computed from the in-memory projection on each render; neither queries the state.</p><p>The control plane keeps a contextual Action bar in the last row of the terminal. It shows the controls the current interaction mode can run, dims one it will not run in the present state, and names the reason on the Message line when the operator presses it anyway.</p><p>The in-app Key guide lists the controls of the modes the app dispatches from its catalogue. Press <code>?</code> or <code>F1</code> to open it from anywhere, including the panes and the modals. It carries the Ticket list, the Ticket detail, the Consultation list, the Consultation detail, the Agent terminal, the response editor, the override panel in both of its row kinds, the decision modal, the missing modal, the guide and the Message view, the controls that are only reachable from another mode, Quit, and the <code>Ctrl+C</code> emergency exit, each with what it does and, where the app will not run it, why. Press <code>Esc</code>, <code>F1</code>, or <code>?</code> to close it.</p><p>This guide does not repeat that list. A table of keys here went stale twice: the guide and the Action bar are generated from one control catalogue (<code>src/components/controls.ts</code>), so what the app shows is what the app runs.</p><p>The Ticket list and detail move with the row, page and jump keys, focus the detail with <code>l</code> or <code>Right</code> and the list with <code>h</code> or <code>Left</code>, hand an open ticket off with <code>Enter</code>, open the decision modal on an awaiting one, the missing modal on a ticket whose agent is gone, and the override panel with <code>e</code>. <code>=</code>, <code>+</code>, and <code>-</code> bump a ticket&#39;s priority up and down through the configured rank, and Backspace clears it to the label rank or unranked. <code>a</code> toggles auto-handoff, <code>r</code> refreshes, <code>g</code> goes to the agent&#39;s pane, and <code>q</code> quits.</p><p>When the Message line is truncated, press <code>m</code> in a base pane or <code>F2</code> in any mode to read the captured message in the Message view. The Message line and the Action bar reserve the two bottom rows at every terminal size, and each list section reserves its header row plus a minimum of three content rows: below the smallest useful frame (40 columns by 19 rows) the panes give way to a size message and a compact Help control, a section that cannot hold its minimum collapses rather than vanishing so both headers keep their counts, and a surface that cannot draw its own rows says so instead of painting them over its border. One hint holds the row&#39;s end cells: Help on a bar that can open the Key guide, and the overlay&#39;s own Close on a utility overlay. A frame too narrow for that hint states one of its whole keys, so the way out of a screen is named at any width and never cut in half.</p><p>The keys the override panel answers with live on the <a href="./consultation.html">Consultation page</a>, under Entry controls.</p><h2 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to &quot;Layout&quot;">​</a></h2><p>The Main view is one surface with two list sections, the Ticket section on top and the Consultation section below, and one context-dependent detail pane on the right (ADR 0019). Both sections start expanded, and the detail pane shows the detail of whichever item the cursor holds: the ticket detail on a ticket, the Consultation detail on a Consultation. <code>x</code> or a click on a header toggles the section under the cursor: it shrinks to its header row and its rows leave the navigation flow, and the same toggle restores it. A collapsed section keeps its list selection, and the selection and detail of a collapsed section survive the collapse, so a re-expand shows the same place. The rows run: the mode line (while the control plane has state to observe), the Ticket header across the full terminal width, the two sections&#39; list panes stacked on the left with the Consultation header between them, the detail pane on the right, the Message line, and the Action bar. The focused section takes the remaining rows after the other section claims its minimum of three content rows, so the list the operator works in gets the room. The Ticket header always shows the pipeline counts - open, running, and awaiting, in the labelled form on a terminal of at least 60 columns and the short form below - and appends the held count with its bell marker only when it is non-zero. The Consultation header carries that section&#39;s attention facts, its awaiting-response and recovery counts, the bell marker while the bell rings, and &quot;new output&quot; while that fact holds, so a Consultation that needs the operator is visible whether the section is expanded or collapsed and no free-standing attention line exists. A section that cannot hold its minimum collapses rather than vanishing, so both headers keep their counts; below the smallest useful frame the compact frame drops the panes with a size message.</p><p>Two panes side by side, flex-sized to the terminal. The list pane on the left shows the tickets of the Ticket section with their state badge, task type badge, title, and repository. The task type badge is the type the control plane would hand off: an open ticket shows its suggested task type, every other ticket shows the task type its recorded handoff started with. A non-open ticket without a recorded handoff shows <code>[unknown]</code>, and only that badge wears a warning color; every configured task type uses one neutral style. The detail pane on the right shows the full detail of the selected ticket: repository, ticket state, the Agent its handoff runs on with its Environment and that handoff&#39;s Model, Thinking level, and context window, source name, source kind, external key, source state, URL, labels, and source health. An open ticket shows the settings its suggested task type&#39;s Task profile resolves to, which is what Enter starts, including the Environment it starts in; a ticket inside a work cycle shows the settings its own handoff started with. A close returns a ticket to open and keeps that handoff&#39;s record as history, so the rows follow the ticket&#39;s state rather than whichever record survives. A setting left to the agent reads <code>left to agent</code> in the dim color. The detail carries one explicit task type line for every ticket: <code>Suggested task type:</code> for an open ticket, <code>Handoff task type:</code> for every other, with <code>Handoff task type: unknown</code> when the handoff data is absent. The detail also carries the ticket&#39;s handoff count against its per-ticket limit, counting the handoffs of every work cycle the ticket ran, and, when one exists, the last completion: its date, the task type, the agent, and the recorded decision. Factory ticket state and external source state stay separate. The panes share one focus. Switching focus never moves the selection.</p><p>The vertical keys act on the focused pane. With the list focused, they move the selection and cross the section boundary when the sections are adjacent: from the last row of the Ticket list the next down lands on the Consultation list, and the next up from its first row lands back on the tickets. Page keys move by one visible list page, and Home and End select the list edges. With the detail focused, the row keys move at the configured speed, PageUp and PageDown retain one row of context, and Home and End move to the detail edges. A new selection starts the detail at the top.</p><p>The detail is a native OpenTUI viewport. Its complete content stays mounted, so wheel bursts translate one stable surface instead of rebuilding visible rows. When the content overflows, its right inner column has a proportional scrollbar. The gutter is always reserved when width permits, so wrapped text does not reflow as the bar appears. Click or drag the scrollbar, or use the wheel or trackpad over any part of the detail. Fast vertical wheel events accelerate to the configured limit. Horizontal and Shift-wheel input is ignored. A click or wheel action focuses its pane. Clicking a visible row in either list selects it, a list wheel event selects one adjacent row, and a click on a section header toggles that section.</p><p>Mouse reporting takes the host terminal&#39;s native text selection with it, so the control plane gives it back as Auto copy: drag with the mouse over any surface and the text highlights, and releasing the drag copies the selection to the system clipboard. A click that does not drag copies nothing, a copy the terminal refuses warns on the Message line, and a copy that takes is silent. Auto copy is always on, over every surface, with no setting. The keyboard Copy control on a field selection keeps its own path.</p><p>When the terminal is too narrow for a field, the field drops out of the row instead of wrapping it. The repository drops before the title does, and the task type badge is complete or absent: the row keeps the whole badge with a readable title, or the badge drops and the title takes the cells. A partial badge could read as another task type, so it never truncates; the full value stays in the detail pane.</p><p>Above the Action bar sits the Message line. It carries the progress and the outcome of the last handoff: <code>Working: handing off &quot;...&quot;...</code> while one is in flight, <code>Warning:</code> for a sibling clone or other recoverable issue, and <code>Error:</code> for a failed handoff. A clean handoff clears its Working line, and a warning the operator drew while it ran - a refused key or clear - then takes the line. While a handoff is in flight keys keep working, and <code>e</code> is refused with a warning. A second handoff claim records its attempt, which blocks a further claim on the same ticket, and queues its external work until the in-flight handoff settles; the ticket moves to <code>handed-off</code> only when the handoff settles and its agent starts, so claims never race each other. A leftover clear holds that same seat, and so does the Close cleanup of any path that runs one: an environment change and a handoff never work beside each other.</p><p>Above the panes sits a mode line. It shows the auto-handoff state and the live agents against the parallel limit: <code>auto: on 1/2</code>, or <code>auto: off 1</code> when no limit is set. The count is the in-flight tickets whose agent was alive in the latest herdr poll. The <code>a</code> key toggles the mode for the session from the Ticket section; the config&#39;s <code>auto-handoff</code> key sets the startup value only.</p><p>A blocked agent replaces the state badge in the list row with a <code>blocked</code> badge: the agent shows an approval or question UI and waits for a human. The ticket stays in flight, in <code>handed-off</code> or <code>running</code>, and still counts against the parallel limit. A missing agent replaces the state badge with a <code>missing</code> badge: the stored pane is gone or holds no agent, so the work stops there until the operator restarts or abandons the cycle. Both badges clear when the next poll no longer shows the condition. The first poll has not landed yet, so no badge appears before it. A ticket that has used up its per-ticket handoff limit wears a trailing <code>handoff limit</code> marker at the end of the row, and the detail pane shows the count as <code>Handoffs: 2/2</code>. Auto-handoff leaves such a ticket open; a manual handoff may still pass the limit. A ticket whose previous herdr environment is still alive wears a trailing <code>leftover</code> marker, and its detail pane names the workspace, tab, and pane that remain, the reason the control plane knows, and since when. <code>w</code> clears it.</p>`,20)])])}const u=t(i,[["render",h]]);export{f as __pageData,u as default};
