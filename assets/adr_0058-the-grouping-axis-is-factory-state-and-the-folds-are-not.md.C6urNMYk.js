import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0058: The grouping axis is factory state and the folds are not","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0058-the-grouping-axis-is-factory-state-and-the-folds-are-not.md","filePath":"adr/0058-the-grouping-axis-is-factory-state-and-the-folds-are-not.md"}'),n={name:"adr/0058-the-grouping-axis-is-factory-state-and-the-folds-are-not.md"};function i(r,e,h,d,l,c){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0058-the-grouping-axis-is-factory-state-and-the-folds-are-not" tabindex="-1">ADR 0058: The grouping axis is factory state and the folds are not <a class="header-anchor" href="#adr-0058-the-grouping-axis-is-factory-state-and-the-folds-are-not" aria-label="Permalink to &quot;ADR 0058: The grouping axis is factory state and the folds are not&quot;">​</a></h1><p>Status: accepted Date: 2026-09-24</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Ticket section&#39;s list can be split into Groups by one fact at a time: the Repository, the Ticket source, the Task type, the Ticket state, or the Workflow position. Two operator choices came with it: which axis is on, and which Groups stand collapsed.</p><p>The axis is a preference the operator picks once per setup and wants kept: an operator who reads the list by repository should not press the key again on every launch. A fold is different. A fold hides rows, and a hidden group can hide an <code>awaiting</code> ticket whose decision is owed. ADR 0036 named the view facts that stay session-only: section collapse, selection, the history filter, and the launcher draft. This decision moves one of those neighbors&#39; peer across the line, and leaves the fold behind.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The Grouping axis is factory state on the state file</strong>, the way the Auto-handoff mode (ADR 0036) and the queue pause (ADR 0052) are. A fresh state file starts with the axis at <code>none</code>, the flat list. The key that changes the axis writes the new value at once, and a restart finds the grouping where the operator left it.</p><p><strong>The record is keyed by section, not by one section.</strong> The axis value stands in a table with one row per section, so the Ticket section is the only row today and a second list that takes grouping needs no new schema version.</p><p><strong>Collapsed Groups are session facts.</strong> Which Groups stand folded lives in memory for the run, keyed by the axis and the group value, so an axis the operator visits twice comes back as they left it. A restart, a dev reload, and a fresh launch open every Group. The plane never restores a fold, so no decision can be out of sight because of a choice made in an earlier run.</p><p><strong>A plane with no state file keeps both in memory.</strong> Where the projection runs without SQLite state, the axis choice holds for the run and nothing is written. Grouping degrades to session-only; it never refuses the key.</p><h2 id="considered-options" tabindex="-1">Considered options <a class="header-anchor" href="#considered-options" aria-label="Permalink to &quot;Considered options&quot;">​</a></h2><ul><li><strong>Both durable: the axis and the folded group keys.</strong> It keeps the whole view the operator left, and it can bring a restart back with an owed decision behind a fold the operator set days ago. Rejected for the same reason the Dispatch pause is a gate and not a surprise: the plane must not hide work the operator has not seen.</li><li><strong>Both durable, with a boot guard that opens any fold holding an awaiting ticket.</strong> It answers the failure above, and it makes a fold unreliable: the same keystroke opens or keeps a Group shut depending on facts the operator did not touch. Rejected because the plane already refuses to open a Group by itself mid-run, and a guard that only runs at boot is two rules, not one.</li><li><strong>Both session-only.</strong> The cheapest shape, and it makes the operator re-pick the axis every launch, which is the one part of the request this decision exists to serve.</li><li><strong>The axis in the Config file.</strong> The Config file is the operator&#39;s and the plane writes back only the Repository mapping section. A per-run view choice does not belong in a file the operator hand-edits, and the factory-state pattern already carries two operator switches.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The state schema gains a version for the axis table.</li><li>The two facts read differently at boot: the axis is read from the state file, and every Group starts open. The Message line names the axis on every change, so a durable value never arrives silently.</li><li>Two planes on one state file share one axis, last write wins, exactly as they share the queue pause and the Auto-handoff mode.</li><li>The other session view facts (section collapse, selection, the history filter, the launcher draft, and now the folds) stay session-only, so ADR 0036&#39;s list stands with one named exception.</li></ul>',14)])])}const f=t(n,[["render",i]]);export{u as __pageData,f as default};
