import{_ as t,o,c as a,a0 as s}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse(`{"title":"ADR 0060: An operator ignores a ticket out of the factory's way","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0060-an-operator-ignores-a-ticket-out-of-the-factorys-way.md","filePath":"adr/0060-an-operator-ignores-a-ticket-out-of-the-factorys-way.md"}`),n={name:"adr/0060-an-operator-ignores-a-ticket-out-of-the-factorys-way.md"};function i(r,e,h,d,l,c){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0060-an-operator-ignores-a-ticket-out-of-the-factory-s-way" tabindex="-1">ADR 0060: An operator ignores a ticket out of the factory&#39;s way <a class="header-anchor" href="#adr-0060-an-operator-ignores-a-ticket-out-of-the-factory-s-way" aria-label="Permalink to &quot;ADR 0060: An operator ignores a ticket out of the factory&#39;s way&quot;">​</a></h1><p>Status: accepted Date: 2026-09-24 Revised 2026-09-25: the first version of this decision lifted an ignore by itself, in a pass of the observation cycle beside the settle and the missing facts, and cleared the flag when it did. That lift is withdrawn here. The flag now stands until the operator&#39;s own key clears it, and the list rule reveals a row instead of unwriting a flag; see <strong>The ignore ends where an obligation begins</strong> and <strong>The reveal is the section&#39;s own key</strong> below, and the lift&#39;s entry in <strong>Considered options</strong>. The same revision names the gate once, <code>ticketIgnored</code>, states the identity reads&#39; one projection read apart from the List filter&#39;s <code>all</code> view, and takes the <code>i</code> line from the read the act caused rather than from the flag&#39;s own fact. Every other decision in this record stands as accepted.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The ticket list carries work the operator has judged out for the foreseeable future: an issue nobody will take, an alert the team accepted as a risk, a pull request from a fork that stays open forever. Each one keeps a row, keeps its place in the section&#39;s counts, and, with Auto-handoff mode on, stays a candidate for the automatic top-up. ADR 0051&#39;s gates hold a ticket the machine has nowhere to send, and ADR 0042&#39;s list rule withholds a ticket whose work sits in an open fixing pull request. Neither answers &quot;I have decided this one out&quot;, because that is the operator&#39;s judgment, not a fact of the source or of the machine.</p><p>The obvious shape was a label write, since the Workflow machine reads labels and a state that offers no task already stops the auto-handoff. The class rule that would carry it does not reach every ticket source: a Dependabot alert, a security advisory, and a secret scanning alert carry no labels of their own, and the plane synthesizes severity onto them alone (ADR 0029), so a rule that filters a class by label works on issues and pull requests and stands blind on the security kinds.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The ignore is the operator&#39;s act on one Ticket, and the flag is factory state.</strong> <code>i</code> on a Ticket row writes the flag on the ticket identity in the state file, and the same key on an ignored row clears it. The plane writes nothing to the source: no label, no close, no comment. The flag survives every refresh, a source that drops the item and brings it back, and a work cycle that ends and starts again, and nothing clears it but the operator&#39;s own key. The cost is that the fact belongs to one state file: another machine or a fresh file does not carry it, and a teammate sees no sign of it.</p><p><strong>An ignored Ticket leaves the list and every automatic start.</strong> The list rule in the ticket projection gains the ignore beside ADR 0042&#39;s covered rule: two causes, one read, in the state module alone, and an ignored row leaves the section&#39;s counts the way a covered one does. The gate is one predicate, <code>ticketIgnored</code>, and it takes the flag and never the row&#39;s face. Three of ADR 0051&#39;s top-up walks call it on the row they hold - the continuation, the re-fired skip on its position, and the open-ticket add - and the fourth, the restart walk, reads the in-flight tickets rather than the list, so it asks the cycle&#39;s one read of the pile, <code>ignoredTickets</code>, which is the same column by identity. The restart walk needed the gate on its own terms: without it an ignored ticket whose Agent went missing would take an automatic Restart, and the plane would start an Agent on work the operator had just judged out. The open-ticket add reads the list with the ignore&#39;s own withhold lifted, so its call is what holds the row out: no walk leaves the gate to the view it happens to read.</p><p>The counts and the held-count bell read that active view, never the operator&#39;s List filter. The machine&#39;s obligations and the section&#39;s numbers are facts about the factory, so a cycle of <code>f</code> moves none of them and rings nothing, and the rows the filter shows are the only thing the view decides. The same rule keeps every read that resolves a Ticket by identity off the filter: a Work queue row and the cancel line that removes its item, an open panel and the Live view&#39;s pane read, a route&#39;s position, and a confirmed override share one projection read and ask it for the whole projection that stands before the list rule, while the live checkout&#39;s conflict name and the launcher&#39;s repository choices take the active view. Neither follows the drawn rows, so a withheld row still names its ticket, keeps its pane, and never tears down the screen that shows it.</p><p><strong>The ignore is an automatic gate, never a hard start gate.</strong> It stands beside the Same-type hold (ADR 0026) and the Dispatch pause (ADR 0016), not beside the source health check the claim runs. The pickup and the force-dispatch both start an ignored ticket&#39;s queue item, because the only item an ignored ticket can hold is one the operator asked for by hand: the ignore itself removes any item that already waited. A gate on the pickup would leave that ask with no way to start.</p><p><strong>The ignore ends where an obligation begins.</strong> The key refuses a Ticket that owes the operator a decision now: an <code>awaiting</code> one, one whose newest settled turn is Held, or one whose Agent is missing. The refusal reads the facts the row&#39;s own face reads: the ticket state, the newest settled turn&#39;s cause and decision, and the latest poll&#39;s missing Agent.</p><p>The ignore then hides a resting Ticket, and never a live one. An ignored Ticket whose Agent works keeps its row because there is live work to reach - the row is how the operator reaches the Live view, the Goto, and the Close - and one whose turn settled keeps its row because the operator owes it a decision. The flag stays set underneath both, so the row wears its <code>ignored</code> marker beside its own state badge while the work runs, and goes back into the pile when the cycle ends and the Ticket rests <code>open</code> again. This is ADR 0042&#39;s shape for the same reason: the in-flight states are never covered, so live work stays listed whatever pull requests exist, and the ignore withholds a row on the operator&#39;s judgment, not on the machine&#39;s facts.</p><p>That is not a courtesy. The Dispatch pause reads the completion traces and asks nothing about the list (ADR 0016), while the held count reads the list, so an ignored Held turn would stall every automatic start behind a mode line that says <code>paused</code> over an empty list with nothing to point at. Hiding a resting row and revealing a live one is the one rule that keeps a stalled factory visible, and it needs no write to do it: every obligation the refusal predicate names lives on a Ticket that has already left <code>open</code>, so no obligation is ever out of the list the counts, the bell, and the Decision region read. And because the flag stands, the gate the walks read stands with it: an ignored Ticket whose Agent has gone missing keeps waiting for the operator&#39;s own hand, cycle after cycle, and the key that put it away is the key that puts it back in the machine&#39;s way.</p><p><strong>The ignore does not mute the operator&#39;s own ask.</strong> A manual Handoff passes an ignored Ticket, the way it passes the Handoff limit and the Same-type hold. The Ticket section&#39;s <code>i</code> control is available in both of the section&#39;s modes, so the detail pane reaches it the way every other Ticket control does.</p><p><strong>The reveal is the section&#39;s own key.</strong> <code>f</code> cycles the Ticket section&#39;s List filter through <code>active</code>, then <code>ignored</code>, then <code>all</code>. This is the Consultation section&#39;s history filter&#39;s shape on the same key: one key, one meaning per section, the way <code>w</code> closes work in both sections and <code>g</code> goes to the Agent in both. The filter is one of ADR 0036&#39;s session view facts beside the history filter: it says nothing about any Ticket, it starts on <code>active</code> at every boot, and no restart brings the pile back on its own. The cursor keeps its ticket when the cycle still shows that ticket. The ignored view keeps the attention bands and the newest-external-update order and carries no order of its own, the way a Group presents the list&#39;s order and never a new one (ADR 0059). It is the ledger of the flag: every row the operator put away stands in it, including one the active view shows again while its work is live, and including one ADR 0042&#39;s covered rule takes out of the list - a Ticket ignored first and covered by a fixing pull request that appears later stands in the pile and in no other view, because the only key that clears a flag rides on the row, and a pile that hid the row would hide the key with it. An ignored row keeps its state badge and takes a trailing marker beside the handoff-limit and leftover markers, and the detail pane states the ignore, the moment it was set, and the key that clears it. The Ticket header carries a conditional <code>ignored n</code> cell above zero, with no bell and no click: the header&#39;s click already toggles the section, and the held bell carries a fact the ignore cannot hold. It is the row&#39;s last cell for that same reason: the row truncates at its end, so the held count and the bell that rings on it stand ahead of it, and a view fact never cuts a decision the operator owes. The <code>i</code> line says which of the two the act did, and it says it from the re-read the act caused: a resting Ticket loses its row, counts, and every automatic start, a live one keeps its row under the flag, and a clear that leaves the row to ADR 0042&#39;s covered rule names that rule instead of promising a row the list does not draw.</p><p><strong>An ignore is not a fold, and the two differ on purpose.</strong> ADR 0058 keeps the Group fold session-only, and ADR 0059 states its rule: a fold hides rows and never facts, and the plane never lifts one by itself. The ignore breaks the first half, deliberately. It hides a row <em>and</em> removes the ticket from every automatic start, because taking the work out of the machine is half of what the operator asked for; a mechanism that only moved the row would leave the top-up walking over a ticket its operator had just judged out. It keeps the second half: the plane never lifts an ignore by itself, because the flag is the operator&#39;s judgment and only their key ends it. What the plane does instead is decline to hide what the operator still has to reach: it withholds a resting row and reveals a live one, and it says so on the row&#39;s own face. Both mechanisms still serve ADR 0058&#39;s one rule, that the plane must not hide work the operator has not seen. The fold keeps it by holding nothing across a restart and lifting nothing on its own. The ignore keeps it by refusing to hold an obligation in the pile at all. An ignored ticket that rests sits in no Group and is counted by no Group header, because every axis reads the list the ignore has already taken the row from: the axis slices the rows that show, the ignore chooses which rows there are.</p><h2 id="considered-options" tabindex="-1">Considered options <a class="header-anchor" href="#considered-options" aria-label="Permalink to &quot;Considered options&quot;">​</a></h2><ul><li><strong>The lift: the plane clears the flag itself when an obligation appears.</strong> The first version of this decision held it, in a pass that ran after the settle and the missing facts wrote theirs, so the row that came back was a flag the plane had unwritten and the cause could be named. It was measured away. A cleared flag no longer gates the restart walk, so in auto mode an ignored Ticket whose Agent went missing still took an automatic Restart one poll later, and the operator could not put the work away again, because <code>i</code> refuses a missing Agent: the one surface the obligation exists to keep was gone by the time it stood. The reveal in this decision costs no write, gates as long as the operator let it stand, and needs no ordering against the Top-up, because there is no pass to place.</li><li><strong>A label write plus a Workflow parking state.</strong> Rejected: the ignore is one operator&#39;s judgment on one machine, not a statement to the team; the plane&#39;s write surface stays the settle-time fire and the recorded-skip re-fire (ADR 0042); and no label rule reaches the security kinds, which carry no labels but the severity the plane synthesizes (ADR 0029).</li><li><strong>A Config rule that ignores a class of Tickets on sight.</strong> Rejected for the same reach, and the judgment is per item: the class an operator judges out today holds the one item in it they want next week.</li><li><strong>Ignore any state, absolutely.</strong> Rejected: it hides the obligations the operator owes, stalls the factory behind an invisible Dispatch pause, and leaves a Missing agent with no surface for its restart-or-abandon. ADR 0042 rejected the same shape for the covered rule on the same ground: the row is how the operator reaches the Live view, Close, Goto, and the decision.</li><li><strong>Keep the row and mark it parked instead.</strong> Rejected: the row is the pain, and a listed row stays a top-up candidate every walk has to be told about again.</li><li><strong>Reveal the pile as a value of the Grouping axis (ADR 0058).</strong> Rejected: the axis is single-valued, so reading the pile would cost the grouping the operator chose, and the flat axis value means the list as it stands, row for row, which a mechanism that removes rows contradicts. The axis slices the rows that show; the ignore decides which rows exist.</li><li><strong>A second key for the un-ignore.</strong> Rejected: one key whose meaning the row&#39;s own fact picks is how Enter already works, and a second letter is one more the Key guide has to hold.</li><li><strong>Ignoring Consultations in the same act.</strong> Rejected: a closed Consultation already leaves its list behind the history filter, and an open one is work the operator started and can still reach.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The state file gains the flag and the moment it was set, and the migration follows the file&#39;s own rule: ask the file, not the stamp. The step is version 22, the two columns on the ticket row: ADR 0058&#39;s Grouping axis took version 21 while this work was open, so the ignore lands behind it.</li><li>The Ticket header&#39;s <code>open</code> count stops counting ignored tickets, so the counts no longer match the source&#39;s own list, on purpose. The <code>ignored n</code> cell is the only bridge, and it is a count, not a filter control. The count, the four pipeline counts, and the held-count bell all read the active view, so a cycle of the List filter moves none of them and rings nothing; only the rows the section draws follow the operator&#39;s view.</li><li>The list rule lives in the state module alone: one read returns the drawn rows, the active view, and the pile, and no screen re-applies the covered rule or the ignore rule beside it. The pile reads the flag over the projection before the list rule, so it is the ledger of the operator&#39;s acts, and the drawn rows and the active view read it after the covered rule, so the list stays ADR 0042&#39;s. The gate is one predicate, <code>ticketIgnored</code>, read from the row a walk already holds or, for the in-flight rows that carry no flag, from one read of the pile per cycle, so neither rule costs a second projection read on a refresh.</li><li>The missing-Agent fact is one rule too: one helper answers what the latest poll reports for a Ticket&#39;s pane, and the in-flight pass, the Restart walk, the Parallel limit seat count, and the list&#39;s failure badge all read it, so the obligation predicate, the badge, and the seat cannot drift apart.</li><li><code>i</code>, and the Ticket section&#39;s use of <code>f</code>, join the Control catalogue, the Action bar, and the Key guide in both Ticket modes.</li><li><code>f</code> now belongs to two sections, so the Work queue&#39;s refusal for it, which names the Consultation section alone (issue #85), needs words that hold for a key two lists own. This is the one place the feature makes an existing sentence untrue.</li><li>An ignored Ticket still holds a Parallel limit seat while its Agent works, still refreshes, still fires its Transition at a completed settle, and still appears wherever its identity is named. The ignore moves the row; it changes no fact. The reads that resolve a Ticket by identity - a Work queue row and the cancel line that removes it, an open panel and the Live view&#39;s pane read, a route&#39;s position, and a confirmed override - share one projection read over the rows that stand before the list rule, and the Consultation launcher&#39;s repository choices and live-checkout conflict names take the active view, so none of them follows the operator&#39;s filter and a withheld row still names itself. A row a panel can stand on is never a withheld row, because each panel reads a state the list rule keeps listed: the frames that pin that read are the queue&#39;s row title and its cancel line, and no state separates the two sources for the panel and the pane read.</li><li>Nothing prunes ignored flags, and nothing needs to: the plane never deletes a ticket row today, so the flag adds no surface the rows did not already have.</li><li><code>docs/operation/main-view.md</code> states current behavior, so it changes with the implementation and not with this decision: the sentence that gives <code>d</code> and <code>f</code> to the Consultation section alone, the header&#39;s counts, the two new keys, and the list&#39;s two causes that take a row away.</li><li><code>i</code> is reserved here. ADR 0058 left the Grouping axis&#39;s key unnamed and no control catalogue binds <code>i</code> today, so whoever implements the axis takes a letter that is not <code>i</code>, and the same holds for the other free letters this plane still has: <code>b n o t u v y z</code>.</li><li><code>f</code> is one key with two section owners, Ticket and Consultation, and no third. The Work queue keeps its refusal, and the refusal&#39;s words must name a key two lists own rather than the Consultation section alone.</li><li><code>CONTEXT.md</code> names the <strong>Ignored ticket</strong> and the <strong>List filter</strong>, and the List filter entry keeps itself apart from the Grouping axis and the fold.</li><li>The pile is the ledger of the operator&#39;s own acts, so the <code>ignored</code> view reads the flag over the projection that stands before the list rule, while the drawn rows and the active view both keep ADR 0042&#39;s covered rule. A Ticket that is ignored <em>and</em> covered therefore stands in the pile and in no other view: the covered rule takes it from the list, and the flag keeps it in the ledger. It is the one state where the two causes do not agree on one row, and it is settled for the operator&#39;s sake - a row the <code>ignored</code> view hides is a row the <code>i</code> key cannot reach, and only that key clears a flag.</li></ul>',20)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
