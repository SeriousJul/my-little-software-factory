import{_ as t,o as a,c as o,a0 as r}from"./chunks/framework.D4fUSYzX.js";const u=JSON.parse('{"title":"ADR 0062: The plane moves a leftover worktree directory aside, and never deletes it","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0062-the-plane-moves-a-leftover-worktree-directory-aside-and-never-deletes-it.md","filePath":"adr/0062-the-plane-moves-a-leftover-worktree-directory-aside-and-never-deletes-it.md"}'),s={name:"adr/0062-the-plane-moves-a-leftover-worktree-directory-aside-and-never-deletes-it.md"};function n(i,e,d,h,c,l){return a(),o("div",null,[...e[0]||(e[0]=[r(`<h1 id="adr-0062-the-plane-moves-a-leftover-worktree-directory-aside-and-never-deletes-it" tabindex="-1">ADR 0062: The plane moves a leftover worktree directory aside, and never deletes it <a class="header-anchor" href="#adr-0062-the-plane-moves-a-leftover-worktree-directory-aside-and-never-deletes-it" aria-label="Permalink to &quot;ADR 0062: The plane moves a leftover worktree directory aside, and never deletes it&quot;">​</a></h1><p>Status: accepted Date: 2026-09-25</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A worktree Handoff asks herdr to create its checkout, and herdr names that checkout after the branch: <code>factory/&lt;ticket id&gt;-&lt;title slug&gt;</code>, with its slashes for hyphens, inside the repository&#39;s herdr worktree directory (ADR 0046 reads the same rule). The name is therefore fixed: the same ticket always asks herdr for the same path.</p><p>That fixed path can stand on disk while nothing owns it. Observed on ticket #37 of <code>seriousjul.github.io</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.herdr/worktrees/seriousjul.github.io/factory-37-deps-.../          exists, 44K</span></span>
<span class="line"><span>  .docusaurus/**                                                     the only content</span></span>
<span class="line"><span>  .git                                                               absent</span></span>
<span class="line"><span>git worktree list --porcelain                                        no record of the path</span></span>
<span class="line"><span>.git/worktrees/                                                      no record of the path</span></span>
<span class="line"><span>git branch --list factory/37-...                                      the branch exists, checked out nowhere</span></span></code></pre></div><p>A build cache an Agent wrote recreates the directory after its checkout is removed, and the removal had already dropped git&#39;s record. Git will check a branch out into an existing empty directory and refuse a non-empty one:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ git worktree add ./empty2 probe/branch   -&gt;  Preparing worktree (checking out &#39;probe/branch&#39;)</span></span>
<span class="line"><span>$ git worktree add ./nonempty probe/branch -&gt;  fatal: &#39;./nonempty&#39; already exists   (and -f does not lift it)</span></span></code></pre></div><p>So the reuse sequence dead-ends. The branch exists, so the Handoff takes the reuse path; herdr finds no worktree holding the branch; the path lookup finds no worktree at the reserved name; the fresh create is refused by the directory. Every retry answers the same way, forever, and the ticket&#39;s committed work strands on its own branch.</p><p>Neither tool clears it. <code>herdr worktree remove</code> needs a workspace, and herdr holds none. herdr&#39;s own remove recovery refuses this shape too: it checks the leftover&#39;s <code>.git</code> file for a gitdir inside the repository, and the directory has no <code>.git</code> at all. <code>git worktree prune</code> clears records whose directory is gone; this is the opposite - a directory whose record is gone.</p><p>ADR 0012 settled the destructive answer for a neighbouring case: a leftover environment is a fact the operator acts on, because &quot;discarding someone&#39;s uncommitted work is never the side effect of an unrelated action&quot;. This case sits on the same rocks with different tools: the residue is not a herdr environment, so the plane&#39;s own surfaces show nothing and herdr offers no action, and the operator has to read a filesystem path out of git&#39;s refusal and delete files by hand.</p><p>The alternatives:</p><ul><li>Delete the leftover directory, then create. Rejected for ADR 0012&#39;s reason: the plane cannot tell a build cache from an Agent&#39;s uncommitted, untracked work, and it must not guess.</li><li>Report the fact and do nothing else. Rejected as the whole answer: the ticket stays unworkable until the operator notices a path in a truncated Message line, and the plane knows exactly which directory it needs.</li><li>Ask herdr for the worktree at a different path. Rejected: the naming rule is how the plane and the operator find a ticket&#39;s checkout, and a second name for the same ticket breaks the reopen lookup that rule serves (ADR 0046).</li><li><code>git worktree prune</code> before a create. Rejected here, kept as a separate question: prune answers a stale record, not an unowned directory, and this residue is the latter.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The plane moves a leftover worktree directory aside, and never deletes one.</strong></p><p>One create refusal is recoverable, and the plane recovers it once: before it asks herdr for the create again, it renames the reserved path out of the way to <code>&lt;path&gt;.leftover</code>, or the next free numbered name beside it, and carries the fact on the Handoff&#39;s notes, which reach the Message line whether or not the start then lands.</p><p>Three proofs gate the rename, and each closes a door the plane must not walk through:</p><ul><li><strong>The naming rule names the path.</strong> The path comes from herdr&#39;s own <code>worktree list</code>, the same derivation ADR 0046 established. A list that does not read names no path, so no directory is touched.</li><li><strong>git holds no record of the path.</strong> A recorded worktree - standing or prunable - is herdr&#39;s and git&#39;s to open or remove. The plane moves nothing a tool still owns.</li><li><strong>The directory holds no <code>.git</code> entry, and holds something.</strong> A directory with a <code>.git</code> entry is a checkout, and the plane never moves a checkout. An empty directory blocks no create, so a refusal beside one is about something else, and moving it would be a side effect with no cause.</li></ul><p>The move keeps every byte: the operator sees where the residue went in the Handoff&#39;s line, and can inspect it or remove it. The plane deletes nothing, anywhere, in this path.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A ticket whose reserved path was resurrected by a build cache starts again on its own, with one warning line that names both paths. The permanent dead end is gone for the shape above.</li><li>herdr&#39;s worktree root gains <code>.leftover</code> directories over time. That is the cost of choosing a move over a delete, and the plane says where each one landed rather than hiding it. The operator removes them, not the plane.</li><li>A refusal the proofs do not explain comes back exactly as it was: git&#39;s own line, and herdr&#39;s stable code beside it. The plane adds no guess to a refusal it cannot name.</li><li>A stale git record for a directory that is gone still blocks a create, and still needs <code>git worktree prune</code> or <code>git worktree remove</code> to clear. The plane reports that refusal and does not act on it; if it becomes common, prune is its own decision, at the same seam.</li><li>The Handoff notes carry one more fact, and both report sites read it: the ticket Handoff&#39;s outcome line and the Consultation&#39;s own. The move line has no success gate, because a moved directory is a fact the operator needs even when the start fails after it.</li><li>The reopen lookup and this rule read one derivation of herdr&#39;s naming rule, so the two can never disagree about where a ticket&#39;s worktree lives.</li></ul>`,21)])])}const f=t(s,[["render",n]]);export{u as __pageData,f as default};
