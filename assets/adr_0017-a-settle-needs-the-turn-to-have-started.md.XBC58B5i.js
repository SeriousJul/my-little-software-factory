import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.ysVzM4be.js";const p=JSON.parse('{"title":"ADR 0017: A settle needs the turn to have started","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0017-a-settle-needs-the-turn-to-have-started.md","filePath":"adr/0017-a-settle-needs-the-turn-to-have-started.md"}'),r={name:"adr/0017-a-settle-needs-the-turn-to-have-started.md"};function n(d,e,i,h,l,c){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="adr-0017-a-settle-needs-the-turn-to-have-started" tabindex="-1">ADR 0017: A settle needs the turn to have started <a class="header-anchor" href="#adr-0017-a-settle-needs-the-turn-to-have-started" aria-label="Permalink to &quot;ADR 0017: A settle needs the turn to have started&quot;">​</a></h1><p>Status: accepted Date: 2026-09-13</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Startup grace defends a fresh Handoff against the window in which a booting agent reports idle before it picks up the prompt. The grace guards only the <code>handed-off</code> state: the moment herdr reports the agent <code>working</code>, the ticket runs, and the next idle or done report settles it at once. The working report was the proof that the turn demonstrably started.</p><p>Herdr&#39;s working report is not that proof. ADR 0015 already rejects herdr&#39;s status as the source of the turn end cause: it is herdr&#39;s view of the pane, not the runtime&#39;s record of the turn. The same holds for the settle gate. Measured against the dev state, a codex handoff into a directory whose hooks were not reviewed settled 17 to 20 seconds after the start, auto-closed the cycle, and its trace held the boot dialog as the turn&#39;s last message: herdr flapped <code>working</code> during boot, the flap dropped the grace, and the parked agent&#39;s idle settled a turn that never started. The task was recorded done without running.</p><p>The session record the settle already reads (ADR 0008, ADR 0015) is the evidence: it holds the turn when the turn ran, and it holds no turn when it did not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>The gate reads the record, not the status.</strong> A done or idle report settles a fresh Handoff when the session record shows the turn ended, or when the Startup grace is over. A working report no longer drops the grace: the ticket still runs and still holds its seat, but the grace stays until the record or the clock says otherwise. A real turn that ends inside the grace settles at once, because its end is in the record; nothing that runs waits longer than it does today.</p><p><strong>The read has three answers.</strong> One read of the record returns: the turn ended, with its log and cause; the record is readable and well-formed but holds no turn; or the record is unavailable, the reader knows no such kind, or the record is unreadable. The first settles at once. The second and the third wait out the grace.</p><p><strong><code>no-turn</code> is a turn end cause, and it holds.</strong> The vocabulary ADR 0015 closed at five values grows a sixth: <code>no-turn</code>, the readable record with no turn in it. It is a held cause alongside <code>failed</code>, <code>aborted</code>, and <code>truncated</code> (ADR 0016): a settle that reaches it waits the grace out, settles with the terminal capture standing in for the log, and rests in <code>awaiting</code> with no automatic decision on it. The operator reads the hold and decides. It does not pause the Dispatch: a turn that never started is a local Handoff event, not the system pressure <code>failed</code> names.</p><p><strong>Unavailable still fails open.</strong> No session reported, no reader for the kind, or a missing, unreadable, or malformed record settles <code>unknown</code> past the grace, exactly as ADR 0015 degrades it today: auto-decides, never blocks. A record that is readable but empty of turns is not unavailable; conflating the two would let a parked agent auto-close its cycle on a capture of its own boot screen.</p><p><strong>The staleness guard is unchanged.</strong> A record whose last turn end predates the Handoff settles <code>unknown</code> at once, as it does today: the record says a turn ended, and the guard only refuses to call it this turn&#39;s completion.</p><p><strong>Consultations fail open on <code>no-turn</code>.</strong> A Consultation turn that settles on a record with no turn settles <code>unknown</code> and rests in <code>awaiting-response</code>, as an unreadable record does today. The ticket&#39;s auto-close is the damage this ADR stops; a Consultation auto-decides nothing, and its operator is present at the screen it settles on.</p><h2 id="considered-alternatives" tabindex="-1">Considered alternatives <a class="header-anchor" href="#considered-alternatives" aria-label="Permalink to &quot;Considered alternatives&quot;">​</a></h2><ul><li>Keep the working report as proof and lengthen the grace. Rejected: the proof is herdr&#39;s view of the pane (ADR 0015&#39;s rejection still stands), and no grace length out-waits an agent parked in a dialog that never ends.</li><li>Settle <code>no-turn</code> as <code>unknown</code> and only fix the gate. Rejected: the parked agent still settles past the grace and still auto-closes its cycle; the operator still sees a task done that never ran, thirty seconds later.</li><li>Never settle a turn the record does not show. Rejected: a parked agent would hold its parallel seat forever, with no trace and no operator surface; settling held keeps the fact durable and the seat freed.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The vocabulary ADR 0015 closed at five values holds six. Legacy traces predate <code>no-turn</code> and read their stored causes unchanged.</li><li>A real turn that ends inside the grace settles at once, on its record: the gate&#39;s evidence is the turn end, so a fast turn does not wait the booting window out.</li><li>A single working report - the flap - can no longer settle a fresh Handoff early. The startup window&#39;s protection is the record and the clock, not a status the pane can flicker.</li><li>A parked agent settles <code>no-turn</code> past the grace, rests held in <code>awaiting</code>, and names itself on the Message line; the operator decides it, and the cycle it held is not closed on a boot screen.</li><li>The Dispatch pause is untouched: it still reads only the held <code>failed</code> traces.</li></ul>',17)])])}const g=t(r,[["render",n]]);export{p as __pageData,g as default};
